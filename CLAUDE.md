# CLAUDE.md

## 応答ルール

1. 簡潔で明確な日本語を使用
2. 「です・ます」調は使うが、冗長な敬語表現は避ける
3. 「いたします」→「します」、「ございます」→「あります」に置換
4. クッション言葉(恐れ入りますが、等)は省略

### 実行環境

- Windows の Git Bash、Windows の WSL、Claude Code on the web (Linux)、macOS の 4 つ。どれでも動くように書く
- macOS の `sh` は bash 3.2 で、`sed` などは BSD 版。変数のすぐ後ろに全角文字を続けるときは `${var}` と括る
  （3.2 は全角の先頭バイトまで名前に読む）。`$( )` の中に `case` を書かない（3.2 は `)` を読み違える）。
  どちらも `tests/core/test_sh_portability.py` が見る
- 使える道具は `jq` 1.6、Node 22 (pnpm 10)、Python 3.12 (uv)、go。これ以外がある前提で書かない

### 呼び名

- **ワークスペースルート** = Claude Code を起動した場所（`CLAUDE_PROJECT_DIR`）
- **git プロジェクトルート** = `.git` がある場所。ワークスペース自身、`projects/` の下の各プロジェクト、
  ワークツリーのそれぞれが持つ
- **統合先ブランチ** = 作業を合流させるブランチ

### プロジェクトを置いて作業するとき

ワークスペースの下に別のリポジトリを clone して直すことがある（設計 §11）。置き場は
`projects/<名前>/` で、**直下の 1 段だけ**。それより深い場所と置き場の外はプロジェクトと数えない。

- **プロジェクトの作業は、プロジェクトに入ってから始める。** `cd projects/<名前>` してから
  `ccnavi-git.sh` を打つ。git の操作はそのリポジトリに対して行われる
- **プロジェクトのリポジトリに入るのは、設定 3 本（`.ccnavi/config/` の `rules.yml`・`phases.yml`・`risks.yml`）と、
  そのプロジェクト向けの提案（`wip/proposals/`）・承認済みチケット・フェーズのマーカー（`.ccnavi/approved/`）。**
  承認とフェーズの進みは親チケットのブランチに乗って他の機械へ届き、clone すれば続きができる（設計 §9.2、REQ-MLT-14）。
  記録と控え（`logs/`）はワークスペース側で、git には入れない
- **チケットは `project:` を持つ。** 値は `projects/` の名前で、決めるのは人の承認。
  エージェントが申告した値は判定に使われない
- `projects/` が無いか空なら、以下はすべてワークスペース自身の作業として読めばよい

### ソースコード編集方法

- 他セッションでも並行して作業が進められるよう、ワークツリーを切ってから手を付けること
- git は直接呼ばず `ccnavi-git.sh` を通す。通る形はそのスクリプトの `--help` に出る
- **sh の綴りは、どこから打つかで変わる。** `.ccnavi/scripts/` はワークスペースにしか無い

  | cwd | 綴り |
  |---|---|
  | ワークスペースルート | `sh .ccnavi/scripts/ccnavi-git.sh ...` |
  | ワークツリーの中 | `sh ../../../.ccnavi/scripts/ccnavi-git.sh ...` |
  | `projects/<名前>` の中 | `sh ../../.ccnavi/scripts/ccnavi-git.sh ...` |

  拒否の文面が案内する綴りは `{root}` から始まる絶対パスで出る。そのまま打てばどこからでも通る

#### ワークツリーの作り方

- 置き場は**ワークスペースの** `.claude/worktrees/<ブランチ名>`。プロジェクトから切る場合も同じ。
  1 段だけで、その下に階層を作らない
- `ccnavi-git.sh worktree add <行き先> -b <名前> <統合先>` で作る。起点は `<統合先>` の HEAD。
  末尾を省くと今いるブランチが起点になるので、`<統合先>` が `main` のときも省かずに書く
- `<行き先>` は cwd から解かれる。ワークスペースルートからなら `.claude/worktrees/<名前>`、
  `projects/<名前>` の中からなら `../../.claude/worktrees/<名前>`。**間違えると止まり、
  文面が正しい綴りを出す**ので、出たとおりに打ち直せばよい
- `worktree add` に渡せるオプションは決まっている（`-b` `-B` `--reason` `--detach` `--force` など）。
  一覧に無いものは通らない。行き先を取り違えないためで、要るものが出たら利用者に足してもらう
- **プロジェクトのワークツリーは、そのプロジェクトから切る。** `cd projects/<名前>` してから打つ。
  `<統合先>` はそのプロジェクトのブランチであって、ワークスペースの `main` ではない
- `<統合先>` がリモートにしか無いときは、先に `ccnavi-git.sh fetch <リモート> <統合先>` をする。
  これもプロジェクトの中で打つ
- **起点はリモートの最新のデフォルトブランチ**（`origin/HEAD` が指すもの。多くは `main`）。
  セッションの頭の `ccnavi-fetch.sh` が、チェックアウトされていなくても ff で進めるので、
  ワークスペースルートが `main` 以外に居ても揃う（ADR-0060）。進められなかったときは
  「ワークツリーの起点になる ... 」で始まる行が頭に出ている。その行が出ていたら、切る前に
  理由を片付ける（分岐なら人に合流させてもらう、未コミットならそのツリーの持ち主に聞く）。
  セッションが長引いてリモートが進んだときは、`ccnavi-git.sh fetch origin <統合先>` を打ち直す
- 作業が完了したら `<統合先>` へ戻す。コンフリクトして判断に困る場合はユーザに確認する

#### 下書きと使い捨ての置き場

本番に入れないもの（設定の写し、再現用のスクリプト、調べた出力、渡す前の下書き）は
**そのワークツリーの `scratchpad/`** に置く。名前が `tmp` でないのは、`tmp/` を追跡対象として
使うリポジトリが実在するため（Rails は `tmp/` の下に置き石を追跡させる）。追跡されている
場所を外すと、承認した範囲の外のものが統合先へ乗る道ができる。

- `.gitignore` に入っているので追跡されず、`git status` にも出ない。`worktree remove` は
  止まらずに通り、ワークツリーを畳めば一緒に消える
- ワークスペースの中にあるので、利用者がエディタからそのまま開ける。**渡すときは
  必ずパスを言う。** 開けない場所に置いた下書きは、無いのと同じ
- **チケットが結び付いたワークツリーでも、そのまま書ける。** 実行前の判定はチケットの範囲を
  `scratchpad/` に当てない。範囲に `scratchpad/*` を足す必要は無い
- **外れるのはルートの直下 1 段の `scratchpad/` だけ。** 綴りの大文字小文字も区別する。
  `SCRATCHPAD/`・`scratchpadX/`・`docs/scratchpad/` と、`scratchpad` という名前のファイルは普通の作業対象で、範囲の
  外なら止まる。`.gitignore` が追跡から外す範囲と一致させてある
- **`scratchpad/` が追跡されているリポジトリでは、下書きが範囲外として報告される。** 実行後の
  監視とサブエージェント終了時の検査は `scratchpad/` を外さない。報告が出たら、下書きを消すの
  ではなく、そのリポジトリの `.gitignore`（または `.git/info/exclude`）に `/scratchpad/` が
  入っているかを見る。`ccnavi --lint` が同じことを警告で言う
- **ワークツリーがまだ無いときは、ワークスペースの外にあるセッションのスクラッチパッドを
  使う。** ワークスペースルートで設定の下書きを作る場面がこれにあたる。ルート直下は
  `main-tree` が止めるので、そこに `scratchpad/` を作っても書けない。セッション側の綴りは
  Environment 欄が持つ。同じ「下書きを置く場所」が 2 つあり、**ワークツリーの中なら
  `scratchpad/`、無ければセッションのほう**、と読み分ける

#### 他セッションの作業を踏まないために

- 自分のワークツリーの外（ワークスペースルートや他のワークツリー）に未コミットの変更があったら、
  それは他セッションのものとして扱う。
- 他セッションの書きかけを、コミットも、ブランチの移動も、`reset` もしない。
  ビルドが壊れていても直さない。何が壊れているかを報告して判断を仰ぐ。
- 退避が要るときは `ccnavi-git.sh stash push -u` を使う。消す前に下書きの置き場へコピーも取る
  （自分のワークツリーの中なら `scratchpad/`、無ければワークスペースの外にあるセッションの
  スクラッチパッド）。他セッションのツリーの中には置かない。

#### `<統合先>` へ戻す

- ワークツリーのブランチが `<統合先>` の HEAD の直上なら fast-forward で入る
- `<統合先>` が先に進んでいたら、ワークツリーの中で `ccnavi-git.sh merge <統合先>` して取り込み、
  衝突を解いてから戻す
- `<統合先>` が保護されていて直接は入れられないなら、マージまでやらない。
- 直接 `<統合先>` に戻した場合は、統合が済んでから `ccnavi-git.sh worktree remove <パス>` と
  `ccnavi-git.sh branch -d <名前>` で片付ける
- マージリクエストに出した場合は、マージを待たず、`ready` で Draft を外した直後にワークツリーを片付ける。
  順は「`ready` → マーカー `.ccnavi/approved/phases/<親>/ready.json` をコミットして push →
  `ccnavi-clean.sh` → `worktree remove`」。`ready` が置くマーカーは push の後に書かれるので、
  コミットせずにワークツリーを消すとマーカーが消える。親に取り込んで閉じた子のワークツリーも、
  このときに外す（前提の検査は子のブランチを引くので、ワークツリーが無くても外れない）
- マージリクエストに出したブランチは消さない。リモートはホストがマージのときに消す（GitHub の
  「マージ後にブランチを自動削除」）。ローカルは squash で入るので `branch -d` が通らず、
  ラッパースクリプトは `-D` を通さない。残ったローカルのブランチは利用者が消す
- `worktree remove` の前に `ccnavi-clean.sh <名前>` で生成物（node_modules・.venv など）を消す。
  Windows では、これが残っていると削除が途中で止まり、消しきれなかったディレクトリが残る。未コミットの変更がある
  ワークツリーでは何も消さずに止まる

#### 何がどこに置かれるか

プロジェクトを置いていても、置いていなくても、同じ表で読める。

| 何 | 場所 |
|---|---|
| 提案（承認待ち `todo/`、レビュー待ち `review/`） | そのツリーの `wip/proposals/<状態>/`。プロジェクト向けは `projects/<名前>/wip/proposals/<状態>/`（そのプロジェクトの git が持つ） |
| 承認済みチケット（作業中 `doing/`、閉じた `done/`）、フェーズのマーカー、子の記録（`.risk.json` など） | 親チケットのツリーの `.ccnavi/approved/`。プロジェクト向けは `projects/<名前>/.ccnavi/approved/`（そのプロジェクトの git が持ち、親のブランチに乗って他の機械へ届く） |
| git のラッパースクリプトの記録 | ワークスペースの `logs/<プロジェクト>/`。ワークスペース自身は `logs/` |
| 判定の記録と控え | ワークスペースの `logs/log.jsonl` と `logs/state/` |
| ワークツリー | ワークスペースの `.claude/worktrees/<名前>` |
| 下書きと使い捨て | そのワークツリーの `scratchpad/`（追跡しない）。ワークツリーが無いときは、ワークスペースの外にあるセッションのスクラッチパッド |
| ワークスペースのルール | `.ccnavi/common/rules.yml`（共通層。リスクの配点も同じ場所）。フェーズの種類は自身の層 `.ccnavi/config/phases.yml` |
| プロジェクトの設定 | `projects/<名前>/.ccnavi/config/`（`rules.yml`・`phases.yml`・`risks.yml`）。正本はここ。親の着手のときに、共通層にあるファイルはその中身で上書きされる（設計 §11.12） |

**プロジェクトのリポジトリに入るのは、提案・承認済みチケットとマーカー・プロジェクトの設定の 3 行。** 記録と控え（`logs/`）と
ワークツリーはワークスペース側で、git には入れない。別の機械で続きをするのに要るもの（依頼時の HEAD、リスクの点、受け入れた指摘、
Draft を外した印）は全部 `.ccnavi/approved/phases/<親>/` にあり、clone で届く。

チケットは 1 本のファイルで、`wip/proposals/todo/`（承認待ち）→ `.ccnavi/approved/doing/`（人が承認）→
`wip/proposals/review/`（`ticket finish`。レビュー要のとき）→ `.ccnavi/approved/done/`（人がレビュー）と
動く（ADR-0055）。`.ccnavi/approved/` へ動かすのは人、`wip/proposals/` へ動かすのはエージェント
（`ccnavi-ticket.sh` 経由）。レビューで残った指摘は、人が `decide`（ボードの「決める」か端末）で、指摘ごとに
「対応しない」「このフェーズで直す（続きの子チケットを `doing/` に起こす）」「issue に回す」を選ぶ。

ラッパースクリプトが返す記録の綴りは、cwd から開ける形で出る。そのまま `sed -n` などで開けばよい。

#### ルールの文面に書く sh の綴り

`rules.yml` の `message` で sh を案内するときは `{root}` から書く。

```yaml
message: 生の git は実行しません。'sh {root}/.ccnavi/scripts/ccnavi-git.sh ...' を使ってください。
```

`{root}` はルールを読むときにワークスペースルートの絶対パスに置き換わる。相対で
`sh .ccnavi/scripts/...` と書くと、`cwd` がプロジェクトの中にあるときに届かないパスを案内することになる。

## 提案と判断

- 提案には必ずメリットとデメリットを両方書く。得るものだけを書いた提案は提案ではない。
  デメリットが思いつかないなら、探し方が足りない。本当に代償が無いなら「無い」と書く
- 不安定な方向に倒れうる対応は、自分で決めずに相談する。次のどれかに当たったら、
  手を動かす前に「何をなぜ変えるか・得るもの・失うもの・やらない場合どうなるか・代案」を
  出して待つ
  - 止まっていたものが通るようになる（判定が緩む、例外が増える、確認が減る）
  - 判定を握りつぶす、条件を狭める、報告を消す
  - 戻せなくなる（消す、履歴を書き換える、外へ出す、他セッションの作業に触る）
  - 影響範囲が読み切れない（共有の設定、hook、スクリプト、配布物、他のプロジェクトへ配るもの）
- 調べること自体は相談を待たない。再現させる、読む、隔離した場所で試す、はいつでもやってよい。
  相談が要るのは、その結果をこのリポジトリに入れるとき

## セッションの役割分担

- メインのセッションは利用者とのやり取り（相談・判断・報告）を受け持つ。
  実装・調査・テストなど手を動かす作業は、サブエージェントをバックグラウンドで起動して任せる
- 任せないもの: 1 ファイルで済む小さな確認や修正、レビュー準備中以降の合流・push・依頼
  （サブエージェントの起動が止まる段）
- 並行で走らせるときは、サブエージェントごとに別のワークツリーを割り当てる
- サブエージェントは利用者に直接は聞けない。「提案と判断」の相談条件に当たったら、
  手を止めて何を聞きたいかを添えて返す。メインが利用者に聞き、答えを持って同じサブエージェントを再開させる
- メインはサブエージェントの報告を差分とテスト結果で確かめてから、利用者に伝える

## 実行ファイルの境界

- ccnavi の実行ファイルはネットワークに出ない。自分で見て判断するのは、プロジェクトの
  ディレクトリの中で把握できるもの（作業ツリー、git、承認済みチケット、マーカー、hook の payload）だけ
- その外にあるもの（マージリクエスト、レビューのスレッド、ホストの API、認証）は
  `.ccnavi/scripts/` の sh が取ってきて、`--result` に渡す。実行ファイルが持つのは
  写しを読んでチケットとマーカーを動かすところだけ
- 止める・通すの判定は hook と実行ファイルが持つ（P13）。sh が ccnavi の状態を読んで止めるのは 2 重目の守りで、
  同じことを hook が先に止める。sh に新しく判定を足したくなったら、hook の側に置けないかを先に考える
- 実行ファイルに「外を見に行く」コードを足さない。足したくなったら sh の仕事に分け、
  実行ファイルには「その結果をどう読むか」だけを足す。写しの形が sh と実行ファイルの契約で、
  テストは sh の代わりに写しを渡す