---
version: 1
ticket: i0054-01
parent: i0054
phase: 1
title: 共通層が .ccnavi/common/ 固定であることの受入テストを書く
human_review:
  required: true
  reason: 判定に当てる設定をどこから読むかを変えるため
rationale: '実装より先に「env を渡しても共通層は動かない」「動かないままでも守りは同じ」を

  テストで言う。書いた時点では落ちる。

  '
allow:
- match: Write|Edit
  glob: tests/*
started_at: ''
completed_at: 2026-09-16T10:49:19+0000
base_sha: ''
ccnavi_approved:
  approved_at: 2026-09-16T19:40:48+0900
  source_tree: ''
  source_path: C:\Users\taniyama\Desktop\git\ccnavi\wip\tickets\todo\i0054-01.md
cancelled_at: ''
cancel_reason: ''
---

## 書くテスト

| 何を言うテストか | 置き場 |
|---|---|
| `CCNAVI_RULES` を渡しても `conf.rules` が `.ccnavi/common/rules.yml` のまま | `tests/config/` |
| `CCNAVI_PHASES` / `CCNAVI_RISK` も同じく動かない | `tests/config/` |
| 共通層の 3 本への書き込みが名指しのツールで止まる（`builtin-guard-common-layer`） | 既存を流用して固定の置き場で確かめる |
| 共通層の 3 本への書き込みがシェルで止まる（`builtin-guard-setting-files`） | 同上 |
| ワークツリー側の写し（`.claude/worktrees/<名前>/.ccnavi/common/rules.yml`）も止まる | 既存の形を保つ |
| `scripts/ccnavi-setup.sh` が配る `env` に `CCNAVI_RULES` が無い | `tests/sh/test_setup.py` |

最後の 1 本は既存の `REQUIRED_ENV` の検査と逆を言うので、この番号では**足すだけ**にして、
古いほうを消すのは次の番号（implement）で行う。並行して直すと、どちらの番号でも
テストが落ちた状態が残る。

## 確かめ方

- 書いたテストが**落ちる**こと（実装前なので落ちて正しい）を出力で示す
- 既存の 917 件が、足したぶんを除いて通ったままであること

## この番号でやらないこと

`ccnavi/` と `scripts/` と `vscode-extension/` には触らない。範囲も `tests/*` だけ。
