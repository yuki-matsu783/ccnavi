# ccnavi 設計書

この文書は、いまの実装がどう作られているかを書く。読む前に位置づけを押さえる。

| 知りたいこと | 読む場所 |
|---|---|
| 外から観測できる約束 | [requirements.md](requirements.md) |
| 使い方と設定、ルールの書き方、記録の読み方 | [README.md](README.md) |
| 実装の現状と次の一手、未実装 | [HANDOVER.md](HANDOVER.md) |
| なぜそう決めたか、以前はどうだったか、採らなかった案 | [docs/adr/](docs/adr/README.md)。この文書を書き直した経緯は ADR-0037 |
| 用語 | [CONTEXT.md](CONTEXT.md) |

ここに書くのは実装の理屈と構造で、経緯は書かない。「なぜ」が気になったら ADR を引く。
文中の `ADR-NNNN` はその番号の 1 枚を指す。実装と食い違いを見つけたら、要件書を正として直す。

---

## 1. 概要

### 1.1 何を解くか

Claude Code のエージェントは、作業の途中で禁止された操作をどうにか突破して実行しようとする。
悪意ではなく、作業に必要だと判断した結果として起きる。「hook が邪魔なので外す」「deny が厳しいので
緩める」も、目的を達成しようとする素直なエージェントが普通に選ぶ手になる。

既存の防ぎ方には穴がある。設定ファイルのパターン一致はコマンドの構造（連鎖・サブシェル・
リダイレクト）を見ないため複合コマンドの後段が素通りする。プロンプトに書いた禁止は確率を
下げるだけで 0 にはならない。権限を静的に固定すると、広く開けるか毎回確認するかの二択になり、
毎回確認すれば承認が形骸化する。拒否の根拠がすべて作業ツリーの中にあり、エージェントが
書き換えられる。

ccnavi は Claude Code の hook から呼ばれ、危ないツール呼び出しを止め、同時に「代わりに何を
すればよいか」を名指しで返す。止めるだけでは足りない。`git push` を禁じられたエージェントは
諦めず絶対パスで打ち直すので、拒否は目的を諦めさせるのではなく別の道へ向け直さないと効かない。

### 1.2 骨子

| 骨子 | 中身 |
|---|---|
| 判定はルールで、既定は委ねる | ルールファイルの `deny` / `ask` / `allow` に当たればその判定。当たらなければ ccnavi は判定を持たず、Claude Code の権限モードに従う（§6.5） |
| 引数に現れない書き込みは実行後に拾う | 実行前の判定はツール呼び出しの引数しか見ない。ビルドの出力やスクリプトの中のファイル操作は、走ったあとの作業ツリーを git で読んで拾う（§7） |
| ガード自身の設定はルールの外で守る | ルールを空にされた形も壊れた形も、ルールから導いた保護領域は 0 件になる。hook の登録・ルール・実行ファイルは組み込みで控えて戻す（§8） |
| チケットは絞る向きにだけ効く | 承認された範囲の外を止め、中は聞かない。ルールの判定と比べて強い側を採るので、ルールの `allow` も範囲の外では止まる。判定の鍵はファイルの行き先が属するワークツリー（§9） |
| 実行ファイルは自分のディレクトリの外を見ない | マージリクエストやレビューのスレッドは `.ccnavi/scripts/` の sh が取ってきて写しで渡す。実行ファイルはネットワークに出ない（§9.10） |

### 1.3 失敗の向き

失敗の向きは非対称。誤検知はエージェントが一手止まるだけで、理由が返れば書き直せる。素通りは
強制の仕組み自体を無意味にし、しかも起きたことに気づけない。判断がつかないときは拒否側へ倒す。

ただし機械的に当てると、設定 1 つの破損が完全な締め出しになる。「守るために自分を止めて回復手段
まで奪う」形は作らない。ルールが読めなければ組み込みの既定に落ちて判定を続け（§5.6）、未承認の
チケットは何も効かせず（§9.4）、設定ファイルを `Write` / `Edit` で直す道は既定では止めない。

---

## 2. 脅威モデルと守るもの

### 2.1 想定する主体

| 主体 | 説明 | 対象か |
|---|---|---|
| 逸れていく LLM | 悪意はないが、文脈の薄れ・誤解・思い込みにより破壊的な操作を行う | 主対象 |
| 誘導された LLM | 取り込んだページ・issue の本文・依存パッケージの文書に混じった指示に沿って動く | 主対象 |
| 事故 | 正常なコマンドの副作用、ビルドの想定外の出力 | 主対象 |
| 実行環境を掌握した攻撃者 | 同一ユーザー権限で任意のシェルを実行できる | 対象外 |

同じ権限で動く hook は、任意のシェル実行が可能な主体に対して原理的に境界にならない。ccnavi は
LLM の逸脱と事故に対する柵で、最終防衛線は OS・DB・実行環境の権限分離が引く（§12）。

判定は呼び出しの内容だけから決め、その呼び出しを引き起こしたテキストの出どころでは変えない
（REQ-PRE-09）。「利用者が頼んだ操作だから通す」を実装する材料は hook の入力に無く、出どころを
理由にした例外は注入された文からも同じように名乗れる。

### 2.2 守るもの

| 何 | 想定する被害 | 守る仕組み |
|---|---|---|
| hook の登録、ルール、実行ファイル、保護済みスクリプト、承認済みチケットとマーカー | 防御そのものの無効化 | 自己防衛（§8）とルールの `deny`、状態の置き場の組み込み拒否（§9.5） |
| 認証情報の置き場、取り返しの付かない操作（`rm -rf`、`git push`、`git reset --hard`） | 漏洩、消失 | 組み込みの既定（§5.6）とプロジェクトのルール |
| プロジェクトが `deny` / `ask` と宣言した場所 | 意図しない書き換え | 実行前の判定（§6）と実行後の監視（§7） |
| チケットが宣言した範囲の外 | 作業の範囲の逸脱 | チケットの範囲（§9.5）とサブエージェント終了時の検査（§9.12） |
| レビューを通っていない成果 | 人が見ていないものの合流 | フェーズの HITL ポイント（§9.8）とレビューの確認（§9.10） |

---

## 3. 設計原則

| # | 原則 | 中身 |
|---|---|---|
| P1 | 拒否側へ倒すが、回復の道は残す | 対象を確定できない呼び出しは許可にしない（§6.3）。設定が読めないのは「判断できない」ではなく「壊れている」なので、既定に落ちて直す道を残す（§5.6） |
| P2 | 宣言された制約は緩められない | ルールの `deny` / `ask` は、チケット・環境変数のどれによっても緩まない。チケットはルールと比べて強い側を採るので閉じる向きにしか効かず、ルールの `allow` も狭める（§9.5） |
| P3 | 言及の無い呼び出しは委ねる | ルールが言及しない呼び出しについて ccnavi は判定を持たず、権限モードに委ねて記録に残す（§6.5、ADR-0009） |
| P4 | 組み込みは最小 | ルールの外で持つのは、ガード自身を守るものと、取り返しの付かない操作だけ（§5.6、§8） |
| P5 | 判定の根拠を構造化する | すべての判定は理由コード・対象・当たったルールの識別子を持ち、記録と文面と試験の出力を同じ材料から作る（§6.4、付録 A） |
| P6 | 逸脱は必ず見せる | 委ねた回、既定に落ちた回、監視が見られなかった回も 1 行ずつ記録し、実行後に汚れた保護領域はモデルと人の両方に示す（§4.7、§7） |
| P7 | 静的と動的の二段 | 実行前のコマンド構造の解析と、実行後の git の状態の検証を併用する（§6、§7） |
| P8 | 拒否は誘導と一緒に返す | `deny` の文面は「なぜ止めたか、代わりに何をするか」を必ず持つ。持たないルールは受け付けない（§5.1） |
| P9 | 判定と試験は同じ関数 | `--test` と `--explain` と拡張の JSON は hook の判定と同じ関数を通る（§10、ADR-0016） |
| P10 | 人の判断は端末で受ける | 承認、未解決の受け入れ、締めは人が端末で打つ。チャットの「進めてよい」を hook は見られず、エージェントが人の合意を出せる形を作らない（§9.4、§9.10、ADR-0029） |
| P11 | 実行ファイルの境界は自分のディレクトリ | 実行ファイルが見て判断するのは作業ツリー、git のオブジェクトと参照、承認済みチケット、マーカー、hook の payload だけ。外にあるものは sh が写しで渡す（§9.10、ADR-0028） |
| P12 | 親のコンテキストは清潔に保つ | 親（メインエージェント）は計画・提案・子の起動・合流・レビューの依頼だけをし、作業は子に切り出す。ccnavi が親へ返す文も短く、詳細は子へ渡す（§9.7） |

---

## 4. 起動と設定

### 4.1 起動の形

実行ファイルは 1 本で、`.claude/settings.json` の hook として 7 つのイベントに同じコマンドで
登録する。標準入力の JSON（payload）から `hook_event_name` を読んでイベントを見分け、
標準出力に応答を返して終わる。登録側に引数でイベントを教えさせないのは、登録の書き間違いが
「別のイベントの判定が走る」という無言の食い違いになるため。

| 起動 | 挙動 |
|---|---|
| hook から（payload あり） | イベントごとの手順（§4.2）。終了コード 0 は通す、2 は止める・差し戻す |
| hook の入力なし | 使い方を標準エラーに出して終了コード 1。黙って 0 を返すと、設置を誤ったまま「動いている」と読める |
| 診断用のオプション（`--lint` `--test` `--test-samples` `--explain`） | payload を読まない。人と VS Code 拡張が使う（§10） |
| `--approve` / `--reviewed` / `ticket …` / `review …` | チケット制御の操作。人が端末で打つか、保護済みスクリプトが呼ぶ（§9） |

ワークスペースルートは `--root` → 環境変数 `CLAUDE_PROJECT_DIR` → 作業ディレクトリから上へ辿って
`.claude/` を持つ最初のディレクトリ、の順で決める。どの階層から起動しても同じ設定に行き着く。

配布物は PyInstaller の onedir（実行ファイルと同梱物 1 フォルダ）。実行時の依存は PyYAML 1 本で、
読むのは `safe_load` に限る（ADR-0002、ADR-0004）。常駐はしない。呼び出しごとに起動し、設定を
読み、判定して終わる（ADR-0003）。

### 4.2 hook のイベント

| イベント | すること | 応答 |
|---|---|---|
| `SessionStart` | コアファイルの控えを取る（実行ファイルはここだけ）。`additionalContextOnce` の記憶を捨てる。チケット制御が有効なら、直接作業とチケット作業の使い分けをモデルに渡す（§9.1） | `additionalContext` |
| `UserPromptSubmit` | 保護領域にいまある変更を控え、ターンの基準にする（§7.4） | 無し |
| `PreToolUse` | 呼び出しを判定する（§6）。コアファイルを控える（§8） | `permissionDecision` と `additionalContext` |
| `PostToolUse` | コアファイルを控えと突き合わせて戻す。作業ツリーを git で読み、保護領域の変更を報告し、設定に従って戻す（§7）。チケットの状態を承認済みチケットへ写し、フェーズの終わりを告げる（§9.8）。サブエージェントが差し戻しを無視して終わったことを親に言う | 終了コード 2 と標準エラー、または `additionalContext` |
| `Stop` | このターンで変わった保護領域を利用者へ報告する（§7.4） | `systemMessage` |
| `SubagentStart` | 承認済みで開いている子チケットの一覧を渡す（§9.12） | `additionalContext` |
| `SubagentStop` | 子のワークツリーに範囲外の変更が残っていれば 1 回だけ差し戻す（§9.12） | 終了コード 2 と標準エラー |
| その他 | 何もせず通す。記録に `event-not-checked` | 無し |

`matcher` は絞らない。絞ると、そこに書かなかったツールで hook 自体が起動しなくなる。
同じイベントに登録された他の hook の結果には依存しない。並行して順不同で走るため。

### 4.3 設定の解決

設定は環境変数 `CCNAVI_*` で渡す。プロジェクトは `.claude/settings.json` の `env` ブロックに書き、
Claude Code がセッションのプロセスに環境変数として渡す（ADR-0005）。読む順は
プロセス環境 → `ccnavi.settings.local.json`（ccnavi 自身のソースツリーでだけ読む、1 人の上書き）→
コマンドラインのオプション。変数の一覧と意味は README「設定」。

`disable` は起動側の環境からしか効かない（REQ-CMN-04、ADR-0006）。守り方は 2 段になっている。

モードを解決する段で見分けられるのは、`ccnavi.settings.local.json` が求めた `disable` まで。
それは無視して理由を出す。`.claude/settings.json` の `env` に書かれた `disable` は Claude Code が
プロセス環境として渡すので、ここでは「人が起動時に渡した `disable`」と区別が付かない。

区別の代わりに、そこへ書く経路そのものを閉じている。`.claude/settings.json` はコアファイル（§8）で、
`Write` / `Edit` はルールの `deny` が止め、シェルからの書き込みは組み込みが止め、すり抜けても
実行後に控えから戻る。加えて `env` は再読み込みされないので、書けたとしても同じセッションでは
効かない。効かせるにはセッションを開き直す必要があり、それは人が起こす。書かれていること自体は
`--lint` が error で名指しし、導入スクリプトは `--mode disable` を断る。

読めない値は報告して `enable` に倒す。緩む側には倒れない。

### 4.4 動作モード

| `CCNAVI_MODE` | 挙動 |
|---|---|
| `enable`（既定） | 判定し、`deny` なら止め、`ask` なら人に確認を出す |
| `dry-run` | 同じ判定を行い、呼び出しには手を出さず「`enable` なら何をしていたか」を `additionalContext` で伝える |
| `disable` | 判定しない。記録に `mode-disabled` を残す |

名前はガードそのものの状態を言う（ADR-0007）。戻す働きの 2 つ（`CCNAVI_RESTORE_IF_DENY`、
`CCNAVI_GUARD_CORE_FILES`）も同じ 3 値を取り、`CCNAVI_MODE=dry-run` のときはこちらが `enable` でも
`dry-run` として振る舞う。呼び出しにも作業ツリーにも手を出さないことがそのモードの約束で、
ファイルを動かすのはそれに反する。組み合わせの表は requirements.md §2.2。

`CCNAVI_GUARD_TICKET_APPROVAL` と `CCNAVI_TICKET_CONTROL`、`CCNAVI_GUARD_UNWATCHED` は
`enable` / `disable` の 2 値。承認は通れば済んでしまうので、「止めずに報告する」段が無い。
確認できる者が居ないモードの門（`CCNAVI_GUARD_UNWATCHED`、§6.5）は、報告する段を
`CCNAVI_MODE=dry-run` が持つので、こちらには要らない。

### 4.5 応答の形と着地

| 場合 | 応答 |
|---|---|
| `enable` の deny / ask | `{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny\|ask", "permissionDecisionReason": "…", "additionalContext": "…"}}`。`additionalContext` はルールが文を持つときだけ |
| `enable` の allow、委ねた回 | 判定は出さない。通知（既定に落ちた、コアファイルを戻した、ルールの文）があれば `additionalContext` だけ |
| `dry-run` の deny / ask | `additionalContext` に「enable なら止めていた / 聞いていた」と理由を並べる。終了コード 0 |
| `Stop` | `{"systemMessage": "…"}` |
| `PostToolUse` / `SubagentStop` の差し戻し | 標準エラーに文、終了コード 2。`dry-run` なら `additionalContext` で 0 |

期限は `PreToolUse` で 3 秒。ルール照合の途中で超えたら `deadline-exceeded` として `enable` は
終了コード 2、`dry-run` は 0（REQ-CMN-08）。遅延が許可に化けないための先回りで、外側で打ち切られた
場合には手が出ない。`PostToolUse` に期限は無く、git の読み取りが 2 秒で打ち切られる。

payload が JSON でない・オブジェクトでない・`hook_event_name` が無いときは `payload-unusable` で
同じ着地。記録を書けないときは標準エラーに出して捨て、判定は変えない。

### 4.6 導入と配布

`scripts/ccnavi-setup.sh <対象>` が、対象プロジェクトに設定を書き、道具を配る。何度打っても同じ形に
落ち着き、既にある値と、ccnavi と関係のない hook はそのまま残す。

| 書くもの | 中身 |
|---|---|
| `.claude/settings.json` の `env` | `CCNAVI_MODE` / `CCNAVI_LOG` / `CCNAVI_BIN_PATH` / `CCNAVI_RESTORE_IF_DENY` / `CCNAVI_GUARD_CORE_FILES` / `CCNAVI_GUARD_TICKET_APPROVAL` / `CCNAVI_GUARD_UNWATCHED` / `CCNAVI_TICKET_CONTROL`。`--all` で既定を持つつまみも並べる |
| `.claude/settings.json` の `hooks` | 7 つのイベントに実行ファイルを登録する。既に別の綴りで登録されていれば足さずに名前を挙げる |
| `.vscode/settings.json` | `git.detectWorktrees: true`。`--no-vscode` で触らない |
| 配るもの | `dist/ccnavi/` の中身を `.ccnavi/bin/<os>-<arch>/` へ、設定 3 本のひな形、`.ccnavi/scripts/ccnavi-{ticket,review,git,common,launcher}.sh`。配布先に既にあるものは触らず、`--force` のときだけ入れ替える。振り分けの sh は配った回に実行ビットを付け、配らなかった回でも落ちていれば付け直す（`--no-deploy` の回と、配布元と配布先が同じ回には触らない） |
| 配布先の `.gitignore` | 配った機械の置き場 `/.ccnavi/bin/<os>-<arch>/` の 1 行（配布先が git のリポジトリで、配るときだけ）。振り分けの sh は代わりに通る sh と同じく追跡する側に置き、無視しない |

**置き場は 2 つに分けて固定する（ADR-0044）。**

```
.ccnavi/scripts/ccnavi-launcher.sh      ← CCNAVI_BIN_PATH が指す振り分けの sh（追跡する）
.ccnavi/scripts/ccnavi-ticket.sh など   ← 代わりに通る sh
.ccnavi/bin/darwin-arm64/ccnavi         ← 機械ごとの組み立て（_internal/ も同じ置き場。無視する）
.ccnavi/bin/linux-x86_64/ccnavi
.ccnavi/bin/windows-x86_64/ccnavi.exe
```

`CCNAVI_BIN_PATH` が指すのは振り分けの sh で、実行ファイルは `.ccnavi/bin/` の下に機械ごとに並ぶ。`settings.json` は
どの環境でも同じものを開き、hook の `command` は 1 行なので、どの実行ファイルを起動するかは起動した
機械が決める。置き場の名前は `build.py` が書く `dist/ccnavi.target` の `<os>-<arch>` で、
語は `ccnavi/platformtag.py`・`.ccnavi/scripts/ccnavi-launcher.sh`・`scripts/ccnavi-setup.sh` の `host_target` の 3 か所で揃える。

sh は ccnavi のリポジトリでも配布先でも同じ綴りに置く。`.ccnavi/scripts/` は「原本と配布先が同じ綴りで、置いた場所で
そのまま動く sh」の置き場で、振り分けの sh はそれに当てはまる。置き場が固定なので、sh は自分の位置から実体を 1 つに
決められ、置き場を動かす引数（`--bin`）が要らない。ccnavi のリポジトリの hook も同じ sh を通るので、同じフォルダを
Windows と WSL から開いても両方で起動し、sh の不具合にも自分で気づける。

**sh の探し方。** `here=${0%/*}`（`$0` に `/` が無ければ `.`）から `bin_dir=$here/../bin` を作り、`uname -sm` を 1 回読んで
`<os>-<arch>` を決める。arm64 の macOS と Windows は、自分向けが無いときだけ x86_64 へ回る。語ごとに `ccnavi` →
`ccnavi.exe` の順に `[ -f ]` を見て、見つかったものへ `exec` で引数と標準入力を渡す。

- **隣（`.ccnavi/scripts/<os>-<arch>/`）は探さない。** そこは配る場所ではない。探すと、自己防衛の綴り（§8.2）が
  当たらない置き場から実体を起動する道ができる
- **`bin_dir` は `..` を含むまま使い、正規化しない。** `${here%/*}` で切る形は `here` が `.` や 1 段の名前のときに壊れる。
  正規化に `case` や `cd` を足すより、文面に `..` が出るほうが安い
- **シンボリックリンクは解かない。** `$0` がリンクなら、リンクの置き場から `../bin/` を探す
- **見つからなければ終了コード 127。** 実行ファイルそのものが無いときにシェルが返すのと同じ値で、文面に
  `<os>-<arch>` と探した `bin_dir` を出す。実行ファイルは在るが実行できないときは `exec` が失敗して 126 で終わる

hook は sh を `sh` 経由ではなく直に起動するので、sh に実行ビットが要る。ccnavi のリポジトリでは追跡するモードを
100755 にし、配布先では導入スクリプトが付け、`--lint` が落ちていれば error で言う（§10）。

**ccnavi のリポジトリでの組み立て。** `build.py` は PyInstaller の出力を `dist/ccnavi/` に入れ替え、`dist/ccnavi.target` を
書いたあと、`install()` で `dist/ccnavi/` を `.ccnavi/bin/<os>-<arch>/` へ写す。写すのであって移すのではない。`dist/` は
導入スクリプトの配布元で、代わりに通る sh が env の無いときに探す先でもある。起動中の置き場へ上書きで写すと onedir の
`_internal/` が前後の版で混ざるので、隣の `<os>-<arch>.new` に写し切ってから `_swap` で入れ替える。前の版にだけ
あったファイルは、退避した側ごと消える。

`_swap` は置き場を `.old` へ退避してから新しいほうを移す。Windows は走っている実行ファイルを上書きできないが、
名前の変更はできる（付録 C）。落ちたら 0.3 秒おきに 5 回までやり直し、やり直しきれなければ `.old` を置き場に戻してから
投げる。戻さないと置き場が空のまま残り、hook が 127 で起動しなくなる。`install()` が落ちたら `build.py` は 1 を返し、
`dist/` は新しく `.ccnavi/bin/<os>-<arch>/` は前のまま、と言う。`dist/` までは巻き戻さない。

- **起動できない窓がある。** rename 2 回の間（数 ms）に来た hook は、sh が 127 で終わる。Claude Code はそれを hook の
  エラーとして扱い、その 1 回は判定が走らない。窓を無くすには起動中の置き場へ上書きで写すことになり、上の混ざりが
  起きるので採らない
- **写すまで hook は新しい実行ファイルを起動しない。** ワークツリーで組み立てると、写す先はそのワークツリーの
  `.ccnavi/bin/` で、hook が起動するのはワークスペースルートの sh なので、走っている hook は変わらない
- **組み立て直すと、セッション開始で取った実行ファイルの控え（§8.3）と大きさ・時刻が食い違う。** 開き直すまで、
  戻す側に数えられる。`dry-run` なら「戻すはずだった」と言うだけで、`enable` なら次のツール呼び出しのあと、
  控えた実行ファイルを置き直す（`selfguard._check_heavy`）。今の `dist/ccnavi/ccnavi` を指す形と同じで、
  新しく増える代償ではない

**既定でない綴り。** 導入スクリプトは、`.claude/settings.json` にいま書かれている `CCNAVI_BIN_PATH` で動きを決める。

| いまの `CCNAVI_BIN_PATH` | 動き |
|---|---|
| 無い | `.ccnavi/scripts/ccnavi-launcher.sh` を書く |
| `.ccnavi/scripts/ccnavi-launcher.sh` | 何もしない |
| それ以外（人が決めた綴り） | 書き換えず、名指しで 1 行出す。導入は止めず終了コードも変えない。`--check` では揃っていない側に数える |

「それ以外」は「値が違う env」の一覧に重ねない。そちらは `--force` で置き換えられるかのような見出しの下に出るため。
人が決めた綴りの先で何が使われているかは導入スクリプトには決められない。

`--mode disable` は断る（設定ファイルに書いても効かないので、§4.3）。`--check` は書かずに
揃っていないところだけを並べ、揃っていなければ終了コード 1。名指しした `--deploy` が
組み立てられていないか、目印が無くて置き場を決められなければ終了コード 2 で断るが、既定の配布元が
使えないだけなら理由を 1 行出して設定は書く。別の機械向けの組み立ては、その機械の置き場へ配り、
この機械で動くものが無いことを言う。仕様は `tests/sh/test_setup.py` と `tests/sh/test_launcher.py` が固定している。

### 4.7 記録

ツール呼び出しは、通したものも判定しなかったものも 1 件 1 行の JSON として `CCNAVI_LOG`
（既定 `logs/log.jsonl`）に追記する。1 行の欄は付録 B。

| 欄 | 数えるもの |
|---|---|
| `decision` = `allow` / `ask` / `deny` / `handover` / `skip` | 下した判定。`handover` は権限モードに委ねた回 |
| `enforced` | 実際に適用したか。`dry-run` は常に偽 |
| `code` | 判定の根拠の種別（付録 A） |
| `reason` | `skip` の理由 |
| `degraded` | コマンドを読み切れず生の文字列に当てたかどうか |
| `fallback` | ルールを読めず組み込みの既定で判定したかどうか |
| `paths` / `detail` | 実行後の監視が拾った変更と件数 |

判定しなかった回も残すのは、記録が無いことを「ccnavi が動かなかった」と読めるようにするため。
違反が無かったのか監視が動いていなかったのかを、記録でしか見分けられない。

---

## 5. ルール

### 5.1 書式

ルールファイルは YAML で、`version: 1` と `deny` / `ask` / `allow` の 3 つの並びを持つ。
1 件は当てるツール（`match`）・探すもの（`glob` か `regex` の片方）・見つけたときに返す文を 1 組で持つ。

| 欄 | 意味 |
|---|---|
| `id` | 識別子。文面と記録で名乗る。無ければルールファイルを名乗る |
| `match` | ツール名を `\|` でつないだもの。`Bash` `PowerShell` `Read` `Edit` `Write` `NotebookEdit` `Grep` `Glob` `Skill` `Agent` `WebFetch` |
| `glob` | `fnmatch` の記法。文字列全体に当たる |
| `regex` | Python の正規表現。`re.search`。先読み・後読み・後方参照は受け付けない |
| `message` | `deny` だけ。必須。止められたモデルに届く「なぜ止めたか、代わりに何をするか」 |
| `additionalContext` / `additionalContextOnce` | 当たるたび / 文脈で最初に当たったときだけ、モデルへ渡す文（§6.6） |
| `additionalContextFile` / `additionalContextOnceFile` | 文に続けて本文を渡すファイル。探す起点からの相対（順は §6.6） |
| `every` | 「渡す回」の刻み。当たった回数がこの倍数になった回だけが渡す回になる。既定は 1 で、当たるたびが渡す回（§6.6） |

`ask` と `allow` に `message` を書くと `--lint` が error にする。ask の文面は人の確認ダイアログにしか
出ず、allow の文面はどこにも出ない（付録 C、ADR-0015）。組み立てられないルールは落として名指しし、
他のルールはそのまま使う。

### 5.2 タイプの強さ

強い順に `deny` `ask` `allow`。1 件でも `deny` に当たれば拒否で、弱いタイプは見に行かない。
同じタイプに複数当たったら全部の文面を返す（§6.4）。どのタイプにも当たらなければ、
ccnavi は判定を持たない（§6.5）。

`allow` は「このプロジェクトで普通にやること」を並べる場所で、権限を配る場所ではない。1 行足す
たびに ccnavi が何も言わない範囲が広がる（ADR-0008）。

### 5.3 記法

`glob` の意味は標準ライブラリの `fnmatch` そのままで、文字列全体に当たる。部分一致が欲しければ
前後に `*` を自分で書く。語の切れ目は入らない。左の切れ目が要るルールは `regex` を使う。
`fnmatch` の外で足している処理は区切り文字の正規化 1 つだけで、`/` と書けば `\` にも当たる
（翻訳後の正規表現の `/` を `[\\/]` に置き換える）。文字クラスの中に `/` を書いた `[a/b]` は
組み立てに失敗して読み込み時に落ちる（ADR-0010）。

`glob` も `regex` も大文字小文字を区別しない。どの機械でも同じで、`*/.ccnavi/*` は `.Ccnavi/config/rules.yml` にも当たる（§11.4）。
書いたとおりに区別させたい部分は `(?-i:...)` で囲む（ADR-0051）。

`{root}` はワークスペースルートの実パスに読み込み時に置き換わる（ADR-0032）。regex では区切りが `[\\/]` になる。
大文字小文字は式ごと区別せずに当てるので、ここで機械を見ることはしない。ルートを渡さずに読むと error。
文面（`message` / `additionalContext` / `additionalContextOnce`）の `{root}` は、書いた綴りのまま持ち、
モデルへ渡すときに実パス（区切りは `/`）へ置き換える（`rules.fill_root`）。ルートが渡らない読み方では素通し。

先読み・後読み・後方参照を弾くのは、書ける範囲を狭く保てばルールがエンジンをまたいでも同じ意味に
なるためで、これらが組み合わせ爆発を起こす書き方の入口でもあるため。Python の `re` に
時間制限は無く、判定が固まった hook は期限に達して素通りになる。

`{!root}` は「ワークスペースルートで始まらない」に読み込み時に展開される（ADR-0063）。
`(?!...)` を禁じたまま否定を書くための合言葉で、ルートを 1 文字ずつ
「ここで終わる（ルートより上）／ここが違う／ここは同じ」の入れ子に分解した式になる。繰り返しを
含まないので、破滅的バックトラッキングの条件に当たらない。展開先はどの層のルールでも
ワークスペースルートで固定（`{root}` と同じ）。

| 決まり | |
|---|---|
| 書ける場所 | `regex` だけ。`glob` に書くと error |
| 置ける位置 | `^` の直後に 1 回だけ。前に何かを置くと error |
| 直後の量化子 | error。`^{!root}?` は展開結果ごと省略でき、どの対象にも当たる式になる |
| 後ろに続く式 | 書ける。ただし `.*` のように任意の位置まで進む形で受けていなければ warn。展開結果が読み終える位置はパスの区切りとは限らない |
| ルートの正規化 | `realpath` したうえで末尾の区切りを落とす。落とさないとルート直下まで「外」になる |
| 対象がルートそのもの | 「中」。`Glob` を `path` 省略で呼ぶと対象が cwd（＝ルート）になる |
| 大文字小文字 | 区別しないので、綴りだけが違うパスは「中」。トルコ語の `İ`/`ı` が `I`/`i` と同一視されるのは既知の例外 |

ルートの長さが 256 字以上だと展開しない。この形の入れ子は 493 字で `re.compile` が
`RecursionError` になるので、その半分以下で止めている（Windows の `MAX_PATH` は 260）。
組み立てられなかったルールは**強い側に倒す**。`deny` と `ask` は `match` の全部に当たる式を
持たせて止め、`allow` はどれにも当てない（当てると ccnavi が黙る範囲が広がる）。
捨てて黙るのは「ワークスペースの外の全部」が素通りになることなので、報告だけでは足りない
（REQ-RUL-10）。書いた文面は残し、止められた側へ返す説明は別の欄に持つ。

### 5.4 対象の取り出し

| `match` に書く名前 | 当てる対象 |
|---|---|
| `Bash` `PowerShell` | コマンド。`Bash` はシェルとして読んでから当てる（§6.3）。`PowerShell` は生の文字列 |
| `Read` `Edit` `Write` `NotebookEdit` | ファイルのパス（行き着く先まで解いてから、§6.2） |
| `Grep` `Glob` | 探す場所のパス（`path`。省略されていれば呼び出し側の cwd） |
| `Skill` | スキル名 |
| `Agent` | 起動の見出し（`description`、無ければ `prompt`） |
| `WebFetch` | URL |

表に無いツール名は対象が空で決して当たらない。`--lint` が warn にする。`match` は名前をそのまま
突き合わせるので、`Bash` のルールが `PowerShell` に及ぶことはない。

### 5.5 どのルールファイルを引くか

| 呼び出し | ルール集合 |
|---|---|
| パスを持つツール（`Read` `Grep` `Glob` `Write` `Edit` `NotebookEdit`） | 共通層に、行き先のツリーが属する 1 層を足したもの。プロジェクトに属すればその層、属さなければワークスペース自身の層。`Grep` と `Glob` の行き先は探し始める場所で、省略されていれば `cwd`（§11.4） |
| パスを持たないツール（`Bash` `PowerShell` `Skill` `Agent` `WebFetch`） | 共通層・自身の層・全プロジェクトの層のルールをタイプごとに連結したもの |

自身の層のルールに当たったときは識別子に `self:`、プロジェクトの層なら `<プロジェクト名>:` を前置して名乗る。

### 5.6 ルールが読めないとき

ルールファイルが無い・壊れているときは組み込みの既定で判定を続け、通した回にも「既定に落ちた」と
伝え、記録に `fallback` を残す（ADR-0012）。既定に入っているのは取り返しの付かない操作だけ。

| 操作 | 既定での扱い |
|---|---|
| `rm -rf`、`git push`、`git reset --hard` | 止める |
| 認証情報の置き場（`.env`、`.ssh/`、`id_rsa`、`.netrc`、`.npmrc` など）への `Bash` `Read` `Write` `Edit` | 止める |
| シェルから `.claude/hooks/` `.claude/settings*.json`、ccnavi ディレクトリ `.ccnavi`、`logs/log.jsonl` `logs/state`、`ccnavi-git.sh` へ書き込む形 | 止める（書き込む綴りと場所の組で見る。§8.2） |
| `Read`（認証情報の置き場を除く。強いタイプが先に当たる） | 通す |
| `Write` / `Edit` でルールファイルを直す | 既定では止めない。権限モードに委ねる |
| それ以外 | 権限モードに委ねる |

壊す側（シェル）と直す側（`Write` / `Edit`）で経路を分けてある。既定にはプロジェクトの `allow` が
無いので、読み取り以外はほぼ確認になる。妨げるのは `deny` だけで、`ask` は人が答えれば進む。
自身の層かプロジェクトの層が読めないときは既定へ落とさず、その層を空として扱い、Bash の和からも
外し、外したことを記録と診断で示す（§11.4）。

---

## 6. 実行前の判定

### 6.1 流れ

`PreToolUse` の 1 回で、上から順に見る。

```
コアファイルの控えを取る（§8）
  │
対象を取り出す（§5.4）── 取り出せない → skip（no-subject）
  │
ルール集合を決める（§5.5）── 読めない → 組み込みの既定（§5.6）
  │  コアファイル保護の 3 本と、チケット制御の組み込みを先頭に足す（§8.2、§9.5）
  │
Bash なら実行される部分に絞る（§6.3）── 何も残らない → skip（nothing-to-run）
  │  実行役のコマンドの中で実行されるコマンドを並べる（§6.3.1）
  │
チケット制御の前置き（§9）
  │  サブエージェントが打てない操作 → deny（DENY_SUBAGENT_TICKET_OP）。中で実行されるコマンドにも当てる
  │  フェーズがレビュー待ちで止まっている → deny（DENY_PHASE_REVIEW）
  │  ワークツリーの元リポジトリと承認済みチケットの project が違う → deny（DENY_TICKET_PROJECT_MISMATCH）
  │
deny → ask → allow の順にルールを当てる（§5.2）── deny に当たった → deny（§6.4）
  │  deny と ask は中で実行されるコマンドにも当てる。allow は当てない
  │
チケットの範囲（§9.5）── 範囲外・範囲の deny → deny、範囲の ask → ask、範囲内 → allow
  │  ルールの判定と比べて強い側を採る。同じ強さならルール
  │
どちらも判定を持たない → 権限モードに委ねる（§6.5）
```

判定を返した後、当たったルールがモデルへ渡す文を持てば `additionalContext` に載せる（§6.6）。

### 6.2 パスは行き着く先で見る

`Read` `Write` `Edit` `NotebookEdit` `Grep` `Glob` のルールは、payload の `file_path`（`Grep` と `Glob`
は `path`）そのものではなく、`cwd` から絶対パスに直し、`~` を展開し、`..` を畳み、
シンボリックリンクを解いた結果に当てる。`Grep` と `Glob` は `path` が省略されていれば `cwd` を解く。守る対象は
名前ではなく場所なので、同じ場所を指す別の綴りでルールを外せてはいけない（ADR-0014）。
まだ存在しないファイルへの書き込みは解けないので、絶対パスにして `..` を畳むところまでで止める。
Bash のコマンドに現れるパスは対象外で、そちらはコマンド文字列としてしか見ない。

`Grep` と `Glob` で解くのは探し始める場所だけで、そこから降りて読まれたファイルは判定に届かない。
`credentials` のような場所を守るルールに `Grep` と `Glob` を足さないのはこのためで、探すツールが
読むファイルは `.gitignore` と `permissions.deny` の `Read(...)` が受け持つ（ADR-0050）。

### 6.3 Bash は実行される部分だけを見る

`Bash` のルールは、コマンド文字列そのものではなく、シェルが実際に実行する部分に当てる。
生の文字列を探すと、禁止された語を書いただけの操作が止まる。逆に、二重引用の中の `$( )` のように
シェルが実行するのに文字列の一部に見えるものを読み落とすと、allow が中身まで通す。

読みは 2 段。先に原文を 1 回走査して、シェルが実行するのに `shlex` が見ないもの（コマンド置換、
プロセス置換、ヒアドキュメントの本文、コメント、改行）を片付け、語の分割は
標準ライブラリの `shlex` に任せる（ADR-0001）。`shlex` は二重引用の中を 1 語として返し、語の途中の
`#` からもコメントにし、改行をただの空白として読む。引用の状態を返す公開の手段は無く、状態機械を
写すと Python の版で壊れるので、引用の状態は走査の側で持つ。引用の規則を 2 か所で持つことになるので、
食い違いは受入テスト（`tests/core/test_shellread.py` の、bash 3.2 と zsh で実測した 65 形）で押さえる。
読み方は次のとおり。

1. 入力の `\x00` と `\x01` を空白に置き換える（後で目印に使う 2 文字を、書いた側から騙れないように）。
   行継続（`\` と改行）は 2 文字とも消す
2. 原文を走査する。単一引用と `$'…'` の中、`\` の後ろの 1 文字は文字。二重引用の中では `$( )`・
   バッククォート・`${ }`・`$(( ))` だけを見る。バッククォートは見つけたところで `backquote` で縮退する
   （下の書き直しを求める形）。`$( )` `<( )` `>( )` の中身は切り出し、
   外側には `$` を 1 文字残す（外側の語の数と形を保つ。`> "$(pwd)/x"` は `> $/x`）。語の始まりの `#` から
   行末はコメントとして落とし、語の途中の `#` は文字のまま。引用の外の改行はコマンドの区切りにする。
   ヒアドキュメント（`<<`、`<<-`）の本文と終端の行は落とし、区切りを引用していなければ本文の置換を
   切り出す。終端の行は前後の空白を落として比べる（シェルより早く閉じることはあっても遅くは閉じない。
   早く閉じれば本文の残りをコマンドとして読む厳しい側）。閉じない引用とヒアドキュメントは
   `unterminated-quote`、閉じない置換は `unterminated-substitution`、`$( )` の中の `case`・算術か
   コマンド置換かが綴りから決まらない形（`$((cmd) | x)`）は `ambiguous-substitution` で縮退
3. `shlex`（posix、`punctuation_chars`、`commenters` は空）で語に分ける。走査がヒアドキュメントと
   読まなかった `<<` がトークンに出たら `unterminated-quote` で縮退（下の限界）
4. トークン列から `$(( … ))` を落とす。左シフトがヒアドキュメントの始まりに見えるため
5. `;` `;;` `&` `&&` `|` `||` `|&` `(` `)` でコマンドを切る。コマンドの位置に立つ予約語（`if` `then`
   `else` `elif` `fi` `do` `done` `while` `until` `for` `case` `esac` `select` `time` `coproc` `!` `{` `}`）は
   それだけで 1 本にし、後ろの語をコマンドの先頭にする。コマンドの位置の `coproc` と `select` は、書き直しを
   求める形として並べる。
   予約語を落とさないのは、`! grep x f` を `grep x f` と読んで allow に当てないため
6. `cd` が移った先から見た綴りを別に組む（§6.3.2）。書かれた綴りは動かさない
7. 縮退するかを決める。先頭語が `eval` `source` `.` `xargs` のとき、または語のどこかに
   `bash` `sh` `zsh` `ksh` `dash` `su` と `-c`（`find` なら `-exec` / `-execdir` / `-ok` / `-okdir`）が
   並ぶとき（`sudo -u root sh -c` のように 4 語目に来ることがあるので、先頭語だけは見ない）。
   比べるときだけ、語からパスと `.exe` `.cmd` `.bat` を落とす
8. コマンド間を `\x00` でつなぐ。語の中の空白は `\x01` にし、語の中の `( ) ; < > | &` は両側に
   `\x01` を置く。目印を 2 つに分けるのは、ルールの `[^\x00]*` が同じコマンドの中だけを指せるように
   するため。同じ文字だと、`[^\x00]*$`（後ろにコマンドが続かない）が引用の中の空白でも止まり、
   `grep -n "git push" f` が読みのルールに当たらない。引用が 1 語につないだ空白はコマンドと引数の間の空白として読まれず、語の中の
   演算子の文字は演算子として読まれない。ルールに当てる綴りは書かれたままで、パスは落とさない
   （`*git push*` は `/usr/bin/git push` に当たらない）
9. 切り出した中身を同じ手順で読み（入れ子は 16 段まで。越えたら `ambiguous-substitution`）、外側の読みの
   後ろに、出てきた順に `\x00` でつなぐ。置換のあった位置で挟まないのは、外側のコマンドを割らないため
   （`find $(pwd) -name x -delete` の `-delete` が find と同じコマンドに見える）。中身は `\x00` の直後に
   立つので、`(^|\x00)` で書いたルールは引用の外と同じように当たり、`[^\x00]*$` の allow は中身が 1 本でも
   あれば外れる。中身が縮退すれば全体が縮退し、理由は中身のものになる

**引用の中の断り。** 読みは、ルールを当てる `text` のほかに `bare` を返す。`text` から、引用の中
（二重引用と、区切りを引用しないヒアドキュメントの本文）で切り出したコマンドを除いたもの。判定は `text` で
下し、当たった deny / ask のルールを `bare` にも当て直す。`bare` に当たらなければ、書いた側が文字のつもりで
いる場所に当たったので、文面に回避策（単一引用、`\$(` と `` \` ``、`commit -F`、`--body-file`、値を出して
読んでから書く 2 手）の断りを 1 文足し、記録と `--test` に `quoted`（そのルールの id の並び）を残す。
一致位置から推し量らずに当て直すのは、glob が一致位置を返さず、ルールの書き方（glob / regex、`^` /
`(^|\x00)`）に依らずに決めるため。

**縮退**（読み切れないとき）は生の文字列との一致に落ちる。縮退した呼び出しに `allow` は当てず、
`deny` と `ask` は当てたままにする。どこにも当たらなければ `PARSE_UNCERTAIN` で人に確認が出る。
ここだけは権限モードに委ねない。読めなかったという事実は判定の結果に現れず、委ねた先には
伝わらないため。文面は「禁止された操作を行った」ではなく「読めなかったので文字列に当てた」と
伝える（REQ-HNT-04、ADR-0011）。記録には `degraded`（`command-taken-as-code` / `unterminated-quote` /
`unterminated-substitution` / `ambiguous-substitution` / `backquote`）が付き、文面の断りも理由ごとに変わる。
後ろの 2 つは必ず書き直しを求める形として並ぶので、ルールより先に止まり、縮退の断りは出ない。
複合コマンドは 1 つの区間が読めなければ全体が縮退する。

実行される部分が何も残らないコマンド（コメントだけの行）は判定に入らず、記録に `nothing-to-run`
が残る。

**ブレース展開**は読まずに止める（ADR-0046）。引用の外の `{a,b}`・`x{,.bak}`・`{1..3}` は、シェルが実行する前に
複数の語に広げる。`{git,push,origin,main}` は 1 語に読めるが、bash は `git push origin main` を実行する。走査は
引用の外（`${ }` の中、置換の中身、`sh -c` と `eval` に渡った文字列を含む）でこの綴りを探して `rewrites` に並べ、
判定はそれがあればルールより先に `DENY_BRACE_EXPANSION` で止める。文面は見つけた綴りを名指しし、語を並べて書くか、
文字なら単一引用で包むよう案内する。展開して読まないのは、広げ方がシェルで割れ（`{1..5..2}` と `${x:-{a,b}}` は
zsh だけが広げ、`{01..03}` の 0 を落とすのは bash 3.2 だけ）、読み違えると広げた形が `allow` に当たって通る側に
倒れるため。縮退にしないのは、生の文字列にも `git push` は無く、`PARSE_UNCERTAIN` の確認にしか届かないため。
並べるのはどちらかのシェルが広げる形（引用の外にカンマがある `{…}`、数か 1 文字の範囲）で、引用の中・`\{`・
引用やエスケープで書いたカンマ（`{"a,b"}`）・ヒアドキュメントの本文・コメント・`{}` と `{a}` は並べない。入れ子は
外側だけを並べる。読み（`text`・`bare`・`degraded`）は変えない。

**実行するときに決まるコマンド名・バッククォート・シェルで読みが割れる形**も、同じく読み分けずに止める
（ADR-0047）。読みはブレース展開と合わせて `rewrites` に（形, 綴り）で並べ、判定はルールより先に形ごとの
理由コードで止めて、書き直し方を 1 回で返す。記録の `rules` には `(形)` が残る。

| 形 | 理由コード | 並べるもの |
|---|---|---|
| `brace-expansion` | `DENY_BRACE_EXPANSION` | 上のブレース展開 |
| `command-name-expansion` | `DENY_COMMAND_NAME_EXPANSION` | コマンドの位置の語に、変数か置換の `$`、`*` `?`、閉じた `[…]` がある。コマンドの位置の語は、前に置いた代入（`a[1]=x`・`x+=1` を含む）とリダイレクト（`>/dev/null`・`2>&1`）を飛ばした最初の語で、`case` `for` `select` の直後の 1 本は見ない。前に置く fd には名前付き fd（`{fd}>`）も含む。読んだコマンドと、実行役のコマンドを外した層（§6.3.1）の両方で見るので、`env $c`・`sh $SCRIPT` も並ぶ。`eval` と `sh -c` の文字列を読み直した層は、外側が縮退しているときは見ない（確認に落ちるので、書き直し先をファイルに追いやらない）。縮退していない `FOO=1 eval "$c"` は見る |
| `backquote` | `DENY_BACKQUOTE` | 単一引用・`$'…'`・`` \` ``・引用付きヒアドキュメントの本文・コメントの外のバッククォート。見つけたところで読むのをやめ、`backquote` で縮退する。中のエスケープの外し方と入れ子の読み直しは持たない |
| `ambiguous-form` | `DENY_AMBIGUOUS_FORM` | `ambiguous-substitution` で縮退した形（`$( )` の中の `case`、`$((cmd) \| x)`、16 段を超える入れ子）と、コマンドの位置の `coproc`・`select` |

止めるのは、書き直した結果がその場で読める形に収まるものだけ。`sh -c`・`eval`・`xargs`・`find -exec` は書き直し先が
ファイルになり、中身が ccnavi から見えなくなるので、縮退と層の読み（§6.3.1）のままにする。

残る限界が 1 つある。引用符の中身が演算子の文字だけでできている 1 語（`grep -n "<<" f`）は、
`shlex` のトークンでは演算子と区別が付かない。走査は引用の範囲を知っているので区別できるが、
それを使うと止まっていたものが通る向きの変更になるので、今の読みを保っている。
ヒアドキュメントの始まりに見え、閉じない本文として縮退し、ヒアドキュメントのルールに当たって
止まる。これは許容する誤検知（§12.2）。

#### 6.3.1 実行役のコマンドの中で実行されるコマンド

`env rm x` の `rm x` のように、別のコマンドを実行することが仕事のコマンドがある。これを実行役のコマンドと呼び、
実行されるほうを中で実行されるコマンドと呼ぶ。ルールの多くはコマンドの先頭に固定して書かれる。組み込みの
シェルの書き込み（`(^|\x00)(mv|rm|…)`）、サブエージェントの禁止（`(^|[;&|]\s*)(sh|bash)\s+`）、承認のルール（実行ファイルの綴り）が
そうで、`env`・`sudo`・`timeout`・`sh -c`・`xargs` などを前に置くだけで外れる。直すのはルールの書き方ではなく読みの側で、
ルールごとに実行役のコマンドを書かせると、組み込みも利用者のルールも同じ穴を 1 本ずつ塞ぐことになる（ADR-0045）。

**層の作り方（`shellread`）。** `read()` は、コマンドごとに実行役のコマンドを 1 枚ずつ外した層を、外側から内側の順に
並べて返す。`Reading.unwrapped` が層を `\x00` でつないだもの、`Reading.runners` が層ごとの実行役のコマンドの名前で、
同じ並び。元の形は含めない。途中の層も残す。`env sh .ccnavi/scripts/ccnavi-approve.sh` からは
`sh .ccnavi/scripts/ccnavi-approve.sh` と `.ccnavi/scripts/ccnavi-approve.sh` の 2 層ができ、承認のルールの `sh …approve.sh` の枝は
1 層目に当たる。層は元の形と同じ規則で組み直す（語の中の空白は `\x01`、語の中の演算子の文字は `\x01` で挟む）。

| 形 | 中で実行されるコマンド | その中をさらに外すか |
|---|---|---|
| `FOO=1 <cmd>` | `<cmd>` | 外す |
| `/bin/sh x`、`git.exe x` | `sh x`、`git x`（区切りの前の道筋と `.exe` `.cmd` `.bat` を落とした名前） | 外す |
| `env` `command` `exec` `nohup` `time` `nice` `sudo` `doas` `timeout` `stdbuf` `chrt` `ionice` `taskset` | オプション、値を取るオプションの値、位置引数、`--`、`env` と `sudo` の `名前=値` を飛ばした残り | 外す |
| `sh` `bash` `zsh` `dash` `ksh` `<ファイル> <引数>` | `<ファイル> <引数>` | 外さない |
| `sh -c '<文字列>'`（`-lc` のようにまとめた形も） | 文字列を読み直したコマンド | 外す |
| `eval <語…>` | 語を空白でつないで読み直したコマンド | 外す |
| `.` / `source` `<ファイル> <引数>` | `<ファイル> <引数>` | 外さない |
| `xargs [オプション] <cmd…>` | `<cmd…>` | 外す |
| `find … -exec` / `-execdir` / `-ok` / `-okdir` `<cmd…>` `;` か `+` | `<cmd…>`（複数あればそれぞれ） | 外す |

- **シェルと `.` / `source` に渡したファイルの先は外さない。** 後ろの語はスクリプトの引数で、コマンドではない。
  外すと `sh scripts/a.sh rm x` の `rm x` をコマンドと読む
- **層を作らない形。** `command -v` / `-V` はコマンドを探すだけで実行しない。`sh -s a b` は標準入力を読み、後ろの語は引数
- **find の 2 つめ以降の `-exec`。** 述語の終わりは `\;` と書くが、`shlex` はそれを区切り記号の `;` と同じ綴りで返すので、
  コマンドはそこで切れ、2 つめの `-exec` は別のコマンドの頭に来る。`-exec` の類で始まるコマンドも find の続きとして読む
- **深さは 4 まで**（`UNWRAP_DEPTH`。元の形は数えない）。**語数は 1 回の読みで 2000 まで**（`UNWRAP_WORDS`）で、超えたら
  そこから先の層は作らない。`eval` や `sh -c` の文字列は読み直すと語が増えるので、深さだけでは抑えきれない。
  どちらも判定の期限に効くので、層の数をここで抑える
- **読み切れない形でも、トークンに割れる限り作る。** `sh -c` や `xargs` のように縮退する形こそ、中で何が実行されるかを
  見たい。`degraded` と理由はそのまま残る。閉じない引用と閉じないヒアドキュメントでは作らない。途中で切れたコマンドの
  残りは推測しない

値を取るオプション（正は `shellread._RUNNERS` と、シェル・`xargs` の一覧）:

| コマンド | 次の語を値に取るオプション | 前に置く位置引数 |
|---|---|---|
| `env` | `-u` `--unset` `-C` `--chdir` | 0 |
| `exec` | `-a` | 0 |
| `time` | `-f` `-o` `--format` `--output` | 0 |
| `nice` | `-n` `--adjustment` | 0 |
| `sudo` | `-u` `-g` `-C` `-h` `-p` `-U` `-r` `-t` `-T` `-D` `-R` `--user` `--group` `--close-from` `--host` `--prompt` `--other-user` `--role` `--type` `--command-timeout` `--chdir` `--chroot` | 0 |
| `doas` | `-u` `-C` | 0 |
| `timeout` | `-s` `--signal` `-k` `--kill-after` | 1（時間） |
| `stdbuf` | `-i` `-o` `-e` `--input` `--output` `--error` | 0 |
| `chrt` / `taskset` | — | 1 |
| `ionice` | `-c` `-n` `-p` `--class` `--classdata` `--pid` | 0 |
| `command` / `nohup` | — | 0 |
| `sh` `bash` `zsh` `dash` `ksh` | `-o` `+o` `-O` `+O` `--rcfile` `--init-file` | — |
| `xargs` | `-I` `-n` `-P` `-d` `-L` `-s` `-E` `-a` `--max-args` `--max-procs` `--delimiter` | 0 |

`--unset=HOME` や `-o0` のように値が同じ語に入っていれば 1 語。短いオプションはまとめて書けるので（`-Eu me`）、値を取る
文字が最後に来たときだけ次の語を値に取る。一覧に無いオプションは値を取らないものとして読む。読み違えると値をコマンドと
読んで層がずれるが、ずれた層は当たらないだけで、元の形の判定は変わらない（下の線引き）。

**一覧は組み込みで持つ。** ルールファイルから足せるようにすると、消すこともできる。守りの根拠を守られる側に置かない
（ADR-0021 と同じ向き）。代償として、足すには ccnavi を作り直す。一覧に無い実行役のコマンド（`script -c`・`watch`・`parallel`・
`busybox sh`・`su -c`・`ssh host <cmd>`・`perl -e`・`python -c`）は外さない。先頭に固定したルールは今までどおり抜け、
どこにも当たらなければ §6.5 に落ちる。変数の値・alias・関数にも届かない（§12.1）。コマンド名の位置に変数を置いた
形（`$SUDO rm …`）は、書き直しを求める形として止める（§6.3、ADR-0047）。

**当てる先の線引き（`judge`）。** 層は止める側のルールにだけ当てる。`Rule.matches` は変えず、`judge` がルール 1 本ずつ、
元の形に当て、当たらなければ層を並びの順に当て、最初に当たった層で止める。PowerShell は `shellread` で読まないので層は無い。

| 当てる先 | 元の形（読み切れなければ生の文字列） | 中で実行されるコマンド | `cd` で移った先（§6.3.2） |
|---|---|---|---|
| `deny` / `ask`（組み込み・利用者） | 当てる | **当てる** | **当てる** |
| `allow` | 読み切れたときだけ当てる（§6.3） | 当てない | 当てない |
| サブエージェントの禁止（`phase.forbidden`） | 当てる | **当てる** | **当てる** |
| 止めている間でも通す形（`phase.exempt`） | 読み切れたときだけ当てる | 当てない | 当てない |
| チケットの範囲（`ticket_verdict`） | Bash には当てない（§9.5） | 当てない | 当てない |

層は止める側に足す当て先で、元の形の判定を消さない。だから層を読み違えても、当たるはずのものが当たらないだけで、
今より緩くはならない。`allow` と `exempt` に当てると向きが逆になる。`sudo -u me cat /etc/hosts` は元の形ではどのルールにも当たらず §6.5 に落ちるが、
中の `cat /etc/hosts` が読み取りの `allow` に当たって通るようになる。`env sh .ccnavi/scripts/ccnavi-review.sh …` に `exempt` を当てると、
止めている間に実行役のコマンド越しに打った形が通るようになる。

代償は 2 つ。当てる文字列が層の数だけ増える（深さと語数で抑える）。`time` や `nice` の後ろに書いた、利用者の `deny` に当たる
コマンドがこれから止まる。その `deny` は元々そのコマンドを止めるために書かれたものなので、意図どおり。

**コードと文面。** 層で当たったルールの文（§6.4）には、`subject:` の次に 1 行足す（`reasons.ran_by`）。

```
[ccnavi] DENY_COMMAND_PATTERN (rule: builtin-guard-setting-files)
subject: env rm -f .ccnavi/common/rules.yml
`env` が実行する `rm -f .ccnavi/common/rules.yml` に当たりました。
ガード自身の設定と hook を、…
```

元の形だけを見た読み手には、ルールのどこが当たったのかが分からない。ルールは `rm` について書かれていて、`env` については
何も言っていないので。層は目印を空白に戻して 1 行に畳み、200 字で切る。

- 層で当たった件には、読めなかった断り（§6.3 の縮退）を付けない。当てた先は生の文字列ではなく、読み直したコマンドだから
- `deny` のコードは、当たったルールが全部層で当たったなら、縮退していても `PARSE_UNCERTAIN` にせず `DENY_COMMAND_PATTERN` にする。
  1 本でも元の形で当たり、縮退していれば今までどおり `PARSE_UNCERTAIN`。ただし承認のルール（`builtin-guard-ticket-approval`）が
  当たった件に入っていれば、層で当たったか元の形で当たったか、縮退しているかに依らず `DENY_TICKET_APPROVAL_CLI` を優先する
  （今回より前からの扱い）。元の形が縮退したまま承認のルールに当たったときは、コードは `DENY_TICKET_APPROVAL_CLI` で、
  読めなかった断りは付く。判定（deny / ask）はどちらでも変わらない。代償として、`PARSE_UNCERTAIN` の件数で数える集計とはずれる。
  `ask` は `RULE_ASK` のまま
- 記録の欄 `unwrapped` には、当たった層だけを `\x00` でつないで残す（同じ層は 1 回）。層全部ではない。どの層で当たったかを
  読むための欄で、元の形で当たったルールの分は空。数えれば、実行役のコマンド越しに止めた件数が分かる。サブエージェントの
  禁止は、元の形で当たらず層で当たったときにその層を残す。`--test --json` にも同じ欄が出る

#### 6.3.2 `cd` で移った先

守りもルールも、当てる先は語の綴り。`cd` で入ってから書くと、行き先の綴りからディレクトリの名前が消えるので
当たらない（`cd .claude && echo x > settings.json`）。この穴は ccnavi ディレクトリの下だけでなく、コアファイルの
`.claude/settings.json` と `.claude/hooks/` にも及ぶ（issue #61、ADR-0069）。

**組み方（`shellread`）。** `read()` は、`cd` の行き先を継ぎ足したコマンドを `Reading.moved` に `\x00` でつないで返す。
綴りの変わったコマンドと、そこから 1 枚ずつ外した層（§6.3.1）が並ぶ。`judge.screen` がそれを中で実行される
コマンドの層と同じ並びに足し、実行役のコマンドの名前には `shellread.MOVED`（`cd`）を置く。当てる先の線引きは
上の表のとおりで、止める側のルールにしか当たらない。**書かれた綴り（`Reading.text`）は動かさない。**

- **読むのは `cd <リテラル 1 語>` だけ。** 行き先は `.` と `..` を畳んで継ぎ足す。絶対パスはそのまま使い、そこから先の
  行き先になる。末尾の区切りは残す（名前がそこで終わる形に当てる守りの当たり方を変えないため）。
  `builtin cd` と `command cd` も `cd`。`env -C <パス>`・`sudo --chdir=<パス>` は、その 1 本だけ行き先を移す
- **継ぎ足すのは、リダイレクトの行き先と、コマンドの名前より後ろの語。** リダイレクトの行き先は、コマンドの位置より
  前に書いたもの（`> settings.json cat x`）とコマンドが無いもの（`> settings.json`）にも当てる。`dd of=<パス>` の
  ように値にパスを取る語は、名前を残して値だけ継ぎ足す
- **コマンドの位置の語には継ぎ足さない。** 止める側のルールは名前をコマンドの頭に固定して書かれており
  （`(^|\x00)(mv|rm|…)`）、名前に継ぎ足すとその固定が外れる。実行役のコマンド（`_RUNNERS`）を通した形は、
  中で実行されるコマンドの名前の位置を `_peel` と同じ読み方で出して、その後ろに継ぎ足す。オプション、
  代入、絶対パス、`2>&1` のような fd の番号にも足さない
- **`cd` の行き先の語そのものは書かれたまま残す。** `cd` は書き込みではないので、当てる先に並べても止めるものが
  無く、`cd a && cd b && …` のぶんだけ当てる先が増えるだけになる
- **区切りで居場所の続き方が変わる。** サブシェル `( )` は出入りで戻す。`|` `|&` `&` の左はサブシェルなので、
  そこで移っても右と後ろには効かない（`cd .claude & echo x > settings.json` は `.claude` に書かない）。
  `case` の枝の `)` はサブシェルの閉じと同じ綴りで来て出入りが読めないので、`case` から `esac` までは読まない
- **行き先が綴りから決まらない `cd`（引数なし・`cd -`・`~`・変数やグロブ・一覧に無いオプション・`pushd` `popd`）の
  あとは、継ぎ足さないだけ。縮退させない。** 縮退は生の文字列に落ちるので、コマンドの頭に固定して書かれた守りが
  当たらなくなり、`cd - && rm -f <守られた場所>` のように**書かれた綴りで今は止まっている形**が止まらなくなる
- **`$( )` の中身は、中身の中で移った先だけ届く。** 中身は別の読みなので外側の行き先は持たないが、
  中身の中の `cd` は中身に効く（`echo "$(cd .claude && rm settings.json)"`）
- **上限を持つ。** 居場所の綴りは 256 字（`_HERE_LIMIT`）、1 回の読みで継ぎ足す語数は 2000 語（`MOVED_WORDS`）まで。
  越えたら継ぎ足さない。`cd a` を数千回並べると居場所が伸び、畳み直す値段が長さに比例するので、上限が無いと
  全体が二乗になる。判定の期限（§6.1 の 3 秒）は読み終えたあとに見るので、読みの中で伸びるものには自分で上限を置く

**文面。** 層と同じ 1 行を足す（`reasons.ran_by`）。

```
[ccnavi] DENY_COMMAND_PATTERN (rule: builtin-guard-setting-files)
subject: cd .claude && echo x > settings.json
`cd` で移った先から見ると `echo .claude/x > .claude/settings.json` で、そこに当たりました。
ガード自身の設定と hook を、…
```

書いた綴り（`echo x > settings.json`）には、当たったルールの名前が出てこない。どこへ書こうとしているかを綴りで示す。
記録の `unwrapped` には当たった層が残るが、そこは実行役のコマンドの層と同じ欄なので、欄の上では区別が付かない。

### 6.4 判定の文面

文面は、勝ったタイプの当たったルールを全部持つ。`deny` / `ask` は 1 件ずつ、`[ccnavi] <コード> (rule: <id>)`
/ `subject:` / （縮退の断り）/ `message` の閉じた文にし、空行で並べて 1 回で返す。どれか 1 つを
選ぶと、選ばれなかったルールの言い分は誰にも届かない（REQ-HNT-01、ADR-0034）。文はそれだけで
読んで成立させ、他の判定の結果に言及しない。同じイベントの hook が並行して走り、どれが利用者に
見えるかが決まらないため。

### 6.5 ルールが言及しない呼び出し

ccnavi は判定を返さず、Claude Code の権限モードに従う（REQ-PRE-10、ADR-0009）。

| `permission_mode` | 結末 | 記録の `decision` |
|---|---|---|
| `auto` | classifier が判断する | `handover` |
| `default` / `acceptEdits` / `plan` | Claude Code 自身の権限の仕組みが決める | `handover` |
| 空 / 不明 | 人に確認が出る | `ask` |
| `dontAsk` / `bypassPermissions` | 通さない（REQ-PRE-08）。`CCNAVI_GUARD_UNWATCHED=disable` なら委ねる | `deny` |

理由コードは `UNDECLARED`。ルールの `ask` に当たった呼び出し（`RULE_ASK`）は権限モードによらず
確認に出す。人が意図して置いた確認ポイントを classifier の判断で消さない。判断できる相手が居る
4 つのモードに委ねるのは、ccnavi がそこに確認を上乗せしても判断する者が増えないため。`default` では
Claude Code 自身が書き込みとシェルに確認を出すので、上乗せすると同じ呼び出しで 2 度聞かれる
（ADR-0049）。確認できる者が居ないモードで通さないのは、そこで ask を返しても「誰も答えないまま
通る」に化けるため。その門はプロジェクトが `CCNAVI_GUARD_UNWATCHED` で開けられる。知らないモードの
名前は確認に倒す。縮退した呼び出しは委ねない（§6.3）。門を開けた層でも委ねない。

端末からの試験（§10）には `permission_mode` が来ないので、言及の無い呼び出しは `ask` として出る。

**`allow` に当たった呼び出しにも、ccnavi は判定を返さない。** `permissionDecision` を返すのは `deny` と `ask` だけで
（`judge.refuse`）、`allow` は記録に残して終わる。`allow` の値打ちは、この表の結末（知らないモードでの確認と、
確認できる者が居ないモードでの拒否）を起こさないことと、`additionalContext` を当てる先になること。
Claude Code 側の権限の扱いは変わらないので、
`allow` を足しても Claude Code の確認は飛ばせない。飛ばせる形にすると、ルールを 1 行足すだけで権限を配れることになる。

### 6.6 モデルへ渡す文

当たったルールの `additionalContext`（毎回）と `additionalContextOnce`（文脈で最初の 1 回）を、
判定とは別の経路で届ける。文脈は `session_id` と `agent_id` の組で、サブエージェントはその起動
ごとに別に数える。記憶は `state/once-<セッション>-<エージェント>.json` に置き、`SessionStart`
（起動・再開・compact の後）で捨てる。控えの置き場が無ければ毎回届ける。

記憶は「鍵 → 当たった回数」で持つ。鍵はルールの `id`（無ければ `match` と綴りの組）。
古い形（当たった鍵を並べた配列）も読める。

**「置き場が無い」と「読めなかった」は別に扱う。** 混ぜると、読めなかっただけの控えを
空で潰すことになる。

| 何が起きたか | どう倒れるか |
|---|---|
| 控えの置き場が無い（`--state ""`） | 刻まず、毎回届ける。覚えられないなら黙るのではなく言う側へ |
| 控えがまだ無い | 普通の状態。1 回目として数え、書く |
| 控えを読めなかった | **届ける側へ倒しつつ、書き戻さない。** 数えはそのまま残る |

読めないのは控えが在るときにしか起きないので、そこで「まだ何も無い」として書くと、消す先は
いつも中身のある控えになる。数えを持つ控えではそれが「刻みが 0 に戻る」ことを意味する。
一時的に開けないだけなら `fsio.read_json` が数回打ち直すので、ここへ落ちるのは重なりが
続いたときだけ（ADR-0056）。

**控えは途中を見せない書き方で置く**（一時ファイルに書いてから差し替える）。素の書き方だと
書いている最中のファイルは空で、そこを並んで走る hook に読まれると、上の「読めなかった」に
毎回落ちる。

#### 渡す回

上の「毎回」と「最初の 1 回」は、どちらも**渡す回**を基準にしている。`every: N` はその渡す回を
刻む欄で、当たった回数が `N` の倍数になった回だけが渡す回になる。`every` を書かなければ
刻みは 1 で、当たるたびが渡す回になる。

| 欄 | いつ渡るか | `every: 5` のとき |
|---|---|---|
| `additionalContext` | 渡す回のたび | 5・10・15… 回目 |
| `additionalContextOnce` | 渡す回の最初の 1 回 | **5 回目だけ**。10 回目には渡さない |

本文のファイルも同じで、`additionalContextFile` は `additionalContext` に、
`additionalContextOnceFile` は `Once` に従う。

**数えるのは「そのルールが当たった回数」で、変わったファイルの本数でも時間でもない。**
同じファイルを 5 回直しても 5 と数える。`match` はツール名なので、`Bash` を書かない限り
シェルやビルドが書いたぶんは数えない。

**`Once` の読み方は、`every` があるかで変わる。** `every` の無いルールでは「当たった最初の
1 回」、`every` のあるルールでは「渡す回の最初の 1 回」。同じ欄が置かれ方で違う回に届くので、
どちらか一方には寄せられない。`every` を添えると `Once` が遅れて届くので、「1 回目に言いたい
こと」と「たまったところで言いたいこと」は 1 件のルールに同居させられない。両方要るなら
ルールを 2 件書く。

`every: 1` は既定を明示しただけなので `--lint` は何も言わない。刻みとして読めない値
（`0`・負・整数でない）は error で名指しし、判定は 1（毎回渡す）に倒して通す。
黙って落とすと、書いた人は刻んだつもりのまま毎回渡ることになる。

**刻みは `SessionStart` で 0 に戻る。** 記憶は「1 度だけ渡す文」と同じ控えに相乗りしていて、
`forget()` が起動・再開・**compact の後**にまとめて捨てる。文脈が新しくなれば「この文脈で
何回目か」も無くなるので、筋は通っている。

**戻ったぶん刻みは正確でなくなるが、ずれる向きは書き方で逆になる。** 数えは進みを捨てるだけで
先へは進まないので、次のように分かれる。

| 書き方 | 0 に戻ると | |
|---|---|---|
| `additionalContext` + `every: N` | 進みが捨てられるだけ | **届くのが遅れる。** 戻りが続けば一度も届かないこともある |
| `additionalContextOnce` + `every: N` | 「渡す回の最初の 1 回」が改めて成り立つ | **セッション全体では 1 度より多く届く** |

どちらにしても、長い作業で正確に N 回ごとを守る欄ではない。**compact がいつ起きるかは
ccnavi から見えない**ので、どれくらいずれるかはここでは分からない。

| タイプ | どう届くか |
|---|---|
| `allow` | 応答の `additionalContext` として届く。判定の理由は無いので、これだけが届く |
| `ask` | 人が Yes を押したときだけ届く。No なら拒否の定型文だけがモデルに届いてターンが終わる |
| `deny` | `permissionDecisionReason` と一緒に届く |
| `dry-run` | 止める代わりに返す文に続けて届く |

ファイル（`additionalContextFile` / `additionalContextOnceFile`）は、行き先のワークツリー → 元リポジトリの
プロジェクト → ワークスペースルートの順に探し、最初に在ったものを読む。絶対パスと `..` で上に
出るパスは読まない。先頭 4000 文字で切り、切ったことと続きの在りかを末尾に添える。
無ければ何も足さない（REQ-PRE-12）。同じタイプに複数当たれば全部の文を空行で割って並べる。

---

## 7. 実行後の監視

### 7.1 何を見るか

実行前の判定はツール呼び出しの引数しか見ない。引数に現れない書き込み（ビルドの出力、許可された
スクリプトが内部で開くファイル、読み切れないシェル構文で書いたもの）は、走ったあとの作業ツリーを
git で読んで拾う（ADR-0018）。

`PostToolUse` で、`Read` `Grep` `Glob` `WebFetch` `WebSearch` 以外の全ツールの後に走る。
読み取り専用のツールの直後は `tool-cannot-write` として見に行かない。作業ツリーを変えようが
ないので、次に走る書き込みうるツールの直後に同じ変更が見える。見落としではなく遅れ。

見るツリーは、ワークスペースルートと、この呼び出しが触ったツリー（パスを持つツールは行き先、
Bash は cwd）。ツリーごとに `git status --porcelain -z --untracked-files=all --no-renames` を
期限 2 秒で起こす。読めなければ `worktree-unreadable` として何も言わない。穴も開かない。

### 7.2 保護領域と違反

保護領域は別に宣言しない。そのツリーのルールのうち、`match` に `Write` `Edit` `NotebookEdit` の
どれかを含む `deny` / `ask` が、そのまま「この場所に書かせない」の宣言になる（ADR-0017）。
変更の実パスがそれに当たれば違反（`POST_VIOLATION`）。そうでなく、ワークツリーに結び付くチケットの
範囲の外（チケットの `deny` の項を含む）なら、ルールの `allow` に当たっていても `POST_TICKET_SCOPE`（§9.5）。記録と控えの置き場（`CCNAVI_LOG`、
`CCNAVI_STATE`）の下は除く。自分の書き込みを自分の違反にしないため。

違反の報告には 1 件ずつ、変更されたファイル・種類・該当するルール・原因となった直前の実行・戻す手順を
添える。戻す手順は対象ごとに 1 つ。数えられる手順でないと、受け取った側が自分で組み立て直す。

| 変更の種類 | 戻し方 |
|---|---|
| 中身が変わった・消えた | `git restore --staged --worktree -- <path>` |
| 追跡されていないものが現れた | `git clean -f -- <path>` |
| 索引に足された状態で現れた | `git rm -f -- <path>` |

このイベントにツール呼び出しを取り消す手段は無い。`enable` では終了コード 2 と標準エラーで
差し戻し、`dry-run` では `additionalContext` で報告だけする。報告は 12 件、控えは 500 件が上限。

### 7.3 前から在った変更と控え

初めて見た変更はその場でセッションごとの控え（`state/<セッション>.json`）に取り、`POST_PREEXISTING`
として 1 度だけ伝え、戻すなと明示し、自動復元の対象からも外す。以降に新しく現れたものだけを
原因付きで差し戻す。既知の変更は繰り返さない。同じ場所でも「中身が変わった」の次に「消えた」が
来れば、別の出来事としてもう一度言う（ADR-0020）。

### 7.4 ターンの基準と人への報告

`UserPromptSubmit` で、そのとき保護領域にある変更を `state/<セッション>.turn.json` に控える。
`Stop` で、その基準に無い違反だけを `systemMessage` で人へ報告し、戻す手順を 1 件ずつ添える。
戻さない。`enable` と `dry-run` で同じように報告する（`disable` はイベントそのものが走らず、
記録に `mode-disabled` が残る）。基準が無ければ黙り、記録に `no-turn-baseline` を残す
（REQ-PST-06、REQ-PST-07）。

呼び出しごとの報告はモデルへ届く。ターンの終わりの報告は、人が「結局どこが変わったのか」を
1 度で見る場所。

### 7.5 自動復元

`CCNAVI_RESTORE_IF_DENY` が `enable`（既定）のとき、新しく現れた違反だけを戻す。中身が変わった・
消えたものは `git restore --staged --worktree`、現れたものは消さずに
`state/aside/<日時>/<path>` へ退避して退避先を報告に載せる（索引にあれば先に `git rm --cached`）。
`dry-run` は戻さず、報告に `would-restore` の行を足す。`disable` は戻さず、その行も出さない
（ADR-0019）。戻せたものは控えに入れない。戻す先はコミット済みの内容なので、保護領域を汚した
未コミットの変更は失われる。

### 7.6 見えないもの

| 見えない | なぜ |
|---|---|
| `.gitignore` に入っているファイル | `git status` に出ない |
| git の作業ツリーの外 | 監視はリポジトリの中だけを見る |
| この呼び出しが触っていない別のツリー（プロジェクト・ワークツリー） | 見るのはワークスペースルートと触ったツリー。子のワークツリーはサブエージェントの終了時に基準点からの差分を見る（§9.12） |
| 失敗したツール呼び出しの副作用 | `PostToolUse` は成功した呼び出しの後にしか走らない |

---

## 8. 自己防衛（コアファイル）

### 8.1 守るもの

実行後の監視の保護領域はルールから導くので、ルールファイル自身をそこで守ることはできない。
書き換えられた瞬間に「何を守るか」の一覧ごと消える。そこで次のものだけはルールの外に
組み込みで持つ（`CCNAVI_GUARD_CORE_FILES`、REQ-SLF、ADR-0021）。

| 対象 | 何が懸かっているか | 控えを取る時点 |
|---|---|---|
| `.claude/settings.json` / `.claude/settings.local.json` | hook の登録そのもの | ツール実行前 |
| 共通層の 3 本（`.ccnavi/common/{rules,phases,risks}.yml`） | 判定の中身そのもの | ツール実行前 |
| 自身の層と各プロジェクトの層の `.ccnavi/config/{rules,phases,risks}.yml` | 同上 | ツール実行前 |
| `CCNAVI_BIN_PATH` が指すファイル（振り分けの sh）と、sh がこの機械で起動する実行ファイル | 判定器の実体 | セッション開始 |

上のうち追跡されているものは、その元リポジトリから切ったワークツリー側の設定も対象に入る（§11.6）。
hook スクリプトと保護済みスクリプトはここに無く、ルールの `deny` で止めている。ccnavi ディレクトリの下のスクリプトも無く、組み込みの `deny` で止める。

### 8.2 止める側

この面が有効な間、`deny` の先頭に最大 4 本を挿す。常に入るのは `builtin-guard-setting-files`（Bash）
の 1 本で、`builtin-guard-binary` は `CCNAVI_BIN_PATH` が設定されているとき、
`builtin-guard-project-home` は ccnavi ディレクトリの綴り（`CCNAVI_PROJECT_HOME`）が決まっているとき、
`builtin-guard-common-layer` は共通層の 3 本の置き場が決まっているときだけ足す（§11.6）。
1 本目は書き込む綴り
（`>` 系のリダイレクト、`mv` `rm` `tee` `dd` `truncate` `patch` `shred`、`sed -i`、`cp` `ln`
`install` の行き先）と場所（`.claude/hooks/`、
`.claude/settings*.json`、`ccnavi-git.sh`、実行ファイル、層の設定 3 本と ccnavi ディレクトリ、共通層の 3 本（`.ccnavi/common/`）、記録と控えの `logs/log.jsonl` `logs/state`）の組で止める。
`logs/` の下の git のラッパースクリプトの記録は、消しても判定に効かないので守らない。
`builtin-guard-binary` と `builtin-guard-project-home` と `builtin-guard-common-layer` は `Write` `Edit` `NotebookEdit` を止める。

**実行ファイルの綴り（`selfguard.binary_clause`）。** `CCNAVI_BIN_PATH` が指すのは振り分けの sh で、hook が実際に走らせるのは
sh が起動する実体のほう（§4.6）。sh だけを守ると、実体を差し替えればルールを 1 行も変えずに判定が入れ替わるので、
実体の置き場にも当てる。この綴りは `builtin-guard-binary` と、`builtin-guard-setting-files` の場所の一覧と、承認のルール
（§9.5）の実行ファイルの名前に入る。綴りを `.` `..` を落とした要素に割り、名前で 2 つの形を切り替える。

| sh の名前 | 当てる綴り |
|---|---|
| `ccnavi-launcher.sh` | sh の末尾 2 要素（`scripts/ccnavi-launcher.sh`）と、その 1 つ上の親の下の `bin/<os>-<arch>/`（既定なら `.ccnavi/bin/<os>-<arch>/`）。sh の隣の `<os>-<arch>/` は sh が探さないので当てない |
| それ以外 | 末尾 2 要素（実行ファイルそのもの） |

- **sh かどうかは名前だけで決め、`platformtag.launched_executable`（§8.3）と条件を揃える。** 段の数など別の条件を
  どちらかにだけ足すと、守る場所と控える場所が食い違う。ファイルを読まないので判定の期限に効かず、在るかどうかに
  依らず同じ綴りを返す。代償として、別の名前で置いた sh は実行ファイルそのものとして読まれ、その `../bin/` は
  `builtin-guard-binary` から外れる。導入スクリプトはその形を作らない
- **既定の配置では二重に止まる。** sh も実体も ccnavi ディレクトリの下なので、`builtin-guard-setting-files` の `.ccnavi` の綴りと
  `builtin-guard-project-home` でも止まる。`binary_clause` を残すのは、ccnavi ディレクトリを動かしたときと、既定でない綴りのため

判定に渡る綴りはワークスペースルートを継ぎ足した絶対パス（`settings._resolve_bin`）なので、段数はふつう 3 以上になり、
名指しのツール（解決済みの絶対パスに当てる）は浅い綴りでも止まる。残るのは相対で書いたシェルのコマンドだけで、
これは守らない。`CCNAVI_BIN_PATH=scripts/ccnavi-launcher.sh` での `rm -rf bin/linux-x86_64`、`ccnavi-launcher.sh` での
`rm -rf ../bin/linux-x86_64` は `builtin-guard-binary` に当たらない。`bin/<os>-<arch>` を位置を問わず当てれば 2 段のほうは
塞がるが、どのディレクトリの `bin/linux-x86_64` にも当たり、1 段のほうはそれでも塞がらない。導入スクリプトはこの綴りを
作らず、実行後の控えと復元（§8.3）はどの綴りでも効く。

止めるのは書き込む綴りと場所の組で、場所の名前が出ただけでは止めない。`cat rules.yml` も
`git add <パス>` も通る。組み込みはルールファイルに何が書いてあっても足す。`builtin-guard-` で始まる id はルールファイルのどの層にも書けず、
書けばそのルールを読み込まずに error にする（`--lint` も言う）。以前は同じ id の `deny` があると組み込みを足さなかったので、何にも当たらない
1 本をその名前で書くだけで守りが黙って消えた。組み込みを外したいなら env で面ごと切る。
既定に落ちている間と `disable` のときは足さない。既定に落ちている間は、組み込みの既定（REQ-PRE-06）がシェルの 1 本
（`builtin-guard-config-via-bash`）を持つ。こちらも呼び出しごとに同じ設定から組むので、実行ファイル・ccnavi ディレクトリ・共通層を
動かしたワークスペースでも、動かした先への書き込みを止める。以前は空の設定で 1 度だけ組んでいて、動かしたワークスペースでは
ルールファイルが壊れたときにだけ守りが外れた。

共通層の 3 本は、置き場がどこでも `builtin-guard-common-layer` が `Write` / `Edit` から止める。当てるのはワークスペースルートと、
そこから切ったワークツリーの下の同じ相対（ワークスペースルートの外に置いたなら、そのパス）。既定の置き場（`.ccnavi/common/`）なら
`builtin-guard-project-home` とも重なるが、共通層を名乗るこちらが先に当たる。見本（`rule-samples.yml`）は 3 本に入らず、既定の置き場なら
`builtin-guard-project-home` が止める。以前は ccnavi ディレクトリの外に置いたときをルールの 1 行（`guard-ccnavi-config`）に任せていたが、
その 1 行は守られるファイルそのものの中にあり、消した・書き換えたルールファイルのもとでは通った。
実行ファイル・ccnavi ディレクトリ・共通層は置き場が設定で動くので、組み込みで持つ。

### 8.3 戻す側

実行前に対象の中身を `state/selfguard/<セッション>/<鍵>` へ控え、実行後に突き合わせて、変わって
いれば控えから戻す。戻す先が「このツール呼び出しの直前」なので、コミットしていない編集は残る。
控えが無いときだけ git のコミット済みへ落ち、落ちたことを報告に書く。実行前に対象が消えていれば
控え → git の順で戻し、どちらも無ければ `.absent` のマーカーを置いて以後黙る。置いていない設定ファイルに
ついては何も言わず、無かったところに現れた場合は言うが消さない。

組み込みの既定に落ちている間の修復だけは戻さない（REQ-PRE-06）。共通層のルールファイルが実行前の控えの時点で読めず、その呼び出しが
`Write` / `Edit` / `NotebookEdit` でそのファイルを書いたときに限る。控えは壊れた中身なので、そのまま戻すと直した結果が同じ呼び出しの中で消え、
「Write / Edit で直せ」という案内と食い違っていた。判断は実行前の中身で行う。実行後の中身で見ると、読める版を壊した書き込み（dry-run の
`deny` は止めない）が戻らなくなる。シェルからの書き込み、他の設定ファイル、ワークツリー側の設定は今までどおり戻す。戻さなかったことは
`left-as-repair` として報告し、直した中身を人が確かめるよう促す。

実行ファイルは `SessionStart` で 1 度だけ控える。控えるのは `CCNAVI_BIN_PATH` が指す sh（控えの鍵 `bin`）と、
sh がこの機械で起動する実体（`bin-launched`）の 2 つ。実体は `platformtag.launched_executable` が sh と同じ順で探し、
名前が `ccnavi-launcher.sh` なら `../bin/`、それ以外は隣を見る（§8.2 と同じ条件）。見つからなければ足さない。
組み立てが無いことは sh が起動の時に言う。実体は中身のハッシュで名前を付けて
`state/selfguard/store/` へ 1 本だけ置き、セッションの側は参照（ハッシュ・大きさ・更新時刻）だけ
持つ。突き合わせは大きさと更新時刻。3 日より長く触られていないセッションの控えは、次の
セッション開始で落とす。

実行前にも見るのは、`PostToolUse` の登録を消されるとその次の呼び出しから実行後の監視が動かない
ため。`PreToolUse` の登録ごと消された場合は ccnavi が一切動かない。hook の登録を hook 自身で
守ることはできない（§12.3）。

---

## 9. チケット制御

ルールが「どこに書かせないか」を決めるのに対して、チケットは「今回どこに書くか」を決める。
ルールは長く置くもので、チケットは作業 1 本のあいだだけ効く。要求は requirements.md の REQ-TKT、
REQ-APV、REQ-RSK。用語は CONTEXT.md。

### 9.1 使うかどうか

全体ルールは全プロジェクトが使う。チケット制御（提案の承認・承認済みチケットの範囲・フェーズの HITL ポイント・
サブエージェントの制限・状態とレビューの操作）まで使うかは `CCNAVI_TICKET_CONTROL`
（`enable` / `disable`、既定 `enable`）で決める。`disable` のとき、判定・監視・診断はチケットを
一切見ず、`--approve` と `ticket` / `review` の副命令は止まり、VS Code 拡張は「チケット管理」の
入口を出さない。承認済みチケットの置き場を空にすることでは切り替わらない。

効いているワークスペースでも、すべての作業にチケットが要るわけではない。

- **直接作業**: 調査や小さな修正。チケットを起こさず、そのまま進める。判定は全体ルールだけ。
  ワークスペースルート直下と、承認済みチケットの無いワークツリーがこれにあたる
- **チケット作業**: 大きな修正。提案を書いて承認を受け、フェーズとリスクの配点に従って issue と
  マージリクエストを作りながら進める

どちらで進めるかはモデルが決める。ccnavi は判定で線引きを担保せず、`SessionStart`（起動・再開・
compact・clear のどの回も）にこの使い分けをモデルへ渡す。サブエージェントには渡さない。

渡す文に入れるのは線引きと入口（提案の置き場と `ccnavi-ticket.sh`）だけにする。セッションの
開始のたびに届く文なので、後から必要な場所で改めて届くものを頭では言わない。レビューの sh の
綴りはフェーズの終わりに来たときの文と `ready` の手順が、人がどこで見るか（`review` の `mr` /
`chat`、§9.8）はそのフェーズの HITL ポイントが、フェーズの種類の在りかは `ccnavi-ticket.sh` の
使い方（`--help`）が、リスクの配点の綴りは承認のときの検査が、後工程の進め方は承認済み
チケットが置かれたときの文が、それぞれ名指しする。

dry-run のときは末尾に 1 行足す。足す文は「止まらない」で終えず、通ったことを許可と読まない
ことまで言う。dry-run は判定に手を出さないので、deny に当たった呼び出しもそのまま実行される。
そこを黙ると、通った事実が次からの許可として読まれる。

### 9.2 置き場

| 何 | 場所 | 誰が書く | git |
|---|---|---|---|
| 提案（親も子も） | `wip/proposals/<状態>/<識別子>.md`（`CCNAVI_TICKETS_PROPOSAL`、ツリーのルートからの相対）。状態は `todo`（承認待ち）と `review`（レビュー待ち）の 2 つの置き場。プロジェクト向けはそのプロジェクトの側に置く（§11） | `todo/` は親が書く。`review/` へ動かすのは `ccnavi-ticket.sh done` だけ | それを持つリポジトリにコミット |
| 承認済みチケット | 親チケットのツリーの `.ccnavi/approved/<状態>/<識別子>.md`（`CCNAVI_TICKETS_APPROVED`、ツリーのルートからの相対）。状態は `doing`（作業中。判定が読むのはここだけ）と `done`（閉じた。取り消しは `cancelled_at` を持つ） | `doing/` へは `ccnavi --approve`（人）と、人が `accept` で起こす続きの子。`done/` へは人のレビュー（`check` / `accept` / `--reviewed` / `wrapup`）、レビュー不要の `done`、`cancel`。コミットと push は `ccnavi-push-approved.sh`（§9.4） | 親のブランチにコミット |
| フェーズのマーカー | 同 `phases/<親>/<N>.pending` / `.requested` / `.reviewed` / `.skipped` | hook、レビューのスクリプト、`ccnavi --reviewed` | 親のブランチにコミット |
| 親のマーカー | 同 `phases/<親>/ready.json` / `wrapup.json` / `closed.json`、受け入れた指摘の `accepted.json` | レビューのスクリプト、`ticket done <親>`、人 | 親のブランチにコミット |
| 子の記録 | 同 `phases/<親>/<子>.risk.json` / `.judge.json` | `ticket done` / `ticket judge` | 親のブランチにコミット |
| ワークツリー | `.claude/worktrees/<識別子>/` | 親が git のラッパースクリプトで切る | 管理外 |

状態は置き場で表す。`ls` で見え、コミットに残り、「閉じたつもり」が起きない。**チケットは 1 本の
ファイルで、2 つの置き場を行き来する（ADR-0055）。** 人が動かす向きは `.ccnavi/approved/` へ
（承認・レビュー済み・受け入れ・締め）、エージェントが動かす向きは `wip/proposals/` へ
（提案を書く・`done` でレビュー待ちにする）。場所が「次に動くのは誰か」を言う。

```
  wip/proposals/todo ──人が承認──→ .ccnavi/approved/doing
                                          │
                              エージェントが done（レビュー要）
                                          ▼
  wip/proposals/review ──人がレビュー──→ .ccnavi/approved/done
                                          ▲
                              エージェントが done（レビュー不要）／cancel
```

`review/` への直接の作成・移動は、`Write` でもシェルでも、誰がやっても組み込みが止める
（`Write` 系は `builtin-ticket-state`、シェルは `builtin-ticket-state-shell`）。`.ccnavi/approved/` は
ccnavi ディレクトリの守り（`builtin-guard-project-home`）が止める。`todo/` への作成と編集は自由。
置き場の綴りが状態名と大文字小文字まで一致しないディレクトリは読まない。

ワークスペースルート直下はチケットを持たない。

提案の置き場の既定は `wip/proposals/`（ADR-0054）。以前は `wip/tickets/` で、承認済みチケットの
置き場と名前が同じだった。綴りを設定で指していないツリーの旧の置き場に提案が残っていると、
それは走査されないまま「承認待ちは無い」で通るので、`--lint` が識別子を添えて warn で名指しする
（`lint._legacy_tickets`）。見るのは置き場の有無ではなく中に残っている提案で、置き場の有無で
決めると、新しい置き場を 1 つ作った時点で旧の残りが見えなくなる。同じ綴りが新しい置き場にも
あるものは数えない。自動では移さない。承認済みチケットの置き場の既定も `.ccnavi/tickets/` から
`.ccnavi/approved/` に変わり（ADR-0055）、提案の置き場の旧の状態（`doing/` `done/` `cancelled/`）と
合わせて、残っているものを同じ検査が名指しする。

**承認済みチケットは git で運ぶ。** 承認した人の機械にだけ在る形だと、A が承認して B の機械で作業し
A がレビューする、という流れが成り立たない。B の機械には無いので、承認は「されなかったこと」になる。
だから承認済みチケットもマーカーも親チケットのブランチに乗せ、`ccnavi-push-approved.sh` がコミットして push する（§9.4）。
B の側はセッションの頭に `ccnavi-fetch.sh` が fast-forward で取り込む。同じ sh が、
ワークツリーの起点になるデフォルトブランチ（`origin/HEAD` が指すもの）も、チェックアウト
されていなければ `update-ref` で進める。起点が古いと、そこから伸びる枝も古い枝から伸び、
承認済みチケットを古い版で判定することになるため（ADR-0060）。

代償は 3 つ。**承認は push するまで効かない**（オフラインでは他の機械に届かない）。**承認の記録が
リポジトリの履歴に残る**ので、消しても過去のコミットから復元できる。**ccnavi が入っていない機械の
エージェントが偽の承認済みチケットを push できる**（配らない形では、その機械の外に影響が出なかった）。
1 つめと 2 つめは運用で飲み、3 つめを塞ぐなら承認に署名を足すことになる（今は入れない）。

置き場は ccnavi ディレクトリ（`.ccnavi/`、§11）の下。そこは組み込みの 1 本（`builtin-guard-project-home`）が
丸ごと止めているので、承認済みチケットのために別の守りを足さずに済む。ccnavi ディレクトリの外に向けると、その 1 本が
当たらず、`--lint` が error で言う。提案の側は ccnavi ディレクトリの外に置く。親が書くものなので、ccnavi ディレクトリの下に入れると
自分の提案を書けなくなる。

**権威は親のツリーの版 1 つ。** 子のワークツリーは親のブランチから切るので、そこにも同じ承認済みチケットが
checkout される。読むのは親のツリーの側だけで、提案の `dedupe` と違い「権威の側に無ければ全部残す」には
倒さない。倒すと、親のツリーで閉じたチケットが、子に残った切った時点の版から開いたものとして復活する。
親のツリーがその識別子を開閉どちらでも持っていないときだけ、見つかった側を残す（親のワークツリーを
作る前に承認した分を落とさないため）。

### 9.3 提案の書式

frontmatter は rules.yml と同じタイプ（`allow` / `ask` / `deny`）で書く。効くのは `match` に
`Write` `Edit` `NotebookEdit` を含む項だけ。`tools` `deny_commands` `target_directories` は
「効かない」と警告して無視する。

```yaml
---
version: 1
ticket: i0050-03
issue: 50                # 親だけ。マージリクエストの Closes に写す。省ける
project: lib             # 置き場と同じ名前。省ける（決めるのは置き場。§11）
parent: i0050            # 子だけ
phase: 2                 # 子だけ。同じ親の同じ番号が 1 つのまとまり。0 以上の整数。計画があれば 1 から
predecessors: [i0050-01] # 先に閉じているべき子。案内にだけ使う
human_review:
  required: true         # 既定。省くなら理由を書く
  reason: 設定の読み込み経路を変えるため
plan: [research, implement]      # 親だけ。全体計画（§9.7）
feedback: [implement-feedback]   # 親だけ。フィードバック計画
title: 設定画面の分割
rationale: |
  Settings 配下のコンポーネント分割。
allow:
  - match: Write|Edit
    glob: "src/components/Settings/*"
ask:
  - match: Write|Edit
    glob: "src/components/*"
started_at: ""           # 以下はスクリプトが書く
completed_at: ""
base_sha: ""
---
本文は自由。承認済みチケットにそのまま写り、マージリクエストの下書きの本文に使う。
```

- ファイル名（拡張子を除く）と `ticket:` は一致すること
- 識別子は、親が `[A-Za-z0-9][A-Za-z0-9._-]*` の 1 語、子が `<親>-<2 桁連番>`。親の識別子が
  名前空間になり、別の親の子と衝突しない。深さは 2 段まで
- ワークツリーの名前は識別子と同じ
- 子は親の部分集合。子の `allow` と `ask` が指す場所は親の `allow` か `ask` の中になければならない。
  部分集合かは、子の項の字義どおりの前置に 1 文字足した綴りを親に当てて決める。子に `regex` は書けない
- 書いていない場所は範囲外。親子は厳しい側が勝つ
- 範囲の `glob` はワイルドカードが無ければ前置（`src` は `src/` 以下全部）。大文字小文字は
  `glob` も `regex` もどの機械でも区別しない（区別が要る `regex` は `(?-i:...)` で囲む。ADR-0051）。
  `..` `~` `$` と絶対パスは書けない。項は 20 件まで、
  `allow` か `ask` が 1 件は要る。綴りは書いたまま持ち、大文字小文字を無視するのは当てるときだけ
- `human_review` は子ごと。フェーズの子に 1 枚でも `required: true` があれば、そのフェーズの終わりで
  レビューが済むまで止まる。省くのに理由が無ければ warn
- `predecessors` は案内にだけ使う。判定も HITL ポイントも見ない
- `started_at` `completed_at` `base_sha` `cancelled_at` `cancel_reason` はスクリプトの欄。スクリプトが
  承認済みチケットの行を書き換える（本文と人の書いた行は保つ）

### 9.4 承認

親は計画中、ルールだけで動く。計画が終わったら人が端末で `ccnavi --approve` を打つ。標準入力が
端末でなければ拒む（`CCNAVI_GUARD_TICKET_APPROVAL=disable` で免除）。引数は無く、承認の対象は
「`todo/` に在って、同じ識別子がどの置き場（作業中・レビュー待ち・閉じた）にも無い提案の全部と、
親の改版」。承認は提案を `todo/` から `.ccnavi/approved/doing/` へ動かす（写しは残らない。ADR-0055）。
改版は同じ識別子の提案を `todo/` に書いて出す。承認で `doing/` の側の計画だけを差し替え、`todo/` の側は
消す。ワークスペースルートと
`.claude/worktrees/*` の全部の提案の置き場を走査する。識別子を並べた `ccnavi --approve <識別子>...` は
その分だけを承認の対象にする（VS Code 拡張が、絞り込みで見えている承認待ちかカードの 1 件を渡す。端末の
`ccnavi-approve.sh <識別子>...` も並べた識別子をそのまま渡し、`-` で始まる語と空の語は受けない）。絞りは対象を狭めるだけで、
絞らないときに落ちるものは通さない。承認待ちに無い識別子が混じれば何も承認せず、親の改版が承認待ちなのに
対象に無い子も何も承認しない（外すと承認済みチケットの旧い計画で検証してしまう）。対象から外した親を持つ子は
「親が承認されていない」で落ち、落ちた親は親子を引き当てる先に入れない。残すと、親の承認を通らずに
子の承認済みチケットができる。子のワークツリーには親の `wip/proposals/` が
そのまま写っているので、同じ識別子が複数のツリーにあれば権威のあるツリー（親のツリー、
親自身なら自分のツリー）の側だけを採る。権威のツリーにどれも無ければ全部残し、`--lint` が
「複数の場所にある」と言う。同じツリーの中で同じ識別子が 2 つの置き場（`doing/` と `done/`、
`doing/` と `review/` など）に在るときも同じ 1 行で言う。動かす途中で止まった跡がこの形で、
状態の操作も「複数の場所にある」で止まる。置き場の移動は、写せたが消せなければ写した側を消して
戻すので、普通は残らない。

承認は「順序」も決める。承認済みチケットをコミットするまでは子のワークツリーに渡らないので、
順序は「承認 → コミット（と push）→ 子のワークツリーを作る → `start`」。コミットの前にワークツリーを
作ると、その子には範囲が効かない。

承認画面に出るのは、件数、各提案の識別子と題（親 / 親 X、フェーズ N: 種類名）、親なら
「書き込みが許される領域」、子なら「親からどれだけ絞ったか」と種類の範囲の上限、新規の子なら親の
`issue:`（何の課題の子かを承認の前に見せる。マージリクエストの本文の Closes になる。改版では `issue` は変わらないので出さない）、子の人間レビュー
要否と先行、親の全体計画とフィードバック計画、理由、ワークツリーと提案のパス、判定に効かない記述の
warn、範囲のうち判定で止まるもの（下）。改版なら計画の差分。リスクの点は出ない（§9.9）。`y` / `yes` でまとめて承認する。1 件だけの
承認はできない。

承認時に確かめること。親の計画が `phases.yml` と噛み合うか（種類の存在、`kind`、`requires`、延期の
先にレビューがあるか）。子の親が承認の対象の中か承認済みチケットにあるか、深さ 2 段。
`project` が置き場に在るか、子は親と同じか。前のフェーズが閉じてレビュー済みか（§9.7）。error が
あればその提案は落として標準エラーに出し、読めない提案があって承認するものが無ければ 1 を返す。

**範囲の広さは承認で止めない。** 子の項が親の範囲を超える、種類の `scope` を超える、regex で
どちらにも入るかを確かめられない、の 3 つは warn。判定が親と種類で切り詰めるので（§9.5）、承認で
止める理由が無い。error に残すのは「チケットの形」の検査で、まとめて 1 度で見せて直させるため
（判定の側も同じ検査を当てるが、そちらに任せると、承認の画面では通って、あとで書き込みが止まって
から気づくことになる。§9.5）。超過は判定に効くので、判定に効かない記述の warn には混ぜず、
承認画面の見出し「範囲のうち、判定で止まるもの（承認しても書けない）」に出す。`--approve --preview --json`
では `batch[]` の `overflow[]` に載り、超過だけの子は `rejected[]` ではなく `batch[]` に入る。`--lint` も
同じ検査を共有するので warn になる。

承認済みチケットは提案そのものに `ccnavi_approved: {approved_at, source_tree, source_path}` を
足して動かしたもの（人の書いた行は保つ。置き場から決まった `project:` が無ければ足す）。改版では
`revised_at` / `feedback_at` が加わる。**この欄は承認の記録であって、承認そのものではない。**
権威は置き場で、`.ccnavi/approved/` はエージェントが書けないので、そこに在ること自体が人の合意に
なる。欄を持たない承認済みチケットも読む（ADR-0058）。欄を求め続けるのは `wip/proposals/review/`
だけで、そこはエージェントが書ける側にあり、守りが組み込みの deny 1 枚しか無い。判定が読むのは `doing/` だけで、承認後に同じ識別子の提案を
`todo/` に書いても効く範囲は変わらない（親の計画の改版だけが承認の対象に入る）。承認済みチケットと
提案の指紋照合は持たない（ボードが見せた中身の指紋は別の目的。下）。承認済みチケットそのものが承認した内容で、
承認のあとに何が書き足されたかは git の履歴で言える（ADR-0023、ADR-0055）。終わったフェーズに子を足して
承認すると、その番号のマーカーを全部消す。

**承認する場所は 3 つある。** 端末の `--approve`、VS Code 拡張のボードのオーバーレイ、そして
置き場を動かすこと（ADR-0058）。
オーバーレイは `--approve --preview --json [<識別子>...]` で一覧を読み、人が押したら
`--approve --yes <識別子,…> --digest <値> --json` を子プロセスで打つ（形は README「承認の JSON」）。
どちらも承認の対象は同じ関数から出るので、見せたものと承認するものが割れない。

| 経路 | 誰が打つ | 人が押したことの証拠 |
|---|---|---|
| `ccnavi --approve [<識別子>...]` | 人が端末で | 標準入力が端末であること。hook の中や `echo y \|` から来ていないことの、いちばん安い証拠 |
| `ccnavi --approve --yes <識別子,…>` | 拡張が、人がオーバーレイで押したときに | 見せた一覧と今の一覧が一致すること、見せた中身の指紋が一致すること。加えて、組み込みの deny がシェルからの `--yes` を止める |
| 提案を `.ccnavi/approved/doing/` へ動かす | 人が、git を触れる場所で（GitHub の画面など） | その置き場にエージェントが書けないこと。動かせたのは書ける権限を持つ人だけ |

`--yes` に端末を求めないのは、拡張の子プロセスが端末を持たないから。端末の壁を残したまま拡張から
承認しようとすると `CCNAVI_GUARD_TICKET_APPROVAL=disable` で壁ごと切ることになり、`--reviewed` と
`accept` の守りまで落ちる。壁を切るのではなく、承認だけを別の守りに移した。

**置き場を動かす経路には承認の画面が無いので、その検査を判定へ持ってくる。** 承認のときにしか
当たらなかった検査のうち、当たらないと「範囲をどこで切り詰めるか」が決まらないもの——親が引けない、
親自身が子、番号が親の計画に無い、`project:` が置き場と違う、予約名を指している——は、`scan` が
承認済みチケットに当てて `Ticket.blocked` に印を付ける。印の付いたチケットは `scope_verdict` が
範囲を当てる前に止めるので、実行前の判定・実行後の監視・サブエージェント終了時の検査の 3 か所が
同じ答えを引く。理由のコードは `DENY_TICKET_BLOCKED` で、範囲の外（`DENY_TICKET_SCOPE`）とは分ける。
受け取った側の次の一手が違うからで、範囲外なら範囲の中で済ませる道があるが、こちらは人がチケットを
直すまでどこにも書けない。フェーズの順序と計画の形はここに入れない。順序が狂っていても、その子の範囲をどの親で
切り詰めるかは決まるからで、線はそこで引いている。止めるとレビュー待ちの間その子が一切
書けなくなり、`held_phase` が `Agent` とシェルだけを止めて Write を通す形（ADR-0024）とは
結果が離れる。`--lint` が warn で言う。

**ボードの承認は、見せた中身の指紋で照合する。** オーバーレイは中身を見せてから承認を送るので、
その間に提案が書き換わると、見ていないものが承認される。`--approve --preview --json` は `digest` を出し、
`--yes` は `--digest <値>` を受け取って、承認のときに読み直した中身の指紋と比べる。違えば何も置かず
`mismatch` と `digest: {expected, current}` を返して 1。`--digest` が無くても 1（ボードの経路で
指紋を省けないように）。値の大文字小文字は吸収する。

- 覆うのは承認画面の本文と、束のチケットごとに承認のとき書き出す中身。新規は提案の frontmatter と
  本文、改版は今の承認済みチケットの frontmatter の計画だけを差し替えたもの。`ccnavi_approved` は
  除く（承認のときに付くので、見せたときには無い）
- 画面の本文だけでは足りない。画面に出ない欄（`issue`、Markdown の本文、知らない欄）を見せたあとに
  書き換えても通ってしまう
- 計算は、部分ごとの SHA-256 を件数と一緒に改行で並べ、その全体の SHA-256。区切り文字でつなぐ形は
  取らない。Markdown の本文は生の制御文字を素通しするので、区切りを本文に含めてつなぎ目をずらせる
- ADR-0023 がやめた指紋（承認済みチケットと提案の照合）とは目的が違う。こちらが止めるのは、見せてから
  押すまでの数分間の書き換え。承認したあとの合意は、今までどおり承認済みチケットそのもの

**承認済みチケットは運ぶまで届かない。** 承認済みチケットは親のツリーの置き場
（`CCNAVI_TICKETS_APPROVED`）に置かれ、コミットして push するまで子のワークツリーにも他の機械にも
渡らない。どちらの経路でも、承認のあと `ccnavi-push-approved.sh` が運ぶ。変更のあるツリーごとに
置き場と、承認で `todo/` から消えた提案（追跡されていたものの削除だけ）をコミットし（パスを限る。
`-a` も `add -A` も使わない）、保護されたブランチ
（`main` / `master` / `develop` / `release` / `release/*`）でなければ push する。承認はしない。

| 経路 | 運び方 |
|---|---|
| 端末の `ccnavi-approve.sh` | 承認が通ったあとに sh から呼ぶ。運ぶのに失敗しても承認は 0 で終わる（コミットは残り、打ち直せば送れる） |
| ボードの承認 | 1 件以上承認したら、`ccnavi-push-approved.sh` を絶対パスで端末に送り、Enter まで送る。sh が無ければ送らず、導入スクリプトで配るよう警告で言う |

- エージェントからは組み込みの deny（`DENY_TICKET_APPROVAL_CLI`）で止める（§9.5）。承認済みチケットを
  運ぶことは合意そのものではないが、push は外へ出す操作で、運ぶ時機は人が決める
- sh の側で標準入力が端末かを確かめる検査は入れない。push はそこまで守らない
- 運ぶのは sh の仕事。実行ファイルはネットワークに出ないので（P11、§9.10）、`--approve` は置き場に
  書くところまでで止まる

**絞りと「見せたもの」は別の引数で渡す。** `--approve --yes <識別子,…> [<絞り>...]` の 2 つは
役割が違う。`--yes` は「オーバーレイに出ていた識別子」、後ろに並べる語は「そのとき掛けていた絞り」
（`--preview` に渡したものと同じ）。同じ列に載せると検査が自分自身と比べる形になり、必ず一致する。
絞ってから比べるのではなく、**その絞りで今できる一覧**と**人が見た識別子**を比べる。絞り込みが無ければ
絞りは空で、承認待ち全部と比べる。ここを 1 つの引数にまとめたために、実際に検査が素通りしたことがあり、
受入テストが拾った。

一覧を見るだけの `--approve --preview` は、承認済みチケットを置かないのでエージェントにも通す。免除するのは
`--preview` の付いた `--approve` だけで、`--yes` は免除しない。免除の範囲はコマンド 1 本まで
（Bash は shellread の区切り、PowerShell は生の文字列なので `;` `&` `|` と改行を区切りと見る）。
Bash の側は語の中の目印 `\x01` もまたがない。またぐと、`ccnavi --approve x "a --preview"` のように
引数の値に書いた `--preview` が免除の理由になる。
ここを緩く書くと、後ろのコマンドに `--preview` を足すだけで承認が通る。**免除は許さない側から書く。**
許す側から条件を足すと、条件が増えるたびに免除が広がる。

**免除の理由になるのは、単独の語として立った `--preview` だけ。** 前は生の空白、後ろは空白か
区切りか行末。語の中の目印（引用がつないだ空白）は数えない。これを見ないと、別のフラグの
**値**に書いた `--preview` で免除が成立する。`ccnavi --approve --reason=--preview` は、argparse が
`--reason` の値として食うので `--preview` は立たず、実行ファイルは本物の `--approve` を走らせる。
hook が見る文字列と、実行ファイルが走らせる枝がそこでズレる。

**承認を頼む前の確認は、その `--preview` に相乗りする。** `--approve --preview --verify [<識別子>...]` は、
置かずに「いま `--approve` を打てば、その提案が承認の対象に入るか」を終了コードで返す（REQ-APV-13、ADR-0059）。
**0 が「はい」、`modes.EXIT_ANSWER_NO`（3）が「いいえ」。** 1 はほかと同じ「使い方か設定の誤り」で、
答えとは分ける。同じ値にすると、打ち方を間違えた回と提案が落ちる回が読む側から区別できない。
判定は承認と同じ `gather`・`candidates` が持ち、`--verify` は読み替えるだけ。`--approve` が
落とさないもの（範囲の超過と、読めない提案）では落とさない。落とすと「確かめは『いいえ』なのに
承認は通る」になる。走査は絞る前の全ツリーを見るので、他のセッションの書きかけ 1 本で、
自分の提案は通るのに「直せ」と言われることにもなる。答えを `--preview` 自身に持たせないのは、一覧を読む側（ボード）が 0 以外を失敗として
扱うからで、対象にしない提案が 1 件あるだけで一覧が出せなくなる。承認で落ちるものは `--lint` も
同じ `candidates` を通って名指しする（終わり方だけが違う）。ただし「まだ承認できない」もの（前のフェーズが
閉じていない子。`rules.KIND_NOT_YET`）は `--lint` では warn。あの終了コードは CI と設定画面の保存が
見るので、編集と関わりのない提案 1 本でそこを止めない。

提案を `todo/` に書いた回に、この確認を勧める文を 1 度だけ渡す（REQ-APV-14、`ticket.propose_notice`）。
**判定には足さない。** 文であって判定ではないので、ルールの表ではなく承認の知らせ（`approval.news`）
と同じ口から渡す。表に allow を 1 本足す形も試したが、`todo/` が「ccnavi が言及する場所」になり、
どのタイプも言及しないときの倒し方を通らなくなる（確認できる者が居ないモードの deny も、知らない
綴りのモードを ask に倒す既定も、そこだけ外れる）。加えて、判定は強いタイプから見て最初に当たった
段で決まるので、提案の置き場に `deny` か `ask` を書いているワークスペースには文が届かない。
承認の流れをいちばん気にしているところにだけ届かない、という向きになる。

`CCNAVI_MODE=dry-run` の間は deny が止めないので、エージェントは `--yes` を打てる。dry-run は
「判定を言うだけ」のモードで、承認の経路だけを例外にはしない。例外を作ると、dry-run で確かめている
判定と本番の判定が別物になる。

**承認されたことは hook が伝える。** 人がボードで承認しても、モデルはそれを知らない。人が
「承認した」と打つまで後工程に入れない形は、承認を拡張へ移した意味を半分に減らす。

- 控えは `<state>/approved-<session>-<agent>.json`。中身は「識別子 → 版」で、版は承認済みチケットの
  `approved_at` と `revised_at`。改版は承認済みチケットを書き換えるだけで識別子を増やさないので、識別子だけを
  比べても新しい合意だと分からない
- 起点はそのセッションの最初の hook。それ以前の承認済みチケットは伝えない。`SessionStart` は控えが無ければ
  起点を作るだけで、控えがあれば触らない（compact の前の承認は、compact の後にも 1 度伝える）
- 伝えるのは `UserPromptSubmit` と `PreToolUse`。どちらでも `additionalContext` で届く
- 閉じた承認済みチケットも見る。承認の直後・次の hook の前に子が閉じると、開いた承認済みチケットだけを見る形ではその承認が
  誰にも伝わらないまま控えに吸われる
- 控えが読めるのに壊れているときは「無い」と同じには扱わない。伝えていない承認ごと現状を起点にして
  黙るより、もう一度伝えるほうがよい
- 控えの読み書きは直列化しない。同じセッションの hook が同時に走ると 2 度伝わることがある。
  伝えすぎる側なので受け入れる

拡張は同じ文を通知から人に渡し、人が進行中のセッションに貼るか、新しいセッションを開く。走っている
Claude Code のセッションへ拡張から文を送り込む公開の口は無いので、そこは人の 1 手を残す。

### 9.5 判定への効かせ方：鍵はファイルの行き先

`Write` / `Edit` / `NotebookEdit` の対象を解いた先が `.claude/worktrees/<名前>/` の中なら、その名前と
同じ識別子の開いた承認済みチケットで判定する。呼び出し元の `cwd` も、サブエージェントかどうかも見ない
（REQ-TKT-01、ADR-0024）。範囲の前置はそのワークツリーのルートから解く。

ワークツリーの特定は、パスの前置だけでなく `.git` ファイルの `gitdir:` と元リポジトリの
`.git/worktrees/<名前>/gitdir` の相互参照でも確かめる。前置だけだと、同じ名前のただの
ディレクトリをワークツリーと読み違える。候補が複数あるときは最長一致を採る。短い側（ワークスペース
ルート）に先に畳むと、`.claude/worktrees/x/src/a` がワークスペースルートの下として判定され、
x のチケットが効かなくなる。

ルールの判定とチケットの判定を両方出し、強い側を採る。強さは `deny` > `ask` > `allow` > 何も言わない。
チケットの範囲の外と、チケットが `deny` と書いた項は `deny`。同じ強さならルールを採る。判定は同じで、
人が書いたルールの文面のほうが、その場所で何に気をつけるかを具体的に言っているため。ルールが `deny` なら
チケットは見ない。結果が変わらず、承認済みチケットの走査の代金だけが掛かる。

| ルール | チケット | 結果 |
|---|---|---|
| `deny` に当たる | どれでも | 止まる（ルールの文面） |
| `ask` / `allow` に当たる | 範囲の外、または `deny` の項 | 止まる（`DENY_TICKET_SCOPE`） |
| `ask` に当たる | 範囲の中（`ask` / `allow`）、またはチケットが無い | 人に確認が出る（`RULE_ASK`） |
| `allow` に当たる | 範囲の中（`ask`） | 人に確認が出る（`TICKET_ASK`） |
| `allow` に当たる | 範囲の中（`allow`）、またはチケットが無い | 通る |
| 何も言わない | 範囲の中（`allow`） | 通る |
| 何も言わない | 範囲の中（`ask`） | 人に確認が出る（`TICKET_ASK`） |
| 何も言わない | 範囲の外、または `deny` の項 | 止まる（`DENY_TICKET_SCOPE`） |
| 何も言わない | ワークツリーにチケットが無い | 権限モードに従う（`UNDECLARED`） |

「範囲の外」には、子の範囲の中だが親の範囲かフェーズの種類の上限の外を含む（下の切り詰め）。

**理由コードは増やさない。** 範囲の外も、チケットの `deny` の項も、ルールを狭めた回も、受け取った側の次の一手は
「範囲の中で済ませるか、範囲を持つチケットを提案する」で同じ。区別は文面と記録で持つ。チケットがルールより
強かった回は、文面の頭に `rule:` 行を足し、記録の `rules` を `(ticket-scope)` とルールの id の並びにし、`source` を
空にする（判定を下したのが層を持たないチケットだから）。チケット（子か親）の `deny` の項に当たったときは
`ticket entry:` 行も足す。

```
rule: <id> (<allow|ask>) lets this through, but the ticket for this worktree narrows it
ticket entry: deny <書かれた綴り>
```

ルールの `additionalContext` は、どちらが判定を決めても載せる。承認済みチケットの走査は 1 回の判定で 1 度だけで、
プロジェクトの食い違いの点検と同じ一覧を使う。承認画面の親の見出し「このチケットで書き込みが許される領域」の下と、
`--explain` のチケットの節の頭に、範囲がルールの `allow` / `ask` より強いことを 1 行で書く。

**子の範囲は親と種類でも切り詰める。** 子 → 親 → 種類の `scope` の順に当て、厳しい側を採る。
種類の上限は `allow` か外しか言わないので、子が `ask` と書いた場所が種類の中なら `ask` のまま。
regex で書いた子の範囲は、実際のパスで親と種類に当てる。承認は範囲の超過を warn に留めるので
（§9.4）、止めるのはここ。実行前の判定、実行後の監視（`POST_TICKET_SCOPE`）、SubagentStop の
差し戻しが同じ合成を使う。理由コードは増やさない（記録を読む側と拡張の数え方を変えずに済む）。
どの上限で外に出たかは、文面の `limit:` 行で名指しする。子自身の範囲の外なら今の `scope:` 行で
足りるので足さない。

```
limit: phase type 設計 (design): wip/design/*, docs/*
limit: parent i0001: src/*, tests/*
```

**種類が読めないときは、種類では切り詰めない。** どの層にも phases.yml が無いか、種類の `scope` が
`inherit` なら、親までで切り詰めて何も言わない。phases.yml が実在するのにその番号の種類を引けない
（壊れた、種類を消した）ときは、親では切り詰め、種類では切り詰めず、実行前の判定に注記
「種類 `<id>` が読めないので、種類の上限では切り詰めていない」を出す。phases.yml の読み込みで
上がる `ValueError` 系（`UnicodeDecodeError` を含む）も「壊れている」と扱い、例外を外へ出さない。
判定が例外で終わると終了コード 1 になり、Claude Code はそれを止める合図と読まないので、止めるはずの
書き込みが通る。

deny に倒さないのは、phases.yml がコアファイル（§8）でエージェントが壊せず、親の切り詰めは残るから。
倒すと、人が phases.yml を編集している間、全部の子のワークツリーで書き込みが止まる。代償として、
壊れている間は種類の上限が効かない。注記と `--lint` の error で気づかせる。

チケットは閉じる向きにしか効かない。ルールの `deny` と `ask` を緩めることは無く、ルールの `allow` は狭める。
未承認のチケットは何も効かせず、結び付いたワークツリーが無いチケットも効かない（承認は残るので、同じ名前で
作り直せばまた効く）。

チケットの置き場（提案の置き場 `CCNAVI_TICKETS` と承認済みチケットの置き場 `CCNAVI_TICKETS_APPROVED` の下）は、
範囲の外として扱わない。塞ぐと、次のチケットを提案する道が無くなり、いちど承認した範囲から出られない。
提案は承認されるまで何にも効かないので、開けても範囲は広がらない。状態の置き場と承認済みチケットは組み込みの
`deny`（§9.2、§8.2）が先に止める。ワークツリーのルートからの相対パスを `/` の境で見て大文字小文字を区別しない。

下書きの置き場（ワークツリーのルートの直下の `scratchpad/`）も、範囲の外として扱わない。塞ぐと、範囲を宣言した
ワークツリーほど手元に何も置けなくなり、承認が要る作業だけが下書きの場所を失う。

外してよい根拠は 1 つだけ、**そのツリーの git が `scratchpad/` を追跡しないこと**。追跡されなければ、そこに書いた
ものは統合先のブランチに 1 バイトも乗らない。この根拠は保証ではない。`.gitignore` は各リポジトリが持つもので、
ワークスペースの 1 行はプロジェクト（`projects/<名前>/` は自分の git を持つ）にも、別のブランチから切った
ワークツリーにも届かない。

だから**外すのは実行前の判定だけ**にしてある（`ticket.is_unscoped`）。実行後の監視とサブエージェント終了時の
検査は、チケットの置き場だけを外して `scratchpad/` は外さない。この 2 つの入力は `git status`（`--ignored` を付けない）と
`base_sha..HEAD` の差分（追跡ファイルだけ）で、**追跡から外れている `scratchpad/` はそこに 1 本も現れない**。つまり
正しく設定されたリポジトリでは、外しても外さなくても答えは同じで、外す意味が無い。答えが変わるのは `scratchpad/` が
追跡されているとき、すなわち根拠が既に崩れているときだけで、そこで外すと、崩れたことを知らせる唯一の経路を
自分で塞ぐことになる。実行前に通ったものが実行後に咎められる形は残るが、咎められるのは「このリポジトリでは
`scratchpad/` が追跡されている」ときだけで、それは本当に知らせるべきことになる。

`--lint` は、追跡されているファイルが `scratchpad/` にあるリポジトリと、`scratchpad/` を無視していないリポジトリを警告で
名指しする（ワークスペースと各プロジェクト）。`.gitignore` は既に追跡されているファイルには効かないので、
無視の設定だけでなく索引の中身も問う。

この 1 つだけ、綴りの大文字小文字を**区別する**。範囲の照合とは逆で、外してよい理由が「追跡されない」ことに
あり、追跡から外しているのは `.gitignore` の `/scratchpad/` で、その照合は Linux では区別するため。区別せずに外すと、
Linux の `SCRATCHPAD/` が「追跡されるのに範囲を当てない場所」になり、承認した範囲の外の変更が統合先へ乗る道ができる。
区別する側に倒せば、除外はどの機械でも追跡から外れる範囲より狭いままで、狭いぶんは範囲の判定が止めるだけで済む。
同じ理由で、ルートの直下 1 段だけを見る（`docs/scratchpad/` は普通の作業対象）、同じ名前のファイルは外さない
（`/scratchpad/` の末尾の `/` はディレクトリにしか当たらない）、置き場の名前は設定で動かさない。

2 つの例外を外すかどうかは、実行前の判定・実行後の監視・サブエージェント終了時の検査が
同じ関数（`ticket.is_unscoped`）を通って決める。

`Read` `Grep` `Glob` は範囲を見ない。Bash の実行前の判定にもチケットの範囲は当てない。コマンド
文字列からパスを追えないため。シェルが書いたものは実行後の監視が `POST_TICKET_SCOPE` で言い
（§7.2）、子のワークツリーについてはサブエージェントの終了時に基準点からの差分を見る（§9.12）。

行き先のツリーが属するプロジェクトと、そのツリーに結び付く承認済みチケットの `project:` が違えば
`DENY_TICKET_PROJECT_MISMATCH` で止める（§11）。

サブエージェント（`agent_id` 付き）からの `ccnavi-ticket.sh` / `ccnavi-review.sh` の操作と
`ccnavi-git.sh push` は、ルールより先に `DENY_SUBAGENT_TICKET_OP` で止める。シェルから実行ファイルを
`--approve` / `--reviewed` 付き、または状態とレビューを動かす副命令（`ticket start|done|cancel|judge`、
`review prepare|requested|check|handoff|ready|wrapup`）付きで直接打つ形は、素性によらず
`DENY_TICKET_APPROVAL_CLI` で止める（ADR-0029）。`ccnavi-approve.sh` と `ccnavi-push-approved.sh` を
`sh` / `bash` で打つ形も同じ理由コードで止める。綴りの大文字小文字によらない。macOS と Windows の
ファイルシステムは `CCNAVI-Approve.SH` でも同じ sh を開くので、区別すると綴りを変えるだけで通る。
`review note|fetch|origin` は sh だけの仕事なので当てていない。

どちらも実行役のコマンド越しに打った形を、中で実行されるコマンドで止める（§6.3.1）。承認のルールの正規表現は
実行ファイルの綴りを先頭に固定したままで、`env sh <sh> --approve --yes x`、`sudo -u me sh -c '<sh> …'`、`source <sh> …`、
`find … -exec <sh> … ;` は層の `<sh> --approve --yes x` に当たる。実行ファイルの綴りには `selfguard.binary_clause`（§8.2）が入るので、
`sh .ccnavi/bin/<os>-<arch>/ccnavi --approve …` も同じルールで止まる。承認のルールにだけ実行役のコマンドの一覧を持たせる形は、
組み込みの守りとサブエージェントの禁止に同じ穴を残すので採らない。

### 9.6 状態遷移

状態を持つ場所は 2 層で、層ごとに動かす者と条件が違う。チケットの置き場（1 本のファイルが
提案の置き場と承認済みチケットの置き場を行き来する。ADR-0055）と、マーカー。判定に効くのは
`.ccnavi/approved/doing/` に在る承認済みチケットだけ。マーカーは HITL ポイントにだけ効く。
ワークツリーの有無は状態として持たない。

**チケットの遷移**

```
                approve                     done（レビュー要）
  todo ─────────────────→ doing ───────────────────────────→ review
  (wip/proposals)      (.ccnavi/approved)                (wip/proposals)
                            │                                  │
                            │ done（レビュー不要）／cancel      │ check / accept / --reviewed / wrapup
                            ▼                                  ▼
                          done ←───────────────────────────────┘
                     (.ccnavi/approved)
                            │
                            └── 人が手で doing/ へ戻す（再開）
```

| 遷移 | 動かす者 | 条件 | 書く欄 |
|---|---|---|---|
| 無し → `todo` | 親が `Write` / `Edit` で作る | 無し | ― |
| `todo` → `doing` | `ccnavi --approve`（人）、ボード | §9.4。同じ識別子がどの置き場にも無い | `ccnavi_approved`、置き場から決まった `project` |
| `todo` → `doing`（手で） | 人が hook の外で動かす | 無し。動かせること自体が条件（その置き場にエージェントは書けない）。承認の検査は判定が当てる（ADR-0058） | ― |
| `todo`（改版）→ 消える | `ccnavi --approve`（人） | 同じ識別子が `doing/` にあり、計画だけが違う。`doing/` の側の計画を差し替える | `revised_at` / `feedback_at` |
| `doing` のまま | `ccnavi-ticket.sh start` | 着手の欄が空。`.claude/worktrees/<識別子>/` がワークツリーとして在り、元リポジトリが承認済みチケットの `project` と合い、綴りが大文字小文字まで同じ。子なら親が `doing/` に在って着手済み | `started_at`、`base_sha`（そのワークツリーの HEAD） |
| `doing` → `review` | `ccnavi-ticket.sh done` | 着手済み。子で、閉じた時点のフェーズがレビュー要（延期を含む。§9.8）。種類の成果物が在り、定性のリスク判定が揃っている（§9.9） | `completed_at` |
| `doing` → `done` | `ccnavi-ticket.sh done` | 着手済み。子ならフェーズがレビュー不要。親なら開いている子が無く、レビューで止まっておらず、計画があればフィードバック計画が承認済みで全フェーズが終わっている（締めていれば子だけ） | `completed_at` |
| `doing` → `done`（取り消し） | `ccnavi-ticket.sh cancel --reason`、`wrapup`（未着手の子） | 理由が空でない。親なら開いている子が無い | `cancelled_at`、`cancel_reason` |
| `review` → `done` | `ccnavi-review.sh check` / `accept`、`ccnavi --reviewed`、`wrapup`、フィードバック計画の承認 | そのフェーズ（延期を引き受けた分を含む）のレビューが済んだ（§9.10） | ― |
| 無し → `doing`（続きの子） | 人が `accept` / `--reviewed --chat` で選ぶ | レビューで指摘が残った（§9.10）。同じフェーズの番号、範囲は見た子の和 | `ccnavi_approved`（`followup_of`） |
| `done` → `doing` | 人が hook の外で動かす | エージェントは `Write` でもシェルでも止まる。再開の意図がレビューのやり直しなら、そのフェーズのマーカーも手で消す | ― |

スクリプトは置き場を動かして欄の行だけを書き換える。本文と人の書いたコメントは保つ（承認も
欄を足すだけで、読み直して書き出さない）。コミットは親がする。`start` と `done` と `cancel` は
`doing/` に在るチケットにしか効かない。未承認の提案（`todo/`）は消せばよく、取り消しの記録は要らない。
同じ識別子が `doing/` と `todo/` の両方に在るときは `doing/` の側を相手にする（`todo/` は改版の
候補か書き損じで、`--lint` が言う）。それ以外で 2 つの置き場に在ると動かさない。

閉じる向き（`doing` → `review` / `done`）は範囲が消える向きなので承認は要らない。再開は範囲が戻る
向きなので人の手に置く。親を閉じても子の承認済みチケットは閉じない。

**子の着手は親の着手のあと。** 親の `start` を飛ばしても途中では何も壊れず、詰まるのは親を
閉じるときだけで、そこは気付ける場所としていちばん遅い（`done` が「作業中ではない」と言う）。
止める場所を最初の子の着手に置くと、親の作業が実際に始まる瞬間に言えて、親の `started_at` も
実態に合う。代償は、最初の子を着手する前に 1 コマンド増えることと、親のワークツリーが要ること
（`start` はワークツリーの HEAD を基準点に書くので、親のワークツリーを作らない進め方では親の
`start` 自体が通らない）。親が `doing/` に無いとき（未承認、閉じた）は着手してよい状態では
ないので、置き場を名指しして同じ場所で止める。案内は置き場で分かれ、`todo/` なら承認から、
`doing/` で未着手なら親の `start` から。親がどこにも無いときと、複数の置き場にあって定まらない
ときも、同じ場所で止めて見えたとおりを言う（`start` の他の前提と同じ扱い）。

**マーカーの遷移**

フェーズは同じ親の同じ `phase` 番号の子のまとまり。「終わった」は導出で、そのまとまりの子が `doing/` に
1 枚も無く、`review/`（レビュー待ち）か `done/`（取り消しでない）に 1 枚以上あること。取り消しだけの
フェーズは終わらない。「レビュー要」も導出（§9.8）。止めているのは **終わった ∧ レビュー要 ∧
`reviewed` が無い** のとき。`pending` と `requested` は止める条件に入らない（`requested` は
呼び名を「レビュー準備中」から「レビュー待ち」へ変えるだけ。§9.8）。

```
    無し ──終わった ∧ mr で見る──→ pending ──request──→ requested ──check / --reviewed──→ reviewed
     │
     ├────終わった ∧ chat で見る──→ pending ──────────--reviewed --chat─────────→ reviewed
     │
     └────終わった ∧ レビュー不要──→ skipped

    どのマーカーからも: 同じ番号に子が承認される ──→ 無し
```

| 遷移 | 動かす者 | 条件 |
|---|---|---|
| 無し → `pending` | `PostToolUse` の告知（cwd が親のワークツリー） | 終わっていて、マーカーが 1 つも無く、レビュー要。文を 1 度だけ返す |
| 無し → `skipped` | 同上 | 終わっていて、マーカーが 1 つも無く、レビュー不要（または延期） |
| 任意 → `skipped` | `ccnavi-review.sh wrapup`（人） | 終わっていないフェーズの全部に置く（§9.11） |
| 任意 → `requested` | `ccnavi-review.sh request`（親） | §9.10 の前提。`pending` は要らない |
| `requested` → `reviewed` | `ccnavi-review.sh check`（親）、`ccnavi-review.sh accept`（人）、フィードバック計画の承認、`wrapup` | 変更要求のレビューが無い。`check` は未解決が 0、`accept` は未解決を人が受け入れる（続きの子を起こす選択ではマーカーは置かず、そのフェーズのマーカーを消す）。置いたときに `review/` の子は `done/` へ動く |
| `pending` → `reviewed` | `ccnavi --reviewed <N> --chat`（人が端末で） | 見る場所が `chat`。依頼の記録は要らない（§9.8） |
| 任意 → 無し | `ccnavi --approve` で同じ番号の子が承認された | 4 種を全部消す |

マーカーは人が子を再開しても残る。再開の意図がレビューのやり直しなら、そのフェーズのマーカーも手で消す。

**2 層の組み合わせで決まるもの**

| 見るもの | 使う層 | 決まり方 |
|---|---|---|
| `Write` / `Edit` の判定 | チケットの置き場、ワークツリー | 行き先のワークツリーと同じ識別子の `doing/` の承認済みチケット。無ければルールだけ |
| `SubagentStop` の範囲外の検査 | チケットの置き場 | cwd のワークツリーが子ならその子、親ならその親の `doing/` の子の全部。基準点は承認済みチケットの `base_sha` |
| HITL ポイント | チケットの置き場、マーカー | cwd のワークツリーと同じ識別子の `doing/` の親 → その子の置き場で「終わった」→ マーカー |
| レビューの依頼の前提 | チケットの置き場、マーカー | 終わっている、`requested` が無い、ほか git の条件 |
| `SubagentStart` の一覧 | チケットの置き場、ワークツリー | cwd のワークツリーが親ならその親の `doing/` の子、子ならその子 |
| `--approve` の承認待ち | チケットの置き場 | `todo/` に在って同じ識別子がどの置き場にも無い提案と、親の改版 |

### 9.7 フェーズの種類と計画

番号だけのフェーズは「2 番目のまとまり」以上のことを言わない。`phases.yml`（共通層と親の `project:` の層の和。§11.4.1）に
**フェーズの種類**を定義し、親が `plan:` にその並びを書くと、フェーズに意味が付く（ADR-0026）。
種類は人が持つ設定で、エージェントは書き換えない（コアファイル。§8）。組み込みの既定は無く、
どの層にも無ければフェーズは番号だけの挙動で、`plan:` は読めない。

| 欄 | 意味 | 効く場所 |
|---|---|---|
| `kind` | `work` は全体計画に、`feedback` はフィードバック計画にしか置けない。既定 `work` | 承認 |
| `title` | 人向けの名前。id と title はどちらも一意。重なれば `--lint` が error | 承認、案内 |
| `review` | `none` / `chat` / `mr`（既定）。そのフェーズの終わりに人がどこで見るか。`chat` はこのセッションで見る（ホストへ出ない。§9.8）。既定であって上限ではない。`feedback` の種類に `none` は書けない | HITL ポイント |
| `scope` | この種類の子が宣言できる範囲の上限（glob の並び）。省略か `inherit` なら親の範囲。子 ⊆ 種類 ⊆ 親。大文字小文字はどの機械でも区別しない（チケットの範囲と同じ規則。§9.3）。超えても承認は止めず、承認画面に判定で止まるものとして出す（§9.4）。判定は子の範囲をこの上限でも切り詰める（§9.5） | 承認（警告）、判定 |
| `deliverables` | フェーズを閉じる前に、親か子のワークツリーに在って追跡されているべきファイル（glob）。中身は見ない | `ticket done` |
| `overlap` | 並行してよい種類。前のフェーズがこの種類なら、閉じる前に次を承認できる。対称 | 承認 |
| `requires` | 計画にこの種類を置くなら一緒に置くべき種類 | 承認 |
| `agent` / `when` | 案内。`agent` は `SubagentStart`、`when` は次のフェーズを促す文に出す。判定には使わない | 案内 |

親の計画は `plan`（全体計画。作業フェーズの種類の並び）と `feedback`（フィードバック計画。
フィードバック対応フェーズの種類の並び）。項は名前だけか `{type, review}`。`review` に書けるのは
`mr`（種類が `none` でも要る）と `defer`（延期。次にレビューがあるフェーズの依頼に含める）だけ。
最後の項は `defer` にできず、種類が `none` の項も延期できない。番号は全体計画が 1 から、
フィードバック計画がその続き。`feedback` が無いのは「まだ計画していない」、`[]` は「見たうえで
対応なし」。

**延期は省略ではない。** 延期したフェーズでは止まらず、次にレビューがあるフェーズの依頼が
延期した分も含めて出る。マーカーは「レビューがあるフェーズの番号」に付く。

**合意は全部 `--approve` を通る。** 全体計画は親の承認。各フェーズの計画は、その番号の子を提案して
承認を受けること。別の文書は求めない。子の題・理由・範囲・本文が計画そのもので、承認画面が
それを見せる。フィードバック計画は `feedback:` を足した親の改版の承認で、全体計画の最後の
レビューが済んでから 1 回だけ。指摘が無くても `[]` で出す。承認画面は人が受け入れた未解決スレッドの数を見せ、承認済みチケットに `feedback_at` が残る。前提は、全体計画の
フェーズが全部終わり、最後のフェーズのレビューを依頼してあること（通っていなくてよい）。

**順序は承認で止める。** N 番目の子は、N-1 番目までが全部閉じてレビュー（延期でなければ）が済むまで
承認されない。`overlap` の組と、フィードバック計画を同時に出した最後のフェーズは例外。
`ticket start` の時点の検査はこれに吸収する。フェーズには必ず子が 1 本以上ある。親は計画・
合流・依頼だけをし、作業はしない（P12）。

**改版**は「承認済みチケットは動かない」の唯一の例外。提案の `plan` / `feedback` が承認済みチケットと違えば改版の候補に
なり、`--approve` が差分を見せる。変えられるのは `plan` と `feedback` だけで、範囲や題や `issue` が
違えば拒む。`plan` は子がまだ承認されていない番号の項に限る（番号がずれると子の `phase` が指す先が
変わる）。`feedback` の承認後は新しいフィードバック対応フェーズを足せない。承認済みのフィードバック
対応フェーズの中で差し戻しを受け、同じ番号に子を足してやり直すのは何度でもできる。

**局面の名前。** `--explain` と `SubagentStart` は親の局面を名指しする。全体計画待ち（承認済みチケットが無い）、
作業中（N 番目: 種類名）、レビュー準備中、レビュー待ち、フィードバック計画待ち、フィードバック対応中、
閉じられる、閉じられる（利用者が締めた）、閉じられる（フィードバック対応済み）。計画が無ければ空。

### 9.8 フェーズの終わりと HITL ポイント

フェーズの終わりは、人の手が入るところ（HITL ポイント。Human In The Loop）の 1 つ。人の手は
範囲の承認・レビュー・未解決の受け入れの 3 か所に寄せてあり（ADR-0030）、ここはその 2 つめ。

同じ親の同じ `phase` の子が `doing/` に無く、`review/` か `done/` に 1 枚以上あるとき、
そのフェーズは終わり。終わりの扱いは**人がどこで見るか**で 3 つに分かれる。子を閉じる
`ticket done` は、その子を閉じたものとして数えたこの答えで行き先を決める。レビュー要（延期を含む）なら
`wip/proposals/review/`、不要なら `.ccnavi/approved/done/`（ADR-0055）。レビューが済むと
`review/` の子は `done/` へ動く。

見る場所は `none` / `chat` / `mr` の 3 値で、次の厳しい側が勝つ（`none` < `chat` < `mr`）。

- 種類の `review`（§9.7）
- 計画の項の `mr`（種類が `none` でも `mr` にする）
- 引き受けた延期の分（延期した項は自分では持たない。引き受けた側が、引き受けた分の
  宣言と自分の宣言の厳しい側で見る。覆っている分の種類が読めなくなっていたら `mr` に
  倒す。延期できるのはレビューのある種類だけなので、そこには必ず見る場所を言った者が
  居た。読めなくなったことでその番号のレビューが消えてはいけない）

閉じた子の `human_review.required` と、実績のリスクが HIGH 以上であること（§9.9）は、
**要るとだけ言い、場所は言わない**。だから宣言が `none` のフェーズを `chat` へ上げるだけで、
宣言された `mr` を `chat` に落とすことはない。場所を言う者が 1 人も居ない番号（計画が無い、
種類のファイルが無い、その名前の種類が読めない）では今までどおりで、子が `required: true`
と言うか実績が高ければ `mr`。緩い側に倒すと、種類が読めないときにレビューの行き先が消える。

| | `mr` | `chat` | `none` |
|---|---|---|---|
| 最後の子を閉じた呼び出しの `PostToolUse` で返す文 | 「合流と push を済ませ、`request` でレビューを頼み、ターンを終えて利用者を待て」 | 「合流して利用者に差分を見てもらい、ターンを終えて待て。先へ進めるのは利用者が端末で」 | 「人間レビューを省略して次のフェーズへ進む」 |
| マーカー | `pending` | `pending` | `skipped` |
| HITL ポイント | 来る。`reviewed` のマーカーまで止まる | 来る。`reviewed` のマーカーまで止まる | 来ない |
| 開ける者 | `ccnavi-review.sh check` / `accept`（§9.10） | 人が端末で `ccnavi --reviewed <N> --chat` | — |

`chat` で通したあと、人が端末で指摘を 1 行ずつ打てば、`accept` の `f` と同じ形で続きの子を
`.ccnavi/approved/doing/` に起こす（§9.10）。何も打たなければ起こさない。

**`chat` はこのセッションで人が見る。** ホストへ出ないので、マージリクエストも認証も要らず、
`.ccnavi/scripts/` の sh は 1 本も動かない。人の合意を確かめる材料は `mr` のときと同じ 2 つで、
標準入力が端末であること（`CCNAVI_GUARD_TICKET_APPROVAL`）と、`--reviewed` を含むコマンドを
エージェントが打てないこと（`DENY_TICKET_APPROVAL_CLI`、§9.5）。`--chat` は**見る場所が `chat` と
宣言されたフェーズにしか当たらない**。当たると `mr` のレビューを安い経路で通せる。

`--chat` が見るのはもう 1 つ、**依頼が出ていないこと**。`request` を通したフェーズには
指摘が付いているかもしれず、それを数えずに開ける道はここに置かない（数えるのは `check`、
数えたうえで受け入れるのは `accept`。§9.10）。

実績のリスクが高い `chat` のフェーズでは、マージリクエストで見ることを勧める文を出すが、
止めはしない。選んだのが人であることは `reviewed` のマーカーに残す（`recommended: mr`）。
強制しない理由は ADR-0065。

告知は「マーカーが 1 つも無い」ときだけ出る。子を足してマーカーが消えた後、再び終わればもう一度出る。
最後の子を閉じる前に、種類の `deliverables` が親か子のワークツリーに在って追跡されているかを見る。
無ければ `done` を拒む。閉じてレビューが通ったら、案内が次のフェーズの計画を促す。

**止めている間の呼び名は 2 つある**（ADR-0053）。依頼を出すまでが **レビュー準備中**（次に動くのは
エージェント。合流・push・依頼）、出してからレビュー済みまでが **レビュー待ち**（次に動くのは人。
判定の `review_waiting`）。拒否の文面・`--explain`・親の局面・ボードのバッジはこの名前で言い、どちらの段かで
「やること」も変わる（未依頼なら request まで、依頼済みなら待って `check`）。`chat` のフェーズは
普段この段を持たないので、人が端末で先へ進めるまで「レビュー準備中」のまま。ただし `request` は
`chat` のフェーズでも通るので（§9.10）、通したときはそこから先は `mr` と同じに「レビュー待ち」へ移る。
JSON の欄名は
`gate_closed` のままで、判定とボードの契約としてだけ残っている（§10）。

止めるかどうかは cwd のワークツリーで親を引いて決める。止めている間、その親の cwd からの `Agent`（サブエージェントの
起動）と `Bash` / `PowerShell` を `DENY_PHASE_REVIEW` で止める。通すのは `Bash` で、連結の全部が
`sh|bash …ccnavi-(ticket|review|git).sh` の形で読み切れたときだけ。読み切れないコマンドに例外は
与えない。`PowerShell` に例外は無く、止めている間は必ず止まる。`Write` / `Edit` は通す。次のフェーズの計画はレビュー前に進めてよい。別の親は別に
止め、ワークスペースルート直下や無関係のツリーからの呼び出しは止めない。

`Stop` の終了コード 2 は「続行させる」道具で、「ターンを終えて人に聞け」を強制する手段は hook に
無い。先に文で言い、進んだら止める、という 2 段になる。`cwd` は `cd` 1 回で外れるが、外れた先で
起動したサブエージェントの書き込みは行き先で止まるので致命傷にならない。書き込みは行き先、
起動とシェルは `cwd`、と鍵を使い分ける。

### 9.9 実績で測るリスク

計画のときに「軽い」と思った作業が、やってみたら大きな変更になることがある。宣言だけを信じると、
それがレビューを通らずに進む。だからリスクは宣言ではなく実績で測る（ADR-0027）。子を
`ticket done` で閉じるとき、その子のワークツリーで `base_sha..HEAD` の差分を数えて点を付け、
`phases/<親>/<子>.risk.json` に残す。フェーズの点は子の最大値。HIGH 以上なら、宣言に関わらず
そのフェーズは人間レビューが要る扱いになる。実績が小さくても宣言のレビュー要は下げない。
上げるのは要否だけで、見る場所は指さない（§9.8）。宣言が `none` だったフェーズは `chat` に上がり、
マージリクエストを勧める文が出る。勧めるだけで、選ぶのは端末に座っている人（ADR-0065）。

配点は `risks.yml`（共通層と親の `project:` の層の和。§11.4.2）。どの層にも無ければ組み込みの 4 項目、壊れていれば
組み込みに落ちて `--lint` と閉じたときの出力が言う。等級の名前は `LOW` / `MEDIUM` / `HIGH` /
`CRITICAL` で固定、閾値（既定 20 / 40 / 70）だけ動かせる。閾値は「以上」。

| 系統 | 書き方 | 誰が測るか |
|---|---|---|
| 定量（組み込み） | `lines_over` / `files_over` / `deleted_over` / `glob`（当たるごとに加点。`max` で上限） | ccnavi が差分から数える |
| 定量（スクリプト） | `script: <.ccnavi/common/scripts/ の下>`（共通層。層ごとの解決先は §11.4.2） | ccnavi が `sh` で走らせる。cwd は子のワークツリー、`CCNAVI_BASE_SHA` / `CCNAVI_HEAD` / `CCNAVI_TICKET` / `CCNAVI_PARENT` を渡し、標準出力の整数か `{"points": N, "message": "…"}` を受け取る。失敗や読めない出力は重い側に倒し、その項目の点を加える |
| 定性（サブエージェント） | `judge: <問い>` | 判定が揃うまで子は閉じられない。`done` が問いと差分の要約を `state/risk-judge-<子>.md` に書き、親がサブエージェントに渡し、報告を `ccnavi-ticket.sh judge <子> <項目> yes\|no --reason` で記録する。判定は子の HEAD に結び、HEAD が動けば取り直し。記録できるのは親だけ |

点と等級と加点した理由は、閉じたときの出力、フェーズの終わりの文面、`--explain`、レビューの
依頼文の先頭に出る。

### 9.10 レビューの依頼と確認

ここは見る場所が `mr` のフェーズの話。`chat` のフェーズは普段この節のどれも使わない。依頼も
写しも無く、先へ進めるのは人が端末で打つ `ccnavi --reviewed <N> --chat` だけで、sh は
1 本も動かない（§9.8）。ただし `request` は `chat` のフェーズでも通る。**止めるのは緩める側だけ**で、
実績のリスクが高いときに勧めるのがこの向き（`chat` の宣言をマージリクエストで見る）だから。
逆に `--chat` は `mr` のフェーズに当たらない。

リモート（GitHub / GitLab）を読み書きするのは `.ccnavi/scripts/ccnavi-review.sh` で、実行ファイルは
ネットワークに出ない（P11、ADR-0028）。実行ファイルが持つのは作業ツリーの中で分かる前提検査と、
sh が渡す写し（`--result <JSON>`）の判定とマーカーの操作だけ。写しの形が sh と実行ファイルの契約で、
テストも同じ経路を通る。

| 写し | 形 |
|---|---|
| `check` / `accept` が受け取るもの | `{host: github\|gitlab, mr: {number, url}, threads: [{id, resolved, url, path, line, body, created_at}], reviews: [{state, url, submitted_at, author}], fetched_at}` |
| `request` の投稿の結果 | `{host, mr, url, created_at}` |

| sh の呼び方 | 実行ファイルの段 |
|---|---|
| `request --phase <N> --body-file <文>` | `review prepare`（前提を確かめ、本文とマージリクエストの下書きを控えの置き場に書き出す）→ sh がマージリクエストを（無ければ下書きで）作り、依頼を投稿 → `review requested`（HEAD が動いていないことを確かめ、`{head, mr, url, host, since}` をマーカーに置く） |
| `check --phase <N>` | sh がスレッドとレビューを取ってくる → `review check`（判定して `reviewed` のマーカーを置き、そのフェーズと引き受けた延期の分の `review/` の子を `done/` へ動かす） |
| `accept <N>` | 人が端末で打つ。sh が取ってくる → `--reviewed N --accept-unresolved`（残っているスレッドを見せ、`a` 受け入れて進む（`accepted.json` に控えてマーカーを置き、`review/` の子を `done/` へ）か、`f` 続きの子チケットを起こす（`review/` の子を `done/` へ動かし、同じフェーズの番号で `.ccnavi/approved/doing/` に直に置き、マーカーを消す）かを選ばせる）→ 受け入れた一覧か起こした子を sh がコメントに写す |
| `note --body-file <本文>` | sh が投稿する。実行ファイルは関わらない。レビューの状態は変えない |
| `handoff --body-file <題と本文>` | `review handoff`（フィードバック計画の承認後に、残ったスレッドの URL を添えた issue の下書きを書く）→ sh が issue を作り、マージリクエストに引き継ぎの note を残す |
| `ready` | `review ready`（親を閉じられる条件と、`wip/` が追跡から消えていて未コミットが無く push 済みであることを確かめ、`ready.json` と note の下書きを置く）→ sh が Draft を外し（GitLab は `squash` を立てる）、note を投稿する。親が閉じたあとに打つ。同じ親に 2 度打っても通る。マージは人 |
| `wrapup --reason <理由> [--no-issue]` | 人が端末で打つ。`review wrapup`（残りを見せて y/N。未着手の子（`doing/` で着手の欄が空）を取り消して `done/` へ、終わっていないフェーズに `skipped`、レビュー未了のフェーズに `reviewed`（その `review/` の子は `done/` へ）、未解決を `accepted.json` へ、`wrapup.json` を置く。変更要求のレビューが立っていれば拒む）→ sh が残りを issue に写し、note を投稿する |
| `fetch` / `origin` | 取ってきた写しを標準出力へ / origin をどう読んだか |

**`request` の前提**は 6 つ。フェーズが終わっている、その番号が延期でない、閉じた（`review/` か `done/`）子の
ブランチが全部親ブランチに取り込まれている（子はブランチ名で引く。引けなければ未充足）、親の
ワークツリーに未コミットの変更が無い（未追跡は数えない）、親ブランチの HEAD が push 済み、
まだ依頼していない（依頼の後に親の HEAD が動き、まだレビュー済みでなければ出し直せる。依頼文を
投稿し直し、マーカーの `head` と `since` を今のものに書き換える。レビュー済みのフェーズは、依頼の記録が
無くても（`wrapup` が置いた形）拒む）。1 つでも欠けたら全件を列挙して拒み、何もしない。マージリクエストの題・本文・
`Closes #<課題>` は親チケットの `title` / `rationale` / 本文 / `issue` から写す。

**`check`** は `requested` が無ければ拒む。依頼のマーカーにあるホストとマージリクエストの番号が承認済みチケットと
一致しなければ拒む。依頼時の HEAD と今の HEAD が同じで push 済みであることを求める。**ただし
ccnavi 自身の置き場（`CCNAVI_TICKETS_APPROVED` と `CCNAVI_TICKETS_PROPOSAL`）だけを変えたコミットは、
動いたとも未 push とも数えない。** チケットは 2 つの置き場を行き来するので、片方だけを外すと
`done` の移動（`doing/` → `review/`）が「人が見るものが動いた」に数えられる。 マーカーはそこに置かれ、親のブランチにコミットして運ぶもの（§9.2）で、数えると
「依頼 → マーカー → コミット」の順のせいで依頼の直後に必ず止まる。未コミットの側を
前提から外しているのと同じ基準で、人がレビューで見るものは変わらない。出し直し（`request`）の
「依頼済み」の判定と、`request` の push の前提（`_unmet`）も同じ基準で見る。**`ready` の前提
（`_merge_problems`）だけは旧来どおり**、置き場も含めて完全に push させる。人がマージのときに
見るのはリモートで、マーカーが他の機械へ渡るのもそこだから。
マーカーの `head` は、`^[0-9a-f]{7,64}$` でなければ差分の相手にしない。マーカーは他の機械から
届くファイルなので、`HEAD` のような「今」を指す値を revision として渡すと空差分になって素通りする。
記録が無い（空の）マーカーも同じで、照合できないものは「動いた」に倒して出し直させる。数えるのは
いま解決されていない指摘の全部で、付いた時刻では絞らない（ADR-0031）。除くのは機構自身の投稿
（`<!-- ccnavi: …`）と、人が `accept` で受け入れたものだけ。レビューの状態（変更要求）は
レビュアーごとの最新だけを見て、取り下げは無い扱い。

| 結果 | 動き |
|---|---|
| 変更要求のレビューが立っている | マーカーを置かない。`accept` でも通せない。解くのはレビュアーの approve / dismiss だけ |
| 未解決のスレッドがある | マーカーを置かず、一覧（URL・場所・要旨）を返す |
| どちらも無い | `reviewed` を置く。止まっていたのが解ける |

sh は GitLab と GitHub をリモート URL のホストで見分け（`github.com` なら GitHub、それ以外は
GitLab）、`gh` / `glab` がそのホストで認証されていれば（起動時に 1 度叩いて確かめる）それを、
そうでなければ `curl` と `GITLAB_TOKEN` / `GITHUB_TOKEN` を使う。
どちらも無ければ止まり、MCP などでリモートを読む形を提案する。結果の組み立てに `jq` が要る。
道具は起動時に絶対パスへ解いて固定する。origin はホストのポートと scheme をそのまま使い、
URL に埋めた資格情報は読み飛ばして出力では伏せる。実行ファイルの側も同じ規則で origin を読み、
`--lint` がホストに合うトークンの有無を言う。

push は親だけが行う。git のラッパースクリプトは子チケットのワークツリー（承認済みチケットに `parent:`）からの push を拒み、
hook はサブエージェントからの `ccnavi-git.sh push` を拒む。リモートに置く枝はマージリクエストの
付いた親ブランチ 1 本で、子の成果は親が手元で合流してから親のツリーで送る（ADR-0030）。

### 9.11 閉じる、締める、Draft を外す

親を閉じられるのは、`plan` と `feedback` の全フェーズが閉じ、`feedback` が承認済み（空でも）で、
止まっていないとき。フィードバック対応フェーズの最後のレビューで指摘が残ったとき、`check` は
2 つの道を言う。同じフェーズに子を足してやり直す（人が `accept` で続きの子を起こすか、エージェントが
提案して承認を受ける）か、残りを切り出して受け入れて閉じるか。
切り出す先は運び方で違う。マージリクエストがある親なら `handoff` で別の issue へ、どのフェーズも
マージリクエストに出していない親（全部 `chat`）なら、新しい親チケットの提案を `wip/proposals/todo/`
に書いて、そちらでワークツリーごと切り直す。ホストに何も無いので issue にはできない。

閉じたことは `phases/<親>/closed.json` に残る。どのフェーズをどこで見たか（`{"reviews": {"1": "chat"}}`）
を親のブランチに置く。提案は `wip/` ごと消してから統合先へ戻すので、`chat` で回した親は、
ここに置かないと締めた事実がどこにも残らない。

順は運び方で分かれる。マージリクエストがあるなら「親を `done` で閉じる → `wip/` を消してコミット →
push → `ready`」。Draft を外す道を `ready` の 1 本にしておくと、外れたマージリクエストは必ず
「片付いて push 済み」になる。取り込みは人が squash で行い、途中のコミットとチケットの置き場は
既定のブランチに残さない。全部 `chat` で回した親には Draft が無いので、「親を `done` で閉じる →
`wip/` を消してコミット → 統合先へ戻す」で終わる。ccnavi はどちらでもマージを行わず、
マージされたかも見ない。

まだ残っているが締めたいときは、人が端末で `wrapup --reason` を打つ。作業中の子がいる間は打てない。
取り消しも省略も受け入れも、それぞれの層に普段と同じ形で残る。`wrapup.json` があれば親は
フィードバック計画が無くても閉じられる。

### 9.12 サブエージェント

| イベント | すること |
|---|---|
| `SubagentStart` | cwd のワークツリーが承認済みチケットに結び付くときだけ、開いている子の一覧（識別子・ワークツリーの有無・範囲・未完了の先行・フェーズの種類・`agent` の案内）を `additionalContext` で渡す。親のツリーならその親の子、子のツリーならその子自身。ワークスペースルート直下とチケットの無いワークツリーからの起動には何も渡さない。止められないイベントなので判定はしない |
| `SubagentStop` | 対象の子の `base_sha..HEAD` のコミット済みの差分と未コミットの変更の両方から範囲外を探し、残っていれば `enable` で 1 回だけ終了コード 2 で差し戻す（`state/subagent-<id>.bounced`）。2 回目は通して文だけ渡す。未コミットが残っていること自体は差し戻さない |
| `PostToolUse`（`Agent`） | サブエージェントが差し戻しを無視して終わったことを親に言う。`SubagentStop` の出力は親に届かないため |

判定は行き先で決まるので、`SubagentStart` の一覧は案内でしかない。

---

## 10. 診断と機械可読な出力

この面だけは hook から呼ばれない。人が端末から叩き、VS Code 拡張が読む。判定は hook と同じ関数を
通る（REQ-DIA-03、ADR-0016）。記録は診断の経路では書かない。試験（`--test` / `--test-samples`）は
控えも内部で外す。拡張と `tools/check_rules.py` は念のため `--log ""` と `--state ""` も渡す。

| オプション | 出すもの | 終了コード |
|---|---|---|
| `--test <ツール> <対象> [--json]` | 判定・根拠コード・当たったルール（タイプ、書いた式、翻訳後の正規表現、`file` か `outside`）・行き着く先・縮退したか既定に落ちたか・当たった中で実行されるコマンド（`unwrapped`、§6.3.1）・返る文面。モードは常に `enable` | 常に 0 |
| `--test-samples <見本> [--json]` | 見本をすべて判定に掛け、期待と食い違ったものを名指し | 文字の出力は食い違いがあれば 1、JSON は常に 0、見本が読めなければ 1 |
| `--explain [--json]` | タイプごとのルール、プロジェクトの一覧とルールの可否、権限モードの扱い、チケット制御の値、承認済みチケットの一覧、親の局面、フェーズごとの状態・マーカー・止まっているか・リスク。判定は行わない | 0 |
| `--lint [--json]` | 判定を行わず、防御を無効化しうる記述を error と warn に分けて報告。ルール・フェーズの種類・配点・settings.json の登録・チケット・プロジェクトを見る | error があれば 1 |

**層の置き場を動かすフラグは 7 本あり、どれも診断でだけ効く**（ADR-0067）。共通層の中身は
`--rules` / `--phases` / `--risk`、**層を探す先**は `--projects`（プロジェクトの層の置き場）と
`--project-home`（ccnavi ディレクトリの名前）、1 つの層だけを差し替えるのは
`--project-rules-file <名前>=<パス>`（その名前のプロジェクトのルールの代わり）と
`--project-phases-file <名前>=<パス>`（その名前の層。`self` は自身の層のフェーズの種類の代わり）。
診断の外（hook からの判定、`ticket` / `review` の副命令）に渡すと落とし、落としたことを標準エラーに
出す。守る対象も本来の場所のまま。保存していない設定が実運用の判定と採点に効く道を作らないためで、
**層を探す先も同じ門に載せる**のは、外せばその層の配点も種類も消えて、中身を差し替えるのと結果が
同じになるから。層の配点にはまだ差し替えが無い。

JSON の形は README の「試験の JSON」「lint の JSON」「ボードの JSON」に定め、拡張の見本と Python の
テストの両方が同じ例を見る。拡張はこれを並べるだけで、提案もマーカーも自分で解釈せず、glob も regex も
自分で当てず、点も数えない（ADR-0035）。止まっているか・承認待ち・ワークツリーの有無は、判定と承認が
使う関数をそのまま呼んで載せる。人の判断のうち、承認は拡張の中で完結する（§9.4）。オーバーレイが
`--approve --preview --json` で一覧を見せ、人が押したら `--approve --yes` を子プロセスで打つ。
`accept` は端末のまま。sh がレビューのスレッドを取ってきてから判断する経路で、拡張に移すには
その結果を JSON で返す契約が別に要る。`wrapup` はボードに置かない。人がレビューを終えたことをエージェントに
伝える「レビュー済み連絡」は、承認の文と同じ経路（コピー / 新しいセッションで開く）で文を渡すだけで、
拡張はマーカーを置かず実行ファイルも起こさない。`check` を打ってマーカーを置くのは文を受けた親で、止めている間に通る
形（§9.8、単体の `sh …ccnavi-review.sh`）で打つ。P10（人の判断は端末で受ける）に触れないのは、ボタンが何も主張せず、
先へ進めるのは `check` がホストの写しから独立に確かめた結果だけだから。
依頼のマーカーが持つマージリクエストの URL はリンクとして出すが、その先の状態は見に行かない。

設定を画面で直す 3 つ（ルール設定・リスク管理・フェーズ管理）は、編集中の内容を一時ファイルに書いて
`--rules` / `--risk` / `--phases` で渡し、`--lint` が error を返さないときだけ保存する。判定も採点も
種類の照合も実行ファイルが出すので、拡張はどれも自分では行わない。

このうちリスク管理とフェーズ管理は、`CCNAVI_TICKET_CONTROL` が `disable` のワークスペースでは開かない。
配点は子チケットを閉じるときに、種類は親の計画と子の範囲にしか読まれないので、その間はどちらも何も
動かさない。効かない設定を直せる入口を残すと、直したのに効かない、という読み違いの元になる。入口は
すべて隠す。リスク管理はサイドパネルとコマンドパレットの 2 つ、フェーズ管理はそれに加えてプロジェクト
管理画面のボタン（各行と「ワークスペース自身」の枠）。隠したうえで、開く側でも見る（`when` を
通らない呼び出しがあるため）。ルール設定は `disable` でも当たるので隠さない。既に開いている画面は
追いかけない。

フェーズ管理の画面には、書式の側から来る制約が 2 つある。実行ファイルは PyYAML（YAML 1.1）で読み、
拡張の `yaml` は 1.2 なので、`yes` `on` `1:30` `0755` のような語は裸で書くと型が変わる。画面はその綴りを
引用符で囲んで書く。もう 1 つは、同じ識別子を 2 つ書くと `safe_load` が後ろで黙って上書きすることで、
そこは拡張が保存の前に止める。`--lint` は提案に残っている親の計画をその種類で照合するので、計画が
指す種類を消す保存はそこで止まる（承認済みチケットについては定義が読めるかまで。承認済みチケットの計画との
食い違いは次の承認と着手で実行ファイルが言う）。

`--lint` の登録の検査は `.claude/settings.json` しか見ない。検証を起動した環境を見るので、モードが
どこから来たかを名乗る。`--lint` は設定をまとめて見るので、`rules.yml` に error がある間は配点も
種類も保存できない。先に `rules.yml` を直す。

`--lint` は `.claude/settings.json` の env の `CCNAVI_BIN_PATH` も見る（`lint._bin_path`）。プロセスの環境ではなく設定ファイルを
読むのは、hook が起動するのはそこに書いた綴り（`"${CLAUDE_PROJECT_DIR}/${CCNAVI_BIN_PATH}"`）だから。

| 深刻度 | 何を言うか |
|---|---|
| error | 指す先が在るのに実行できない。hook は sh を直に起動するので 126 で起動せず、判定そのものが動いていない |

error は POSIX でだけ見る（`os.access(X_OK)`）。Windows は実行ビットを持たない。綴りは書いたとおりに見て、`.exe` を補わない。
hook が起動するのは書いた綴りで、補った先が実行できても hook は起動しない。指す先が無いときは言わない。組み立ての前や、
利用者ごとの設定で別の綴りを渡している形があり、無いことは自己防衛が missing と言う。

---

## 11. 複数のリポジトリ

道具（hook の登録、実行ファイル、保護済みスクリプト、スキル、CLAUDE.md）を持つ private の
リポジトリをワークスペースと呼び、その下の置き場に clone した別々のリポジトリをプロジェクトと呼ぶ。
Claude Code はワークスペースを開く。要求は requirements.md の REQ-MLT。

骨子は 4 つ。**道具はワークスペース、設定はプロジェクト、git はツリーごと**（ADR-0033）。**設定 3 本（ルール・フェーズの種類・リスクの配点）は
「共通層 + そのツリーの層」の和で判定する。足すだけ、上書き無し、厳しいほうが勝つ**。**パスを持つツールは共通層 + 行き先の 1 層、
パスを持たないツールは共通層 + 全部の層**（§11.4）。**チケットのプロジェクトは提案を置いた場所が決め、判定はその値を見ない**。

### 11.1 何を解くか

ルートを 1 つ、設定を 1 組だけ持つ作りでは、次の 5 つで行き詰まる。前の 3 つはルートとルールファイルの数の話、後ろの 2 つは設定 3 本の持ち方の話。

- ルートが 1 つ。相対パスの解決、実行後の監視、ワークツリーの列挙が、すべてワークスペースルート 1 か所を向く。
  ワークスペースが git 管理下にあればその git だけを見てプロジェクトの変更は見えず、無ければ `top_level` が上へ抜けるか止まる
- ルールファイルが 1 本。プロジェクトごとのルールを置く場所が無い
- プロジェクトに `.claude/` を置く形は採れない。Claude Code がそこのスキルを読み込み、`--lint` が迷い子として拾う
- ワークスペースのルールがプロジェクトのファイルに効かない。どのツリーでも効いてほしい deny（hook の守り、認証情報、main への直書き）を
  プロジェクトごとに書き足して育てることになり、書き足し忘れたプロジェクトが穴になる
- フェーズの種類とリスクの配点がワークスペースに 1 本しか無い。種類の `scope` はワークツリーのレイアウトに縛られるので、
  レイアウトの違う 2 つ目のプロジェクトで破綻する

もう 1 つ、**後ろの層が前の層を上書きできる形**も採らない。プロジェクトを編集しているエージェントは（守られていなければ）
そのプロジェクトの層を書けるので、上書きできる形は共通層の deny を消す道になる。和なら、書けたとしても足すことしかできない。

### 11.2 置き場

| 何 | 場所 | git |
|---|---|---|
| hook の登録、実行ファイル、保護済みスクリプト、スキル、CLAUDE.md | ワークスペースルート | ワークスペース |
| **共通層**のルール / フェーズの種類 / リスクの配点 | `.ccnavi/common/{rules,phases,risks}.yml`（置き場は固定） | ワークスペース |
| ルールの見本 | `.ccnavi/common/rule-samples.yml` | ワークスペース |
| **ワークスペース自身の層**の 3 本 | `<ワークスペースルート>/.ccnavi/config/{rules,phases,risks}.yml` | ワークスペース |
| **プロジェクトの層**の 3 本 | `projects/<名前>/.ccnavi/config/{rules,phases,risks}.yml` | プロジェクト |
| 固有スクリプト（配点の `script:` が指す先） | 共通層は `.ccnavi/common/scripts/`。自身の層とプロジェクトの層は、それぞれの `.ccnavi/scripts/` | 層と同じ |
| プロジェクト | `projects/<名前>/`（`CCNAVI_PROJECTS`、既定 `projects`、ワークスペースルートからの相対）。直下で `.git` を持つディレクトリだけ。1 段に固定 | ワークスペースでは無視。プロジェクト自身の git |
| ワークツリー | `.claude/worktrees/<識別子>/`。ワークスペースかプロジェクトから切る | 管理外 |
| 提案 | そのツリーの `wip/proposals/<状態>/<識別子>.md`。プロジェクト向けはそのプロジェクトの git が持つ | そのツリーのリポジトリ |
| 承認済みチケット（閉じたものを含む）、フェーズのマーカー | 親チケットのツリーの `.ccnavi/approved/`（§9.2）。承認済みチケットに `project:` が入る | そのツリーのリポジトリ |
| 記録、控え、セッションの状態 | ワークスペースの `logs/`（`logs/log.jsonl`、`logs/state/`、`logs/session/`） | 管理外 |
| git のラッパースクリプトの記録 | ワークスペースの `logs/<プロジェクト>/`。ワークスペース自身は `logs/` | 管理外 |

`.ccnavi/` は ccnavi ディレクトリの既定の綴りで、`CCNAVI_PROJECT_HOME` の既定値。以下この章で `.ccnavi/` と書くのは既定のままの綴りを指す。

**層は 3 種で、形は同じ。** 共通層は「どのツリーにも効くもの」を置く場所で、ワークスペースルートの `.ccnavi/common/` に置く。
人が持つ設定は ccnavi ディレクトリの下、実行のたびに書かれる記録と控えは `logs/` に置き、
`.claude/` には Claude Code 自身のもの（settings.json・hooks・skills・worktrees）だけを置く（ADR-0042）。
共通層は ccnavi ディレクトリの下にあるので、ccnavi ディレクトリの名前を動かしていなければ、共通層の 3 本と見本も
名指しのツールから組み込みの deny（`*/.ccnavi/*`）が止める。共通層の置き場は ccnavi ディレクトリの名前（`CCNAVI_PROJECT_HOME`）に付いて動かず、
env でも動かない（ADR-0052）。別の場所を指せるのは `--rules` / `--phases` / `--risk` のフラグだけで、それも診断（`--lint` / `--test` / `--test-samples` / `--explain`）に限る（ADR-0067）。層を探す先を動かす `--projects` / `--project-home` も同じ門に載る。自身の層とプロジェクトの層は
「そのツリーにだけ効くもの」を置く場所で、どちらも git プロジェクトルートの下の `.ccnavi/config/` に同じ形で置く。ワークスペース自身に層を
分けるのは、ワークスペースのフェーズの種類（`scope: ["ccnavi/*", ...]`）がワークスペースのレイアウトにしか合わないから。共通層に置くと
全プロジェクトに効いて、2 つ目のプロジェクトで破綻する。

**共通層のフェーズの種類は空から始める。** ワークスペースの種類（research / design / acceptance / implement / docs / design-feedback /
implement-feedback など）は、上の理由で自身の層 `<ワークスペースルート>/.ccnavi/config/phases.yml` に置く。
ルールと配点は汎用なので共通層に置く。導入スクリプトのひな形も同じ分け方で配る（§11.9）。
自身の層の `.ccnavi/config/` はルールと同じく人の持ち物で、エージェントの Write / Edit は通らない（チケットが `allow` に
`.ccnavi/*` を書いていても、組み込みの deny に負ける。それでよい）。

**環境変数は `CCNAVI_PROJECT_HOME` の 1 本。** 既定 `.ccnavi`、git プロジェクトルートからの相対。自身の層とプロジェクトの層の両方に同じ値が効く。
`config/` と `scripts/` と 3 本のファイル名は固定で、動かせるのは ccnavi ディレクトリの名前だけ。

**`config/` ではなく `.ccnavi/` の下に置く理由。** `config/` はプロジェクトが既に持っていることが多い名前で、そこに ccnavi のファイルを混ぜると、
組み込みで守る綴りをプロジェクトの持ち物と分けられない。`.ccnavi/` なら 3 本とスクリプトを 1 つのディレクトリの下に置け、組み込みの deny が
`*/.ccnavi/*` の 1 行で済む。採らなかった形は 2 つ。プロジェクトの `.claude/`（Claude Code がそこのスキルを読み、`--lint` が迷い子として拾う。
§11.1 のまま）と、3 本をそれぞれ別の環境変数で動かす形（動かす理由が無く、つまみが増えるだけ）。

**読むのは常に元リポジトリに checkout されている版。** ブランチは問わない。ワークツリーの中の `.ccnavi/` は読まない（REQ-MLT-04）。
ワークスペース自身の層も同じで、読むのはワークスペースルートの `.ccnavi/config/` であり、ワークスペースから切ったワークツリー側の設定は読まない。

**層のファイルが無い = その層は空。** 3 本は独立に無くてよい。壊れている = その層は空 + 記録の `fallback` に層の名前 + `--lint` の error。
組み込みの既定へは落とさない。共通層が有るのに組み込みに落とすと、共通層の deny が消える側に倒れるから。

共通層のルール（`.ccnavi/common/rules.yml`）自身が読めないときは今のまま、組み込みの既定に落ちる（REQ-PRE-06）。そのとき自身の層とプロジェクトの層は足さない。
「共通層が壊れている = 設定が壊れている」の扱いを変えない。壊れた共通層の上に層を足しても、何が効いているかを人が読めない。

**プロジェクトのリポジトリに書くのは、ccnavi ディレクトリと、人が合意したもの。** ccnavi ディレクトリ（設定 3 本と配点のスクリプト）に加えて、
提案と承認済みチケットとフェーズのマーカーがプロジェクトの git に入る。提案を public のリポジトリに出すとワークスペースの運用の痕跡が残るが、
A が承認して B の機械で作業する流れでは、その痕跡こそが共有したいもの（§9.2）。痕跡は feature ブランチに乗り、
マージリクエストの squash merge で 1 コミットに畳まれる。

git のラッパースクリプトの記録（`logs/`）はワークスペース側に残す。こちらは人の判断ではなく、その機械で何を打ったかでしかない。

プロジェクトが無いとき（`projects/` が無いか空）は、共通層と自身の層だけで判定し、提案・承認済みチケット・フェーズのマーカー・記録は
ワークスペースの置き場だけに書く（REQ-MLT-15）。

### 11.3 ツリー

ツリーは 3 種（ワークスペースルート、プロジェクト `projects/<名前>`、ワークツリー `.claude/worktrees/<識別子>`）。
所属は実パスの前方一致で最長のもの。ワークスペースルートの外は判定を持たない（`tree_of` は外に行き先があれば `None`）。
`Tree` は `project` を持ち、ワークスペースなら空、プロジェクトならその名前、ワークツリーなら元リポジトリがプロジェクトならその名前になる。

ワークツリーの確かめ方は §9.5 の相互参照のまま。ただし `.git` ファイルの `gitdir:` が指す先は、ワークスペースではなく
プロジェクトの `.git/worktrees/<識別子>` になる。ワークツリーの元リポジトリは、`gitdir:` の指す `.git` の親で分かる。
ワークスペースから切ったワークツリー（ワークスペース自身の作業）も数える。

候補が 3 種に増えても最長一致なので、`.claude/worktrees/x/src/a` はワークスペースルートに畳まれない（§9.5）。
列挙はファイルシステムだけで行い、git を起こさない。

### 11.4 ルール

**パスを持つツール（Read / Grep / Glob / Write / Edit / NotebookEdit）は共通層 + 行き先の層の和。** 行き先は解いた先のパスで、
Grep と Glob が探す場所を省いたときは `cwd`。行き先のツリーがプロジェクトに属するなら、その git プロジェクト
ルートの `.ccnavi/config/rules.yml`。ワークスペースのツリー（ワークスペースルートと、そこから切ったワークツリー）なら
`<ワークスペースルート>/.ccnavi/config/rules.yml`。どちらも共通層の上に足す。ルールは今までどおり解いた先の絶対パスに当てるので、
書き方は変わらない。プロジェクトのルールに `*/schema/*` と書けば、元リポジトリでも、そこから切ったワークツリーでも当たる。

ワークツリーの中に checkout されている層のファイルは読まない。読むのは元リポジトリの版。ブランチで変えたルールが
そのブランチの判定に効く形は、人が見ていないルールが効く形になる。

**パスを持たないツール（Bash / PowerShell / WebFetch / Skill / Agent）は全部の和。** 共通層、自身の層、全プロジェクトの層をタイプごとに
連結して当てる。`deny` `ask` `allow` の順は変わらない。どのプロジェクトの呼び出しかは payload から決められないので決めない。Bash は
`cd` を挟めば `cwd` の外に届き、WebFetch・Skill・Agent はそもそも行き先のパスを持たない。どのプロジェクトに `cwd` があっても同じ判定になる
（REQ-MLT-05）。

**順は 共通層 → 自身の層 → プロジェクトの層（名前順）。** 同じタイプの中で層の順に並ぶ。先に当たったルールが文面に載るだけで、判定は同じ。

**識別子には層の名前を添える。** 共通層は裸の `id`、自身の層は `self:id`、プロジェクトは `<名前>:id`（REQ-MLT-07）。記録の `rules` と
文面の `source` はこの形で出す。読んだ人が id だけで直しに行くファイルが分かる。

**裸の `id` にコロンは書けない。** 3 本とも、書いた `id`（`phases` の鍵、配点の項目名を含む）にコロンがあれば error にして読み込みから落とす。
共通層に `id: "lib:custom"` と書けると、プロジェクト `lib` の定義に見え、記録を読んだ人がどのファイルを直すのか決められない。
接頭辞は層を足す側だけが付ける。

**層の名札に 2 つの名前を予約する。** `common` と `self`。この名前のプロジェクト（`projects/common/`、`projects/self/`）は層として数えず、
`--lint` が error で名指しし、`--approve` はその `project:` を承認しない。比べるのは `casefold` した綴りで、`projects/Self/` も同じに扱う。
機械が綴りを区別するかどうかとは別の話で、`Self:schema` という id を記録で見た人は `self:schema` と取り違える。

**予約名のプロジェクトへの書き込みは共通層だけで判定する。** 数えないことを「行き先の層が無い」として返し、ワークスペース自身の層へは落とさない。
名前で層を引くと、`self` を名乗るプロジェクトへの Write がそのプロジェクトの deny を一度も読まずにワークスペースの層のルールで判定され、
ワークスペースの層に広い `allow` があればそれで通る（実測で確認した穴）。予約の判断は 1 か所に置き、層を数える側と行き先の層を引く側の
両方が同じ答えを引く。片側にしか予約が掛かっていないと、数えない名前で別の層の判定を引ける。

**重複は後ろを捨てる。** 裸の `id` が同じで、`{root}` を置き換えたあとの全欄（`match` / `glob` / `regex` / `message` /
`additionalContext*` / `additionalContext*File` / タイプ）が一致する定義は、同じルールの重複とみなし、後ろの層のものを捨てる。`--lint` が
info で言い、`--explain` は残った 1 本だけ出す。全部の和でも同じ。見本をそのまま持ち込んで始めたプロジェクトが共通層と同じ行を持つのは普通の形で、
それを衝突と呼ぶと本当の衝突が埋もれる。

**同 `id` で中身が違うとき、ルールは両方効く。** `--lint` が warn で言う。ルールは足すだけの面なので、`deny` と `ask` は増える側に倒れる。
`allow` は広がる側で、パスを持つツールでは行き先の 1 層にしか足さないので、広がる範囲はそのツリーの中に閉じる。パスを持たないツールでは、
ある層の `allow` が他のプロジェクトの作業にも効く。フェーズの種類と配点は
両方効かせられないので扱いが違う（§11.4.1、§11.4.2）。

**`glob` も `regex` も、どの機械でも大文字小文字を区別しない。** 当たり方が機械で変わると、同じルールファイルがある機械では
止め、別の機械では通す。`.ccnavi/` は「無くてよい層」なので、区別すると `.Ccnavi/` で作れば `*/.ccnavi/*` に当たらない。チケットの範囲、
フェーズの種類とリスクの配点の `scope`、組み込みの守り（§8.2）の綴りも同じ規則で当てる。`regex` だけ区別していた頃は、同じ場所への
書き込みが経路で割れていた（シェルは組み込みの守りが綴りの違いを無視して止め、`Write` は `regex` のルールが綴り違いで外れる）。
区別が要る `regex` は `(?-i:...)` で囲む。囲むのは区別したい部分だけでよく、否定の文字クラス（`[^c]` のような除外）を持つ
ルールは、区別しないと除外の側が広がるのでそこを囲む（ADR-0051）。コマンドの名前も区別しない。区別しない機械では `CAT` は
`cat` そのものなので、同じ綴りには同じ判定を返す。
見本（`--test-samples`）で判定が変わらないことを確かめる。

**Grep と Glob は、探し始める場所で層を選び、ルールもその場所のパスに当てる。** 探す範囲の中身は見ない。ワークスペースルートや
`projects/` から探すと、中のプロジェクトのファイルまで探すが、足す層は探し始める場所のツリーの 1 つで、`*/secrets*` のようなルールは
探し始める場所のパスに `secrets` が無ければ当たらない。どの層を足しても当たらないので、層の選び方では塞がない。層の選び方で差が出るのは、
パスによらず当たる広いルール（`match: Grep`・`glob: "*"` など）だけ（ADR-0048）。

**読めない層は空として扱う。** そのツリーを行き先にするパスを持つツールは共通層だけで判定し、全部の和からもその層を外す。外したことは記録の `fallback` に
層の名前付きで残し、`--lint` が error で言う。組み込みの既定へは落とさない（§11.2）。

**代償。** 3 つ。プロジェクトは共通層の `ask` を `allow` に緩められない（和は足すだけで、`ask` は `allow` より先に当たる）。
共通層の `allow` は全プロジェクトに効く。パスを持つツールに他プロジェクトのルールは足さないので「A のルールを B にも」は書けない。要るなら共通層へ上げる。

読み込みは層の数だけ増える。1 本 200 行の YAML で数 ms なので、実行前の判定の期限の中に収まるが、`ms` の欄で見る（§11.10）。

#### 11.4.1 フェーズの種類の合成

**どの層を足すかは親の承認済みチケットの `project:` で決まる。** 値は提案を置いた場所が決め（§11.5）、親と子は同じ置き場に並ぶので
子も同じ値を持つ。空ならワークスペース自身の層。ワークツリーの元リポジトリは `project:` と合うことを着手時に確かめてある（REQ-MLT-13）ので、
種類を選ぶのにこの値を使っても、判定が申告に依存する形にはならない。

**`id` ごとに合成する。** 共通層の種類と、その層の種類を 1 つの集合に並べる。同 `id` で中身が違えば error。表示名（`title`）の重なりも
層をまたいで error（今の 1 本の中の規則を層をまたいで適用する。人は表示名で見るので、承認画面で見分けられない）。同 `id` で全欄が一致するものは
ルールと同じく重複とみなして後ろを捨て、info で言う。

**`overlap` / `requires` が指す先は合成後の集合で確かめる。** 層 1 本を読むときは参照を確かめず、合成したあとで、その層が新しく足した種類の
参照だけを当てる。層から共通層の種類を指すのは正しい形なので、1 本だけで確かめると必ず落ちる。共通層の側を再び当てないのは、そこは共通層を
読んだ時点で確かめてあるから。

**衝突した層は空として扱う。** error があるとき、判定はその層を空にし（共通層の種類だけで進む）、`--lint` が error で言う。
共通層だけで進むと、親の `plan:` がその層の種類を指している場合は「種類が無い」で承認が止まる。止まる側に倒れる。衝突した片方を黙って採ると、
どちらの `review:` が効いているかを人が読めない。記録の `fallback` に層の名前を残すのは配点の側だけ（§11.4.2）。種類の衝突は承認が
止まる形で表に出るので、記録には足していない。

採らなかった形は 2 つ。共通層の定義を採る形（衝突を黙って解いてしまい、lint を見ない人には上書きと同じに見える）と、
合成全体を捨てて番号だけの挙動に落とす形（共通層まで巻き添えになる）。

**`id` は裸のまま鍵にする。** `plan:` と `feedback:` とマーカーは裸の `id` で種類を指す。層の名前は `--explain` と記録の層の欄に出す。
ルールと違って `self:design` のような接頭辞を id に付けないのは、id が合成の鍵であり、人が `plan:` に書く名前だから。接頭辞を付けると
同 `id` の衝突が定義上起きなくなり、「同じ名前で違う中身」を error にできない。

**`scope` はワークツリーのルートからの相対のまま。** 種類が効くのは、その層のプロジェクトから切ったワークツリーだけなので、相対の基準は今と同じ。
`inherit` も同じ。`{root}` はここでは使わない。

#### 11.4.2 リスクの配点の合成

**どの層を足すかはフェーズの種類と同じ。** 子を閉じるときに、親の承認済みチケットの `project:` の層を共通層に足す。

**項目は連結、閾値はキーごとに小さいほう。** 共通層の項目の後ろに層の項目を並べる。同 `id` は error（今の 1 本の中の「重複している」を
層をまたいで適用）。全欄一致なら重複として後ろを捨てる。`levels` は `medium` / `high` / `critical` のそれぞれで、**書かれた鍵だけが参加し**、
そのうち小さいほうを採る。どの層も書いていない鍵は既定（20 / 40 / 70）。読み込みが既定で埋めた値を参加させると、`levels` を書いていない
プロジェクトが共通層の緩めた閾値を黙って戻すことになる。合成後に `medium <= high <= critical` でなければ error。各層が単独で順を守っていれば
キーごとの min でも順は崩れないが、確かめるのは安い。

**衝突した層は空として扱う。** 種類と同じ。`fallback` に層の名前を残し、閉じるときの出力が言う。共通層だけで測るので、点は小さくなる方向に
倒れうる。ここは「壊れたスクリプトがリスクを消す」と同じ穴なので、`--lint` を error にして、直すまで目に付く形にする。

**`script:` の解決先は層ごとに違い、たがいに指せない。**

| 層 | `script:` に書ける綴り | 解決の基準 |
|---|---|---|
| 共通層 | `.ccnavi/common/scripts/...` | ワークスペースルート |
| 自身の層 | `.ccnavi/scripts/...`（`CCNAVI_PROJECT_HOME` に従う） | ワークスペースルート |
| プロジェクトの層 | `.ccnavi/scripts/...`（同上） | そのプロジェクトの git プロジェクトルート |

共通層から `.ccnavi/scripts/` を、層から `.ccnavi/common/scripts/` を指す定義は読み込みで error。指す先が git プロジェクトルートに無ければ
`--lint` が error（走らせるときは今どおり「測れなかった」でその項目の点を加える）。ワークツリーの中のスクリプトは読まない。`cwd` は今どおり
子のワークツリーで、環境変数（`CCNAVI_BASE_SHA` 等）も同じ。

たがいに指せないのは、プロジェクトのリポジトリに入る定義がワークスペースの道具に依存する形を作らないため。プロジェクトの `.ccnavi/` は
そのプロジェクトだけで閉じ、ワークスペースの側は共通層だけで閉じる。

### 11.5 チケット

チケットのプロジェクトは、提案を置いた場所が決める（§9.2、§9.3）。

- **置き場が決める。** プロジェクトの `wip/proposals/` ならその名前、ワークツリーの中ならその元リポジトリ、ワークスペースの `wip/proposals/` なら空。
  走査がそれを入れ、承認済みチケットに残す。frontmatter の `project:` は宣言ではなく照合のためで、書かなくてよく、置き場と違えば
  承認しない。親と子は同じ置き場に並ぶので、承認が子に継がせる段は無い。承認画面がプロジェクトと提案の道を出し、人は書き込みが
  向かうリポジトリを見て承認する。ワークスペースの `wip/<名前>/proposals/` は、名前が `projects/` に在っても無くても走査されないので、
  そこに提案を置いても画面にもボードにも出ない（`--lint` が名指しし、名前が在るなら正しい置き場を案内する）
- **判定は申告を見ない。** 書き込みは行き先のツリーで判定され、そのツリーに結び付く承認済みチケットの `project:` と
  ツリーの `project` が違えば止める（`DENY_TICKET_PROJECT_MISMATCH`）。エージェントがプロジェクトを思い違えても、
  違うプロジェクトに書いた時点でそこのルートで判定されて止まる。検知の正しさにガードが依存しない
- **ワークツリーはプロジェクトから切る。** `.claude/worktrees/<識別子>` を `projects/<名前>` から切ったワークツリーとして作る。
  切るのは親が git のラッパースクリプトの `worktree add` で行い、プロジェクトの中で打つ（ラッパースクリプトは `cwd` の git を使う）。
  `ticket start` は、そのワークツリーの元リポジトリが承認済みチケットの `project:` と合うことを確かめてから動かす

### 11.6 守るもの

コアファイル（§8。ルールファイルの外で、組み込みで、控えと復元まで持つ対象）は次のとおり。

| 対象 | 場所 |
|---|---|
| hook の登録 | `.claude/settings.json`、`.claude/settings.local.json` |
| 共通層の 3 本 | `<root>/.ccnavi/common/{rules,phases,risks}.yml` |
| 自身の層の 3 本 | `<ワークスペースルート>/.ccnavi/config/{rules,phases,risks}.yml` |
| プロジェクトの層の 3 本 | `projects/<名前>/.ccnavi/config/{rules,phases,risks}.yml` |
| 実行ファイル | `CCNAVI_BIN_PATH`（振り分けの sh）と、sh が起動する実体（§8.2） |
| ワークツリー側の設定 | 上の各ファイルのうち追跡されているものについて、**その元リポジトリから切った**ワークツリーが持つ同じファイル |

ファイル単位で、無いものは対象から外れる（REQ-SLF-03）。Write / Edit の拒否、シェルからの書き込みの拒否、控えと復元の 3 つとも、
今 `rules.yml` に掛けているものをそのまま掛ける。守る場所は層の数だけ増えるが、守る対象の種類は「設定 3 本」の 1 種のまま。
ルールは何を止めるかを、フェーズの種類はレビューが要るかを、配点は点が幾つかを決める。3 本とも判定の中身そのもので、ここが書けると、
エージェントが自分のルール・レビューの要否・リスクの配点を緩められ、それが次の呼び出しから効く。

**予約名のプロジェクトの 3 本も守る。** 判定の層としては数えない（§11.4）が、ファイルは控えて戻す。`--lint` が名前を変えるよう言っている
あいだも、そこに置いてある 3 本は ccnavi の設定ファイルで、書き換えられたら戻すほうが筋が通る。判定に効かないものを守るだけなので、
緩む側には倒れない。控えの名前は層の**種別**で決め、名札の綴りでは決めない。名札で比べると `projects/common/` の 3 本が共通層と同じ名前になり、
重複の排除でその層の 3 本が控えと復元の対象から丸ごと落ちる（実測で確認した穴）。プロジェクトが名前を 1 つ選ぶだけで守りが外れる形にしない。

**共通層のフェーズの種類と配点もコアに入れる。** この 2 本は「人の持ち物」で、ルールの 1 行で止めるだけでは足りない。
ルールで止める形は、ルールの並びと書き方で緩みうるし、ワークツリー側の設定は統合で統合先へ入る道を持つ。
コアへ入れて、シェルからの書き込みは組み込みで止め、書けても戻す。名指しのツールから共通層の 3 本を守るぶんも組み込み
（`builtin-guard-common-layer`）で持つ。ルールの 1 行に任せると、その 1 行は守られるファイルの中にあるので、書き換えたルールファイルの
もとでは通ってしまう。組み込みで名指しのツールを止めるのは、置き場が設定で動く実行ファイル・ccnavi ディレクトリ・共通層の 3 つ。
hook の登録（`.claude/settings*.json`）はルールの `deny` に任せる。

**プロジェクトから切ったワークツリー側の設定も足す。** ワークツリーは元リポジトリを添えて列挙し、ワークツリー側の設定の相対を元リポジトリから
組む（REQ-MLT-08）。元リポジトリを添えずに列挙すると、プロジェクトから切ったワークツリーが列挙されない。相対を「ワークスペースルートから」組むと
（`projects/lib/...`）、ワークツリーの中にその綴りが無い。

**`.ccnavi/scripts/` はコアに入れない。** スクリプトは配点から `script:` で参照されて初めて効き、参照が無ければ何もしない。配点がコアで
守られていれば、新しい参照を足すことはできない。残る道は「既に参照されているスクリプトの中身を書き換える」で、これは次の組み込み deny と
`CCNAVI_RESTORE_IF_DENY` で止めて戻す。コアの控えと復元まで持たせると、ファイル数が定まらないものを呼び出しのたびに読んで比べることになり、
実行前の判定に張った期限に効く。数が定まっているのは設定 3 本のほうで、そこがコアとコアでないものの境になる。

**ccnavi ディレクトリの全体に組み込みの deny を掛ける。** `match: Write|Edit|NotebookEdit`、`glob: */.ccnavi/*` の 1 本を `deny` の先頭に足す
（`builtin-guard-project-home`。綴りは `CCNAVI_PROJECT_HOME` の値で組む）。シェルからの書き込みも同じ 1 本
（`builtin-guard-setting-files`）で止める。ルールファイルの外に置くのは、置き場が設定で動き、そのプロジェクトのルール自身に任せると
書けた瞬間に緩められるから。名指しする先を 3 本に絞らないのは、絞ると配点が呼ぶスクリプトが外れるから。

**シェルの綴りは「区切りが続くか、そこで終わる」形で書く。** `\.ccnavi(?:[\\/ \x00\x01]|$)` の形にして、`rm -rf .ccnavi` と
`mv .ccnavi .ccnavi.bak` も止める。区切りが続く形（`\.ccnavi[\\/]`）だけで閉じていると、ccnavi ディレクトリごと消す・退かす綴りが素通りする。
ccnavi ディレクトリごと消えれば、下のファイルを 1 本ずつ書き換えるのと同じだけ守りが消える。語の中の目印 `\x01` も終わりに数える。目印が 1 つだった頃は
`rm ".ccnavi x"` がここで止まっていたので、数えないと目印を分けただけで通るようになる。指す名前は `.ccnavi x` という別名なので
誤検知だが、目印を分ける変更のついでに判定を緩めることはしない。`.claude` の側も同じ形に揃える。ただし `.claude` の下は
名前を絞ってあるので（`worktrees/` は守る対象ではない）、そこで終わる形は空白・コマンドの切れ目・語の中の目印だけで閉じる。区切りを含めると
`.claude/` に続く綴り全部が入り、ワークツリーの片付けまで止まる。場所の綴りもコマンドの名前（`rm` / `cp`）も、どの機械でも大文字小文字を区別せずに当てる（ADR-0051）。
そこを決めるのは機械のファイルシステムではなくシェルで、`RM` が通る保証は無い。

deny なので、実行後の監視が保護領域に数え、`CCNAVI_RESTORE_IF_DENY` が git の変更一覧に出た差分を戻す。これで `.ccnavi/scripts/` の
追跡済みファイルへの書き換えは、Write / Edit とシェルの両方で止まり、抜けても戻る。

**ワークツリーの中の `.ccnavi/` の新規ファイルは拾わない。** ワークツリーの `.ccnavi/scripts/` に足した新しいファイルは、元リポジトリの
配点から参照されていないので効かない。効くのは、そのブランチが統合され、元リポジトリに checkout された後で、そこは人のレビューを通る。
`--lint` が「ワークツリーの `.ccnavi/` に元リポジトリに無いファイルがある」を warn で言い、統合の前に気づける形にする。

**前提。** ワークスペースの利用者は、プロジェクトに checkout されている版を信頼している。ccnavi が守るのは
「checkout されている版が、セッションの中で書き換えられない」ことまでで、そこへ入る前の経路（ブランチ、マージリクエスト、レビュー）は人の運用に置く。

### 11.7 実行後の監視

`git status` をツリーごとに起こす。見るのはワークスペースルートと、そのターンで書き込みのあったプロジェクトとワークツリー。
触ったツリーはセッションの控え（`session/<id>.trees`）が既に持っているので、それを使う。基準（ターンの始まり）と
「もう伝えた」の控えはツリーごとに分ける。差し戻しはそのツリーの git で行う。報告はツリーの名前を頭に付ける。

元リポジトリで見た変更に当てるのは共通層 + そのツリーの層のルールの和、ワークツリーならそのツリーに結び付くチケットの範囲と、
共通層 + 元リポジトリの層。Bash の和はここでは使わない。実行後に見るのはファイルの変更で、行き先が分かっている。

### 11.8 保護済みスクリプトと案内

sh はワークスペースにしかない。プロジェクトの中に `cwd` があるとき、`sh .ccnavi/scripts/ccnavi-git.sh` は届かない。
Bash ツールの環境に `CLAUDE_PROJECT_DIR` は来ない（実測済み。hook の環境にだけ来る）。だから拒否の文面が案内する
綴りは、ルールファイルの `message` に `{root}` で書く。`{root}` はモデルへ渡すときにワークスペースルートの絶対パスへ
置き換わる（書いた綴りは `--explain` に残る。§4）。自身の層とプロジェクトの層のルールでも同じで、置き換わる先はそのプロジェクトではなく
ワークスペースルート。sh はそこにしか無く、拒否の文面が案内する綴りはそこを指すから。重複の判定（§11.4）も置き換えた後の欄で比べる。
共通層とプロジェクトの層に同じ `{root}/...` を書いた定義は、置き換え先が同じなので重複として一致する。書いた綴りで比べると、
同じ場所を指す 2 本を別物として両方効かせることになる。

フェーズの種類と配点の欄にはパスの綴りを書く場所が `script:` しか無く、`script:` は §11.4.2 の解決先で決まるので `{root}` は使わない。
文面の欄（`message` / `when`）に `{root}` を書いた場合の置き換え先もワークスペースルート。ただし種類と配点の読み込みは `{root}` を
置き換えていない（書いても素通し）。案内の綴りが要るのは拒否の文面で、それはルールの側にある。

git のラッパースクリプトの記録はワークスペースの `logs/<プロジェクト>/` に書く。ラッパースクリプトは `cwd` の git で動くので、プロジェクトの作業はプロジェクトに入ってから始める。
これは CLAUDE.md の運用に書く。判定はそれに依存しないが、レビューの依頼と確認が `origin` を `cwd` から引くので、
そちらはこの運用が前提になる。

### 11.9 診断と記録

**`--explain` は層ごとに全件。** ルールは `id / match / glob（か regex）` を共通層・自身の層・各プロジェクトの順に、タイプごとに並べる。
重複で捨てた定義は出さない。フェーズの種類と配点は `id / 出どころの層 / 主な欄` の表を足す。

```
■ rules 共通層（.ccnavi/common/rules.yml、deny 12 / ask 3 / allow 4）
  deny  guard-hooks          Write|Edit|NotebookEdit  */.claude/hooks/*
  ...
■ rules 自身の層（.ccnavi/config/rules.yml、deny 0 / ask 0 / allow 1）
  allow self:worktrees       Write|Edit               */.claude/worktrees/*
■ rules lib（projects/lib/.ccnavi/config/rules.yml、deny 2 / ask 0 / allow 1）
  deny  lib:schema           Write|Edit               */schema/*
■ phases（共通層 0 種、自身の層 7 種、lib 3 種）
  id            層     kind  title         review  scope
  design        self   work  設計          mr      wip/design/*, docs/*
  build         lib    work  ビルド        mr      src/*
■ risk（levels: medium 20 / high 40 / critical 70）
  id            層     当て方            points  message
  big-diff      共通   lines_over 300    25      行数が多い
  schema        lib    glob db/schema/*  30      スキーマに触った
```

層ごとの表は、判定と同じ順・同じ重ね方で読んだ結果を並べる。読み方を 2 つ持つと、診断で見えるものと判定に入るものがずれる。
読めない層はその位置に「読めない: <理由>」と、空として扱っていることを出す。置いていない層は「置いていない（無い = 空）」と出す。
`levels` の行に出るのは共通層の側の値。層の `levels` はキーごとに小さいほうが勝つので、実際に効く値はチケットの層で決まる。

**記録に出どころの層を 1 欄足す。** 記録の 1 行は `tree` と `project`（この呼び出しがどのツリーのものと判定されたか）に加えて、
層の名前を持つ。値は `common | self | <名前>`。層の欄に入るのは、判定を下したルール（当たったものの先頭）の層。
ルールが当たらなかった行は空。配点の記録（`phases/<親>/<子>.risk.json`）は加点した項目ごとに、判定の記録（`<子>.judge.json`）は項目ごとに、
その項目の層。フェーズのマーカーのうち種類を根拠に置くものには、その種類の層。マーカーに足すのはこの「種類を根拠に置くもの」だけで、
他のマーカーは種類を見ずに置くので、層を書いても根拠にならない。

`--explain --json`（ボードの JSON）にも層の並びを足す。鍵は `layers[]` で、層ごとにルール・フェーズの種類・配点と、それぞれの綴りと
読めなかった理由が入る。拡張が層を自分で探しに行かないため。

**`--lint` が言うこと。** 今の項目（`projects/` がワークスペースの `.gitignore` に入っているか、プロジェクトが `.claude/` を持たないか、
ワークツリーの元リポジトリが承認済みチケットの `project:` と合うか、走査されないチケットの置き場が残っていないか）に次を足す。`.ccnavi/config/` が無いことは言わない。無いのは正常で、言うと本当に言うべきものが埋もれる。

| 深刻度 | 何を言うか |
|---|---|
| info | 裸の `id` と全欄が一致する重複を後ろの層で捨てた（ルール / 種類 / 配点） |
| warn | ルールの同 `id` で中身が違う（両方効いている） |
| error | フェーズの種類の同 `id` で中身が違う、表示名が層をまたいで重なる |
| error | 配点の同 `id`、合成後の `levels` の逆転 |
| error | 配点の `script:` が指す先が git プロジェクトルートに無い、層の外を指している |
| error | 層のファイルが壊れている（空として扱っている） |
| error | 予約名（`common` / `self`、綴り違いを含む）のプロジェクトがある |
| error | 裸の `id` にコロンが書かれている（3 本とも） |
| warn | ワークツリーの `.ccnavi/` に、元リポジトリに無いファイルがある |

`--lint` の JSON も同じ苦情を同じ深刻度で出す。層の苦情は `where` に層の名前が入る（`(self) rule-id`、`(projects/lib) (risk) id`）。

**端末から打つ `--approve` の終了コード。** 承認の対象の一部が落ちたら 1。通ったぶんの承認済みチケットは置き、落ちたものは名指しする。成功で終わると、
端末を見ていない側（スクリプト、CI）は全部通ったと読む。拡張が子プロセスで打つ `--approve --yes`（§10）の終了コードは変えていない。

**導入スクリプト。** `ccnavi-setup.sh` は共通層に `rules.yml` と `risks.yml`、自身の層に `phases.yml` のひな形を配る。3 本とも
「まだ無いもの」の点検に数える。`--all` の env に `CCNAVI_PROJECT_HOME: ".ccnavi"` を足す。

dry-run でまず層の分布を見て、Bash の和と、大文字小文字を区別しない照合で増えた `ask` と `deny` を数えてから `enable` に切り替える。

### 11.10 実装時に実測するもの

- Bash ツールの環境に `CLAUDE_PROJECT_DIR` が来るか。**来ない（実測済み）。** 案内は `{root}` で書く（§11.8）
- プロジェクトの数に対する PreToolUse の `ms`。プロジェクト 5 本で期限の半分を超えるなら、ルールの読み込みに mtime の控えを足す
- Windows でプロジェクトから切ったワークツリーの `.git` ファイルの `gitdir:` の綴りと区切り
- `projects/` をワークスペースの `.gitignore` に入れたとき、Claude Code がプロジェクトの中の CLAUDE.md を読むか。読まれてよいが、読まれるなら
  ワークスペースの CLAUDE.md と矛盾しないように書く
- `cwd` がプロジェクトの中にあるとき、hook の `${CLAUDE_PROJECT_DIR}` がワークスペースルートのままか

### 11.11 見ないもの、入れないもの

- VS Code 拡張のリスク管理画面の層への追従。共通層の 1 本（`.ccnavi/common/risks.yml`）だけを開き、自身の層とプロジェクトの層の risk は画面から
  直せない（層の配点を差し替えて検証する口も無い）。ルール設定画面とフェーズ管理画面は `--explain --json` の `layers[]` から置き場を取り、
  プロジェクト管理画面から自身の層とプロジェクトの層を開く。層の種類は `--project-phases-file` で共通層と合成して検証してから保存する
- `CCNAVI_TICKET_CONTROL` のプロジェクト単位化。あれはワークスペース単位のつまみで、README の語をその意味に直すだけ
- 統合先ブランチの git オブジェクトから設定を読む形。読むのは checkout されている版だけ
- プロジェクトの `.claude/` を認める形
- `.ccnavi/scripts/` をコアファイルにする形（§11.6）。中身のハッシュを突き合わせる形も入れない
- 層が無いことを `--lint` が言う形。無いのが正常なので言わない。この結果、層を消す・層が入る前のコミットへ `checkout` する、のどちらでも
  そのプロジェクトの deny が痕跡なく消える。「壊す」と error、「消す」と無言、という非対称は残っている。別の issue で決める
- プロジェクト A に居るときだけ効く Bash のルール、`cwd` と `cd` の追跡、引数の語の走査
- 2 段以上の深さに置いたプロジェクト、ワークスペースの外に置いたプロジェクト
- ワークツリーの中に checkout された設定ファイルとスクリプト

受け入れる代償。

- プロジェクトは共通層の `ask` を `allow` に緩められない
- 共通層の `allow` が全プロジェクトに効く
- パスを持つツールに他プロジェクトのルールは足さないので「A のルールを B にも」は書けない
- ワークツリーで足した `.ccnavi/` の変更は、元リポジトリに入るまで効かない
- `glob` も `regex` もどの機械でも大文字小文字を区別せずに当てるので、`allow` も綴り違いに広がる
- 層の数だけ読み込みが増える。1 本 200 行の YAML で数 ms なので期限の中に収まるが、`ms` の欄で見る（§11.10）

---

## 12. 限界と保証しないこと

### 12.1 保証しないこと

§2.1 で対象外に置いた主体（実行環境を掌握した攻撃者）と、そこへ向けた防御は、そのまま保証の外。
加えて次を保証しない。

| 保証しないこと | なぜ |
|---|---|
| 敵対的な回避への防御 | 意図的な文字列の分割、変数展開、alias 経由の実行は対象にしない。完全なシェルの解析を書いても守れる範囲は広がらない |
| `cd` の行き先を読み切れない形 | 変数・置換・グロブを含む `cd`、`cd -`、`pushd` の先は追わない（§6.3.2）。そのあとは継ぎ足さないので、書かれた綴りで当たるものだけが当たる。`case` の枝の中、`$( )` の中身から見た外側の行き先、一覧に無い実行役のコマンドが移す先も同じ |
| 一覧に無い実行役のコマンドの中 | `ssh host <cmd>`・`watch`・`python -c` など、組み込みの一覧（§6.3.1）に無い実行役のコマンドは外さない。先頭に固定したルールはその中に当たらず、未宣言なら §6.5 に落ちる |
| ccnavi の外側で打ち切られた場合 | プロセスごと終了させられると応答が捨てられ、呼び出しはそのまま通る。自分の期限で先回りするだけ |
| 巨大なコマンドを期限の中で読み切ること | 読みは置換 1 つごとに `shlex` を組み直すので、置換を数万個並べると読みだけで期限（3 秒）に届く（8 万個・949KB で 3.6 秒）。読み終えたあと期限切れとして止める側に倒れる（`deadline-exceeded`）が、hook の実行時間はそのぶん延びる |
| git の管理下にないファイルへの事後検知 | 追跡されていない変更・`.gitignore` の中・リポジトリ外への出力は検知しない |
| 情報流出を防ぎ切ること | 外へ送れる形のコマンドはルールで拒否と誘導の対象にできるが、一覧は網羅ではない。スクリプトの中で送る形や Bash 以外の送信経路には届かない。egress を閉じられる sandbox やネットワークポリシーが上位 |
| ルールが列挙していない領域の保護 | 未宣言の領域は判定を持たず、扱いは委ねる先が決める。列挙漏れは防げない |
| hook の登録そのものを消された場合 | 登録が消えれば呼ばれない。`PreToolUse` の登録ごと消されれば一切動かない |
| チケットの生成 | チケットはエージェントが書き、人が承認する。ccnavi は検証と提示だけ |
| ブランチの親子とマージ先の検証 | 判定で確かめるのはワークツリーの名前と識別子の一致だけ。例外はレビュー依頼の前提 |
| チケット同士の範囲の重なり | 同じ場所を 2 本が宣言しても止めない。衝突はマージで解く |
| レビューの中身の評価 | 見るのは未解決のスレッドの有無だけ |
| チャットでの許可の検出 | 人の判断は端末からの直接起動で受け取る |
| リモートの読み書きそのもの | 写しが実物と一致していることは sh と道具の側の責任 |
| Claude Code 以外のエージェントや IDE | ツール名と入出力の書式が異なる |

### 12.2 許容する誤検知

失敗の向きが素通り（実行を見逃す）のものは塞ぎ続け、誤検知（実行でないものを止める）のものは
理由が返るので書き直せる。判断が付かないときに倒す先は常に誤検知の側。

| 誤検知 | 回避 |
|---|---|
| `grep -n "<<" f` がヒアドキュメントに見えて止まる（§6.3、ADR-0013） | 対象をファイルへ逃がす |
| 二重引用の中の `$( )` が、文字のつもりでも実行として読まれる。シェルは実際に実行する | `\$(` で書く、単一引用、`--body-file <ファイル>` |
| バッククォートは、文字のつもりでも中身に依らず止まる（`--body "…"` に書いた Markdown の ``` `git push` ```、閉じないバッククォート。§6.3、ADR-0047） | 単一引用で包む、`` \` `` で書く、`--body-file <ファイル>` |
| `commit -m "$(cat <<'EOF' … EOF)"` や `gh pr create --body "$(cat <<'EOF' …)"` に heredoc のルールが当たる | 本文を Write で置いて `commit -F <ファイル>` / `--body-file <ファイル>`。改行を含む `-m "…"` |
| `cd "$(git rev-parse --show-toplevel)"` が raw-git に当たる | ラッパースクリプトで値を出して読み、次のコマンドに値を書く |
| `grep -rn foo "$(pwd)"` や、grep を改行で 2 行つないだものが allow から外れて確認になる | 値をそのまま書く。1 行ずつ打つ |
| `$( )` の中の、コマンドの先頭ではない `case` の語でも止まる（`"$(echo just in case)"`） | 語を変える。case は if/elif で書く |
| `$( )` の中の縮退（`xargs`、`sh -c`、シェルで割れる形）が、外側のコマンドにも広がる | スクリプトを Write で置いて `sh <ファイル>` |
| 1 語のパターンが引用の中でも当たる（`echo ".env"`） | ルールの綴りを絞る |
| 縮退した複合コマンドの、読めた側の区間も生の文字列で判定される | 別のコマンドに分ける |
| `PARSE_UNCERTAIN` で人に確認が出る | `Write` でスクリプトを置いてから渡す |
| 引用の外のブレース展開が、読める形でも止まる（`grep -rn foo --exclude-dir={a,b} .`、`cp f{,.bak}`。§6.3、ADR-0046） | 語を並べて書く（`--exclude-dir=a --exclude-dir=b`、`cp f f.bak`） |
| シェルが広げない位置のブレースも止まる（代入の右辺 `x={a,b}`、case のパターン `case $x in {a,b})`、`[[ $f == *.{jpg,png} ]]`、bash 3.2 だけで動かす `{1..5..2}`） | 単一引用で包む（`x='{a,b}'`）。パターンは `a\|b)` や `[[ $f == *.jpg \|\| $f == *.png ]]` と書く（`[[ ]]` の中のブレースはどちらのシェルでも一致しない） |
| 変数や置換をコマンド名に置く普通の書き方が止まる（`G=…; $G add`、`sh $SCRIPT`、`"$PYTHON" -m x`。§6.3、ADR-0047） | コマンド名とスクリプトのパスをそのまま書く |
| シェルが展開しない綴りのコマンド名も止まる（単一引用した `'$c'`、エスケープした `\$c`、引用したグロブ文字 `"gi?"`） | 綴りを変える |
| `coproc`・`select` がコマンドの位置で止まる（`for` と `case` の直後の語は止まらない） | `&` で裏に回す。`for` で回す |

### 12.3 未実装

要件書にあって手が付いていないもの。確認の記憶（REQ-PRE-07）とその事後無効化（REQ-PST-04）、
セッション開始時の実効権限の提示（REQ-SES）、記憶の消去（REQ-DIA-05）。一覧と次の一手は
HANDOVER.md。

### 12.4 最終防衛線

ccnavi はアプリケーション層の柵で、それ自体を最終防衛線にしない。実行環境の認証情報に本番
リソースへの権限を与えない、開発用の DB 接続に破壊の権限を与えない、外部通信を許可の一覧で
絞る、コンテナや VM の中で動かす。ccnavi がすべて突破されても、実行環境の認証情報が本番を
破壊できなければ、最悪の被害は起きない。

---

## 付録 A. 理由コード

| コード | 意味 |
|---|---|
| `DENY_COMMAND_PATTERN` | Bash の実行される部分がルールに当たった。読み切れない形でも、当たったルールが全部中で実行されるコマンドで当たったときはこれ（§6.3.1） |
| `DENY_PATH` | ファイルのパスが当たった |
| `RULE_ASK` | ルールの `ask` に当たった |
| `UNDECLARED` | どのルールも言及しない。権限モードに委ねた、または確認できる者が居ないので断った |
| `PARSE_UNCERTAIN` | コマンドを読み切れず生の文字列に当て、どこにも当たらなかった |
| `DENY_BRACE_EXPANSION` | 引用の外にブレース展開を書いた。ルールより先に止める（§6.3、ADR-0046） |
| `DENY_COMMAND_NAME_EXPANSION` | コマンド名の位置に変数・置換・グロブを書いた。ルールより先に止める（§6.3、ADR-0047） |
| `DENY_BACKQUOTE` | 実行されるバッククォートを書いた。ルールより先に止める（§6.3、ADR-0047） |
| `DENY_AMBIGUOUS_FORM` | シェルで読みが割れる形（`$( )` の中の `case` など）か `coproc`・`select` を書いた。ルールより先に止める（§6.3、ADR-0047） |
| `DENY_TICKET_SCOPE` / `TICKET_ASK` | チケットの範囲の外 / 範囲の `ask` |
| `DENY_TICKET_BLOCKED` | 承認済みチケット自体が信じられない。範囲を当てる前に止める（親が引けない、番号が親の計画に無い、`project:` が置き場と違う、など。§9.4、ADR-0058） |
| `DENY_TICKET_PROJECT_MISMATCH` | 行き先のプロジェクトと承認済みチケットの `project:` が違う |
| `DENY_TICKET_APPROVAL_CLI` | 実行ファイルを承認用のオプション付きで直接打った。実行役のコマンド越しに打った形を含む（§9.5） |
| `DENY_PHASE_REVIEW` | フェーズのレビューで止まっている（レビュー準備中・レビュー待ち） |
| `DENY_SUBAGENT_TICKET_OP` | サブエージェントがチケットの状態・レビュー・push を動かそうとした |
| `POST_VIOLATION` / `POST_PREEXISTING` / `POST_TICKET_SCOPE` | 実行後の監視。保護領域の変更 / 前から在った変更 / チケットの範囲外 |

記録の `skip` の理由: `mode-disabled`、`event-not-checked`、`no-subject`、`nothing-to-run`、
`payload-unusable`、`deadline-exceeded`、`tool-cannot-write`、`worktree-unreadable`、`no-turn-baseline`。

## 付録 B. 記録の 1 行

`ts`（ISO 8601、ローカルのオフセット付き）、`mode`、`permission_mode`、`event`、`tool`、`subject`
（1000 字で切り `…(+N)`）、`decision`、`enforced`、`code`、`reason`、`degraded`、`unwrapped`（当たった中で実行されるコマンド。
`\x00` でつなぎ、1000 字で切る。§6.3.1）、`fallback`、`detail`、
`tree`、`project`、`source`、`rules[]`、`quoted[]`、`paths[]`、`guarded[]`、`session`、`ms` の 23 欄。空欄は落とす。
`ts` / `mode` / `decision` / `enforced` / `ms` は常に出る。`O_APPEND` で 1 行を 1 回の write で書く。

## 付録 C. 実測で確かめた前提

設計が乗っている Claude Code の振る舞い。版が変わったら測り直す。

| 前提 | 実測 |
|---|---|
| `PreToolUse` で `permissionDecision` と `additionalContext` を 1 つの応答に入れたとき、両方がモデルに届くか | 届く（Claude Code 2.1.235）。deny と ask では `permissionDecisionReason` と一緒に届く |
| ask の `permissionDecisionReason` が誰に見えるか | 人の確認ダイアログに出る。Yes を押した後もモデルには届かない。No のときは拒否の定型文だけがモデルに届いてターンが終わる。allow の文面はどこにも出ない |
| `/compact` の後に `SessionStart` が来るか | `source: compact` の `SessionStart` が同じ `session_id` で届く |
| `${CLAUDE_PROJECT_DIR}` は `env` で展開されるか | hook の `command` では展開されるが `env` では展開されない |
| `env` は再読み込みされるか | されない。セッションを開き直すまで古い値が残る。hook の `command` は即座に反映される |
| Bash ツールの環境に `CLAUDE_PROJECT_DIR` は来るか | 来ない。hook の環境にだけ来る |
| `.claude/settings.json` に独自のトップレベルキーを足せるか | スキーマ検証に弾かれる |
| Windows は実行中の実行ファイルを上書きできるか | できない。名前の変更はできる |
| `shlex` は引用された `<<` と素の `<<` を区別するか | しない |
| `shlex` は二重引用の中の `$( )` を区切るか | 区切らない。引用の中を 1 語として返す |
| `shlex` は語の途中の `#` をどう読むか | コメントの始まりにする（`echo a#b c` → `echo a`）。bash は `a#b c` と出す |
| `shlex` は行継続を知っているか | 知らない。`\` と改行をトークンの中に残す |
| `SubagentStart` の `additionalContext` がサブエージェントに届くか | 未実測。届かなければ、サブエージェント内の最初の `PreToolUse` で渡す形に変える |
| `isolation: worktree` で起動したサブエージェントの hook が受け取る `cwd` | 未実測。止めるかどうかは cwd で親を引くので、そこが割れる |
