---
version: 1
ticket: i0060
title: 刻みのあるルール（every > 1）に「広い allow」の苦情を掛けない
human_review:
  required: true
  reason: --lint の報告を 1 件消す向きの変更で、全配布先の検証の出力が変わる
rationale: 'i0055 で `every`（渡す回の刻み）が入ったが、`--lint` の `_broad` はそれを知らない。

  `additionalContext` を持つ広い allow を見ると、刻みの有無にかかわらず

  「当たるたびに同じ文がコンテキストに積まれる」と言う。


  `every: 5` を書いたルールでは、この理屈は事実として成り立たない。渡るのは 5 回に

  1 度で、それがそのルールの目的そのもの。苦情のほうが `every` を知らない。


  実例が 1 件ある。i0055-04 が `wip/design/nudge-rule-check.md` に置いた

  `nudge-rule-check`（`glob: "*"` / `every: 5`）は、写すと warn を 1 件増やす。

  対処しない warn が `--lint` に居座ると、次に warn を見る人がそれを飛ばす習慣を付ける。


  改版: フェーズ 1（acceptance）・フェーズ 2（implement）とも完了しレビュー済み。

  main の更新（ADR-0055 のチケット体系刷新）も取り込み、全件テストも通っている。

  追加の作業は無いので `feedback: []` を出して閉じる。

  '
plan:
- acceptance
- implement
feedback: []
allow:
- match: Write|Edit
  glob: ccnavi/*
- match: Write|Edit
  glob: tests/*
started_at: ""
completed_at: ""
base_sha: ""
---
## 改版の理由

フェーズ 1・2 とも完了・レビュー済み。追加のフィードバック対応は無いため `feedback: []` を
出して閉じる。本文（前提・やること等）は元の提案のまま変更なし。
