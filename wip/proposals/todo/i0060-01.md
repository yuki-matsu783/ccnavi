---
version: 1
ticket: i0060-01
parent: i0060
phase: 1
title: every > 1 のルールに広い allow の苦情を掛けないことの受入テストを書く
human_review:
  required: true
  reason: 親と同じく --lint の報告を変える変更の一部であるため
rationale: |
  実装（`ccnavi/lint.py:1175` の条件に `rule.every <= 1` を足す）より先に、
  「刻み（every）のあるルールには `_broad` の苦情を掛けない」「刻みが読めない値なら
  従来どおり掛ける」をテストで言う。書いた時点では落ちる。
allow:
  - match: Write|Edit
    glob: "tests/*"
---

## 書くテスト（`tests/config/`）

| 何を言うテストか |
|---|
| `glob: "*"` + `additionalContext` + `every: 5` の allow は warn を出さない |
| 同じルールから `every` を外すと、いままでどおり warn を出す |
| `every: 1` は warn を出す（既定を明示しただけで、毎回渡る） |
| `every` が読めない値（`0` / `-1` / `"x"`）のときも warn を出す。判定は毎回渡る側へ倒れるので、広さの苦情も残す |
| `additionalContextFile` に本文を置いた `every: 5` のルールも warn を出さない |

最後の 2 つが要る理由。読めない `every` は `rules.py:428` が既定の 1 に倒すので、
`rule.every` を見れば自動的に warn が残る。その挙動を落とさないための杭で、
`every_written` のほうを見る実装に書き換えたときに落ちる。

## 確かめ方

- 書いたテストが**落ちる**こと（実装前なので落ちて正しい）を出力で示す
- 既存のテストが、足したぶんを除いて通ったままであること

## この番号でやらないこと

`ccnavi/` には触らない。範囲も `tests/*` だけ。
