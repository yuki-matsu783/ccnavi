---
version: 1
ticket: i0062-01
parent: i0062
phase: 1
human_review:
  required: true
  reason: エージェントが次に読む指示を書き換えるため。手順の誤りの後始末も含む
title: "{!root} の書き方を add.md に足す"
rationale: |
  親 i0062 の唯一のフェーズ。.claude/skills/ccnavi-config/references/add.md に
  {!root} の書き方と、素朴に置いたときに何が塞がるかを足す。
allow:
  - match: Write|Edit
    glob: ".claude/skills/*"
started_at: "2026-09-22T22:35:27+0900"
completed_at: "2026-09-22T22:36:01+0900"
base_sha: "e67949a42d553543d8eed6aaf639874675c2c829"
ccnavi_approved:
  approved_at: 2026-09-22T22:30:29+0900
  source_tree: i0062
  source_path: C:\Users\taniyama\Desktop\git\ccnavi\.claude\worktrees\i0062\wip\proposals\todo\i0062-01.md
---

# `{!root}` の書き方を `add.md` に足す

## 先に伝えること — 成果物は既に親ブランチにある

**手順を誤りました。** 親 i0062 は `plan: [docs]` を持つので作業は子で行うべきでしたが、
親のワークツリーで直接編集してコミットしました（`58c7ac0`、1 ファイル +35 行）。
`ticket done i0062` が「終わっていないフェーズがある」と拒んで気づきました。

ラッパーは履歴を書き換える手段（`reset` / `rebase` / `revert`）を通さないので、
自分で巻き戻せません。利用者の判断で、**子を起こして手順に戻す**ことにしました。

そのため **この子の差分は空になります**。リスクの数字も 0 になりますが、
実態を表していません。実際の成果は親ブランチの `58c7ac0` です。

レビューは、その 1 コミットを見てください。

## 書いたもの

`.claude/skills/ccnavi-config/references/add.md` の「当て方」の節に 1 行、
そのあとに `{!root}` の節を 1 つ。

| 決まり（表で書いた） | |
|---|---|
| 書ける場所 | `regex` だけ。`glob` は error |
| 置ける位置 | `^` の直後に 1 回だけ |
| 直後の量化子 | error。`^{!root}?` はどの対象にも当たる式になる |
| 後ろに続く式 | `.*` 等で受けていなければ warn |
| 対象がルートそのもの | 「中」 |
| 大文字小文字 | 区別しない |

あわせて **素朴に `deny: ^{!root}` と置いたときに塞がるもの**（セッションのスクラッチパッド、
他リポジトリの参照、他ツールの出力先）と、先に考えること（`match` を絞る、
例外が要るなら名指しのルールにする）を書いた。

ルートが 256 字以上で全部止まること、回復は利用者にしかできないことも書いた。

展開の中身と、なぜ先読みを使わないかは ADR-0058 と `ccnavi.md` §5.3 にあるので、
スキルは「書くときに踏む穴」に絞ってある。

## 書いた内容は実装で確かめてある

`scratchpad/try.yml` に 7 件のルールを書いて `--lint --rules` に掛けた。
表の 7 行が、実際の判定と一致することを確認済み。

```
ok-plain / ok-suffix                                      苦情なしで通る
warn-structural                                           warn
err-quantifier / err-position / err-twice / err-in-glob   error
```

## 得るもの

- 実行ファイルの語彙とスキルの説明が揃う。次のセッションが古い書き方を試して止まらない
- フェーズの記録が残り、レビューの経路が正規のものになる

## 失うもの

- **子の差分が空。** リスクの数字（0）が実態を表さない。この提案と依頼文に理由を書いて補う
- 手順を戻すための往復が 1 周増える

## やらない場合

親が `done` できないまま止まる。`plan` を空にする改版で親が直接やる形にもできるが、
「計画のあるチケットは子が作業する」という前提を、私の誤りに合わせて崩すことになる。
