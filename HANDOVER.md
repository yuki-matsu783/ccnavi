# 引き継ぎ

2026-09-06 のセッションからの引き継ぎ。次に入る人が最初に読む想定。

## この道具は何か

Claude Code の hook から呼ばれ、危ないツール呼び出しを止め、
同時に「代わりに何をすればよいか」を名指しで返す。止めるだけでは足りない、
というのが出発点。`git push` を禁じられたエージェントは諦めず絶対パスで打ち直すので、
拒否は目的を諦めさせるのではなく別の道へ向け直さないと効かない。

読む順番は次のとおり。

| 文書 | 中身 |
|---|---|
| [CONTEXT.md](CONTEXT.md) | 用語集。全体ルール・チケット制御・直接作業・チケット作業・提案・写し・印。実装のことは書かない |
| [requirements.md](requirements.md) | 外から観測できる要求だけ。135 件、13 面。実装の理屈は書かない |
| [ccnavi.md](ccnavi.md) | 到達点の設計。仕様側。冒頭に「現在の実装との食い違い」の注記がある |
| [README.md](README.md) | 設定、ルールの書き方、モード、記録の読み方 |

`knowledge/` は参照専用の資料で、git 管理外。Claude Code の hook にまつわる実測が
入っている。要件と実装の判断はほぼここから来ている。

## いま動くもの

`PreToolUse` のルール照合と `PostToolUse` の事後監視。dry-run で自分自身に
仕掛けてある。

設定 3 本（ルール・フェーズの種類・リスクの配点）は**層の和**で判定する（設計 §25.2〜§25.4.2、REQ-MLT）。
共通層 `.claude/ccnavi/`、ワークスペース自身の層 `.ccnavi/config/`、プロジェクトの層
`projects/<名前>/.ccnavi/config/` の 3 種（`.ccnavi` は `CCNAVI_PROJECT_HOME` の既定値）。

- Write / Edit / NotebookEdit は共通層 + 行き先の 1 層、Bash は共通層 + 全部の層。足すだけで上書きは無い
- 層の id は `self:id` / `<名前>:id`。写し（全欄一致）は後ろを捨てて info、ルールの同 id 中身違いは両方効いて warn、
  種類と配点の同 id 中身違いはその層を空にして error
- フェーズの種類と配点は親の写しの `project:` の層を足す。`levels` は書かれた鍵だけがキーごとに min。`overlap` / `requires` は合成後に確かめる
- 予約名 `common` / `self`（`casefold`）のプロジェクトは層として数えず、そこへの書き込みは共通層だけで判定する
- `glob` は機械の見方で大文字小文字を扱い、`regex` は区別を残す。裸の `id` にコロンは書けない
- 記録の `source`、`--explain` の層ごとの全件、`--explain --json` の `layers[]`

自己保護（selfguard）の中核は、hook の登録と実行ファイルに加えて、共通層の 3 本、自身の層の 3 本、各プロジェクトの層の 3 本、
それらの作業ツリーの中の写し（切り元基準で列挙）まで広がった。層の傘 `.ccnavi/` の下は組み込みの deny
（`builtin-guard-project-home`）で名指しのツールから、`builtin-guard-setting-files` でシェルから止める。シェルの綴りは
`rm -rf .ccnavi` のように傘ごと消す形も止める。`.ccnavi/scripts/` は中核に入れず、この deny と `CCNAVI_RESTORE_IF_DENY` に任せる。

**移行の途中。** このワークスペースの自身の層 `.ccnavi/config/phases.yml` は置いてある。旧 `.claude/ccnavi/phases.yml` の削除は、
新しい実行ファイルを配ったあとに人が行う。逆順にすると古い実行ファイルが自身の層を読まず、フェーズの種類が全部消える（実際に起きた。設計 §25.12）。
層が無いことを `--lint` が言うか（消す・古いコミットへ `checkout` するとプロジェクトの deny が痕跡なく消える件）は別の issue で決める。

```
main.py                     配布物の入口。PyInstaller が渡すスクリプト
ccnavi/hookio.py            stdin の payload の解釈と stdout に返す応答
ccnavi/rules.py             ルールの読み込みと検証
ccnavi/builtin.py           ルールを読めないときの組み込み既定
ccnavi/globmatch.py         glob から正規表現への翻訳
ccnavi/shellread.py         コマンド文字列のうち実際に実行される部分の切り出し
ccnavi/settings.py          環境変数と設定ファイルからの設定解決
ccnavi/gitstate.py          作業ツリーで実際に何が変わったかを git から読む
ccnavi/post.py              実行後の監視。検知・差し戻しの文・復元
ccnavi/ticket.py            チケットの読み込みと、そこが宣言する作業範囲。親子の部分集合
ccnavi/tree.py              作業ツリーの特定。判定の鍵はファイルの行き先
ccnavi/approval.py          承認済みの写し・フェーズの印・子ごとの記録・承認の画面
ccnavi/risk.py              実績で測るリスク。risk.yml・差分の計測・スクリプト・定性項目
ccnavi/phase.py             フェーズの終わりとゲート。提案から写しへの同期
ccnavi/phasetypes.py        フェーズの種類の定義（phases.yml）の読み込みと検証
ccnavi/review.py            レビューの依頼と確認。作業ツリーの前提検査と、sh が渡す写し（JSON）の判定。network に出ない
ccnavi/ops.py               チケットの状態を動かす ticket start / done / cancel
ccnavi/audit.py             1 行 1 件の追記記録
ccnavi/lint.py              設定とルールの検証
ccnavi/diagnose.py          判定を実行せずに試す --test と --explain
ccnavi/cli.py               引数の解釈と振り分け。ticket / review の副命令を ops / review へ
ccnavi/events.py            hook のイベントごとの手順
ccnavi/judge.py             実行前の判定
ccnavi/reasons.py           判定に添える文面と理由コード
ccnavi/ruleload.py          この呼び出しに当てるルール集合の決定
ccnavi/subagent.py          SubagentStart / SubagentStop
ccnavi/ctxfile.py           additionalContext の組み立てと once の控え
ccnavi/selfguard.py         ccnavi 自身の設定ファイルと実行ファイルの控えと復元
ccnavi/modes.py             enable / dry-run / disable と終了コード
ccnavi/gitcmd.py            git を 1 回起こす
ccnavi/fsio.py              ファイルの読み書きの型
build.py                    PyInstaller の onedir で配布物を組み立てる
tests/                      受入テスト。入口（cli.run）に引数と標準入力を渡し、応答だけを見る
tests/inproc.py             その起動をプロセスを起こさずに行う。起動の検査は test_entry.py だけ
tools/gitlab/               実物または代役の GitLab で 1 周する道具。人が手で回す。自動テストは呼ばない
```

実行時の third-party 依存は PyYAML 1 本。チケットの frontmatter が YAML なので、
自前の読み手を書くと書式の解釈違いがそのままガードの穴になる。読むのは
`safe_load` に限る。任意の Python の型を組み立てる `load` は、エージェントが
書けるファイルに向けては使わない。PyInstaller と ruff は開発時にしか要らず、
配布物には入らない。

依存ゼロは以前の方針だったが、この判断で降りた。増やした 1 本ぶん、
ガード自身が点検できない供給網が増えている。

確認コマンド。

```sh
uv run python -m unittest discover -s tests -t .
uv run --with ruff ruff check .
uv run --with ruff ruff format --check .
uv run --with pyinstaller python build.py
```

## 未実装

要件書にあって手が付いていないもの。

- 確認の記憶の事後無効化（REQ-PST-04）。無効にする記憶がまだ無い。REQ-PRE-07 と対
- セッション開始時の提示（REQ-SES）。REQ-SES-02 / -03（却下された要求と想定範囲外の
  許可の区別）はチケットが入ったので書ける形になったが、まだ書いていない
- 診断コマンド（REQ-DIA）。残るのは記憶の消去（REQ-DIA-05）だけ。表示は `--explain`、試験は `--test`、設定検証は `--lint` で入った
- 確認の記憶（REQ-PRE-07）。`ask` が入ったので、同じ根拠で繰り返し聞かない仕組みが
  次に要るものになった。ルールが `ask` と書いた確認は記憶の対象外（設計 §13.1）
- 期限（REQ-CMN-06）はループの中で見ているだけで、実測していない
- モードは `disable`（判定しない）/ `dry-run`（判定して報告するだけ）/ `enable`（判定を適用する）。指定が無いときは `enable`。未知の値は報告して `enable` に倒す
- 並行するチケット（REQ-TKT）のうち、GitHub の実物に対する `request` と `check` は
  実測していない。GitLab は実物（CE 18.5）で 1 周した（「実物の GitLab で 1 周した」）。
  自動テストは sh の代わりに写し（`--result`）を渡す形で通す

## 次にやること

**`test-py.sh` が、作業ツリーの抜け殻で落ちる（人が直す。hook はエージェントが触らない）。**
`git worktree remove` が `.venv` の 1 ファイルを消せずに抜け殻を残すことがある（下の
「作業ツリーが消せない」）。抜け殻はディレクトリとしては在るので `[ -d "$target" ]` を通り、
`uv run … unittest discover` が `Start directory is not importable: 'tests'` で落ちて、
関係のない差し戻しがモデルへ届く。`[ -d "$target/tests" ] || continue` に直せば済む。

**`ccnavi-review.sh` に、敵対的レビューで見つかった漏れが 2 つ残っている（人が直す）。**
スクリプトはエージェントが触らない決まりなので、直し方だけ書く。
(1) `origin` を読めなかったときの `fail` が URL をそのまま stderr に出す（114 行付近と、
その後の 2 か所）。`ssh://oauth2:<token>@host:2222/g/p.git` のように読めない綴りだと
トークンが漏れる。伏せた綴りを origin を読む前に 1 度作り、`fail` にはそれだけ渡す。
(2) `origin` サブコマンドの伏せ字が最初の `@` まで（`s#^([a-z]+://)[^/@]+@#`）で、解析は
最後の `@` まで。`glpat-A@B` のように `@` を含む資格情報だと後半が出る。`[^/]*@` に直し、
scheme の `[a-z]+` は大文字も含める。exe 側の `remote_kind` は同じ規則に直してある。

**複数のリポジトリ（REQ-MLT、設計 §25）は入った。** private のワークスペースリポジトリを
Claude Code が開き、その下の `projects/<名前>/` に clone したプロジェクトを横断する形。
`tests/test_projects.py` がワークスペース 1 つとプロジェクト 2 つで通す。骨は、書き込みは行き先の
プロジェクトのルール（`config/rules.yml`）で判定し、Bash はワークスペースと全プロジェクトのルールの和で
判定する（`cli.rules_for`）。実行後の監視はツリーごとに git を起こす（`post.Watched`、`cli.watched_for`）。
チケットは `project:` を持ち、承認が置き場に在ることを確かめて子に継がせ、作業ツリーの切り元と
違えばルールより先に止める（`DENY_TICKET_PROJECT_MISMATCH`）。プロジェクトのルールファイルは
selfguard の中核に入る。`projects/` が無ければ前と同じに動く。
（この段落は入った時点の形。ルールの置き場と判定は、のちに設定 3 本の層の和へ置き換わった。今の形は「いま動くもの」と設計 §25。
`config/rules.yml` はもう読まない。）

残っているもの。

- `.claude/scripts/ccnavi-git.sh` の記録を `logs/<プロジェクト>/` へ寄せる（REQ-MLT-14 の後半）。
  今は toplevel の `logs/` に書くので、プロジェクトの中に出る。sh は `guard-scripts` が止めるので人が直す
- 拒否の文面が案内する sh の綴り。`CLAUDE_PROJECT_DIR` は Bash の環境に来ない（実測済み）ので、
  プロジェクトのルールの `message` は `{root}/.claude/scripts/...` で書く。このリポジトリの rules.yml も同じ
- §25.10 の残り。プロジェクトの数に対する `ms`、Windows でプロジェクトから切った作業ツリーの `gitdir:` の綴り

**VS Code 拡張にプロジェクト管理画面が入った（2026-09-12、拡張 0.3.0、設計 §24.10 の末尾）。** `projects/` の
一覧、URL からの `git clone` の送信（ターミナルへ。拡張は clone を自分では走らせない）、`.gitignore` と
`config/rules.yml` の雛形をボタンで整える、各行からプロジェクトのルール設定画面とボードの絞り込みへの導線、
`fetch` / `pull`。実行ファイルには 2 つのオプションを足した。`--lint --json`（README「lint の JSON」、REQ-DIA-08）と、
診断だけで効く `--project-rules-file <名前>=<パス>`（REQ-DIA-09。hook からの判定では無視する。selfguard は
本来の場所を守る）。`tests/test_lint.py` と `tests/test_projects.py` の末尾のテストが通す。拡張の
手動確認は README の表 25〜33。まだ拡張開発ホストでは通していないので、次に入る人が 25〜33 を 1 度踏む。
**拡張は層の和に追従していない。** プロジェクト管理画面とルール設定画面は今も `CCNAVI_PROJECT_RULES` / `config/rules.yml` を
読み書きするので、そこで保存したルールは判定に効かない（設計 §25.11）。`.ccnavi/config/rules.yml` を開く形へ直すのは別の作業。

**並行するチケット（REQ-TKT、設計 §24）は入った。** `tests/test_ticket.py` が親 1 本と子 2 本を
フェーズ 1 つで通す。残っているのは実測が要るもの。

- SubagentStart の `additionalContext` がサブエージェントに届くか。届かなければ、
  サブエージェント内の最初の PreToolUse で渡す形に変える（§24.9）
- `isolation: worktree` で起動したサブエージェントの hook が受け取る `cwd`。判定は行き先で
  決まるので止め方は変わらないが、ゲートは cwd で親を引くので、そこが割れる
- GitHub の実物に `request` / `check` を当てる。GraphQL の `reviewThreads` は文書どおりに
  書いただけ。GitLab は済んだ（`tools/gitlab/probe_gitlab.py` で繰り返せる）。GitLab の変更要求
  （`request_changes`）だけは CE に無い機能で、EE でしか当てられない
- `.claude/scripts/` への Write は `guard-scripts` が止める。今回の 2 本はこの
  リポジトリで作ったので入っているが、他のプロジェクトへ配るときは人が置く
先に §24.12 の 4 つを実測してから書く。とくに SubagentStart の `additionalContext` と
`isolation: worktree` の `cwd` は、結果で §24.9 の置き場所が変わる。

**チケットの状態遷移を §24.14 に 1 か所でまとめた。** 提案の置き場・承認の写し・フェーズの印の 3 層を
並べて、動かす者と条件を層ごとに表にした。まとめる途中で動かして確かめた 3 つは、いまの挙動として
書いてあるが、それでよいかは決めていない。

- `ticket start` / `done` は写しの有無を見ない。未承認のまま `doing/` `done/` まで進める。止めるか、
  せめて「未承認」を stderr に出すかは決めていない（`--lint` は言う）
- `--approve` は `done/` にある未承認の提案も束に入れる。承認した写しは次の hook で即座に閉じる。
  `cancelled/` と同じく `done/` も除くほうが自然に見える
- 人が子を再開しても、そのフェーズの `reviewed` は残る。再び `done` にしてもゲートは閉じず、
  告知も出ない。再開の手順に「印も消す」を入れるか、写しを戻したときに機構が消すかは決めていない

**権限モードへの委譲がどれだけ出るかを実測する。** ここがいちばん未知。記録の `code` が
`UNDECLARED` の行を数え、`tool` と `subject` の傾向を見る。
同じ場所が繰り返し出るなら `allow` に 1 行足す先が決まっているということで、
毎回違う場所が出るなら `allow` の粒度が細かすぎる。数が多いまま放置されると、
人は中身を読まずに承認するようになり、ルールが置いた確認もそこに埋もれる。
`RULE_ASK` との比も同時に見る。前者が後者を大きく上回っているうちは、
確認の質が落ちている。

**実行後の監視を実測で見る。** `PostToolUse` に登録して自分自身に仕掛けてある。
記録の `event` が `PostToolUse` の行を数え、`decision` が `deny` の回に
何が挙がっているかを見る。見たいのは 3 つ。`worktree-unreadable` が出ていないか
（出ていれば監視は 1 件も見ていない）、`detail` の `preexisting` が毎セッション
大量に出ていないか（出ていれば控えの取り方が合っていない）、`paths` に同じ場所が
繰り返し出ていないか（出ていれば止めるべきは経路ではなく出力先の設定）。
`git status` を毎回起こす代金も、`ms` の欄で `PreToolUse` の行と比べられる。

**誤検知の数を実測で比べる。** コマンド位置の判定と縮退の文面（REQ-HNT-04）は
入った。`ccnavi/shellread.py` が Bash のコマンドから実行される部分だけを切り出し、
読み切れないときだけ生の文字列に縮退して、そのときは文面を変える。
記録に `degraded` が付くので、止めたもののうちどれだけが読み切れないまま出た判定かを
数えられる。`.claude/ccnavi/log.jsonl` を貯めて、導入前の誤検知（`echo "git push"`
`grep -n "git push"` の類）が消えたことと、`degraded` の割合が小さいことを確かめる。
割合が大きければ縮退の条件が広すぎる。

**ヒアドキュメントを既定のルールで止めるようにした。** `<<` を含む Bash 呼び出しは
拒否する。ヒアドキュメントで書いたファイルは `permissions` の宣言も
`PostToolUse` の検査も通らないので、ファイルの中身を落とす経路がそこだけ
素通しになっていた。ファイルへ書き出す形だけを止める案もあったが、
プログラムへ渡す形（`uv run python - <<'PY'`）も `Write` でスクリプトを
置けば代わりが効くので、全部止めるほうにした。実装がルール 1 行で済み、
抜け道も少ない。記録では Bash 883 件中 42 件が該当で、内訳は
ファイルへ 17 件、プログラムへ 25 件だった。

**ファイルのパスは行き着く先で見るようにした。** `file_path` を来たまま当てていたので、
`docs/../.env` のような綴りでルールを外せた。絶対パスに直し `..` を畳み、
シンボリックリンクを解いてから当てる。Bash のコマンドに現れるパスは対象外で、
そちらはコマンド文字列としてしか見ていない。`cat docs/../.env` は
`.env` の綴りが残るので今も当たるが、`cat $(dirname x)/.env` のような形は追えない。

**語の中のワイルドカードが当たらなかった。** 語の切れ目を自動で入れる仕組みが、
ワイルドカードの両隣にも `\b` を出していた。`foo*bar` が `\bfoo\b.*\bbar\b` になり、
`foo-bar` には当たるのに `fooXbar` にも `foobar` にも当たらない。書いた人は
当たるつもりで、実際は何も止めない。ルールを 1 件足したのに黙って死んでいる形で、
この記法がいちばん防ぎたいものそのものだった。ワイルドカードに接する側の `\b` を
出さないように直した。ルールで実際に使っている書き方は、翻訳した形をテストで
固定してあった。この記法自体をあとで glob に置き換えたので、いまその守りは
`tests/test_glob.py` が引き継いでいる。

**残っている誤検知は 1 語のパターン。** 空白を含まないパターンは、
引用の中にあっても当たる。`echo ".env"` は今も `.env` のルールに当たる。
語の連なりを引用が 1 語につないだかどうかで見分ける仕組みなので、
元から 1 語のものは見分けようがない。塞ぐならルール側に
「コマンド位置だけ」「引数だけ」の区別を足すことになり、ルール書式の版が上がる。

**前からある取りこぼし。** `git -C /repo push` は `git push *` に当たらない。
`glob` が語の並びをそのまま探すからで、記法を替えても消えていない。
knowledge の同じ文書がグローバルオプションの飛ばし方を書いている。

## 判断の経緯

やり直しになった箇所と、その理由。同じ道を戻らないために残す。

**自前のパターン記法をやめ、glob に寄せた。** 欄の名前は `pattern` から `glob` に、
意味は `fnmatch` そのままになった（版 3）。以前は `*` と `?` と空白の並びを扱い、
語の切れ目を自動で挟み、部分一致にしていた。便利だったが、書いたものと当たるものの
間に見えない層が 1 枚あることになり、その層が実際に事故を起こしている。
語の中に置いたワイルドカードの両隣に切れ目が入り、`foo*bar` が `foobar` に
当たらなくなっていた。足したつもりで何も止めていないルールは、この記法が
いちばん防ぎたかったものそのもの。

代わりに失ったものが 2 つある。**部分一致は自動で付かない。** 前後に `*` を
自分で書く。`git push *` は `cd /repo && git push` に当たらず、`*git push*` なら
当たる。**語の切れ目も入らない。** 右側だけなら空白を書いて `*sed *` とすれば
`sedate` を外せるが、左側は glob では書けない。`*git push*` は `legit push` にも
当たり、`* git push*` と書くと行頭の `git push` が外れる。左の切れ目が要るルールは
`regex` を使う。

残したのは区切り文字の正規化 1 つだけ。`/` と書けば `\` にも当たる。ここを
落とすと、ルール 1 本の意味が走らせる機械で変わることになる。実装は
`fnmatch.translate` の出した式の `/` を置き換えるだけ。文字クラスの中に `/` を
書いた `[a/b]` のような形は組み立てに失敗して読み込み時に落ちる。黙って別の
意味になるより落ちるほうがよい。

**判定を実行せずに試せるようにした（REQ-DIA-01・02・03）。** `--test` に
ツールと対象を渡すと、判定・根拠コード・当たったルール・`glob` が翻訳された
正規表現・返る文面が出る。`--explain` はタイプごとのルールと承認済みの作業範囲を
一覧する。どちらも `cli.decide_before` をそのまま呼ぶので、試験で見た判定と
実運用の判定が同じものになる。ここを作り直すと、試験が通ったのに実運用で落ちる、
という一番まずい形になる。

見本は `.claude/ccnavi/rule-samples.yml`、回すのは `tools/check_rules.py`。
`/ccnavi-config` スキルがその流れをまとめ、食い違いの原因を調べて、
怪しい当たり方を利用者に確認する。ルールを 1 件足したら見本も 1 行足す。
止めたいものだけでなく止めたくないものを一緒に置くこと。片側だけの見本は、
ルールを広げすぎたことに気づけない。

**ルールを 3 タイプに分け、既定を許可から確認に変えた。** ルールファイルは
`.claude/ccnavi/rules.yml`（YAML）で、`deny` `ask` `allow` の 3 つに分かれる。
強い順に見て、どこにも当たらなければ Claude Code の権限モードに従う。つまり
`allow` を書き切るまで、言及されていない呼び出しはすべてそこへ渡る。設計 §11.1 の順 5 がそのまま
入ったことになるが、設計はこれを `sandbox_root` と `expected_roots` の内側で
やる想定だった。それが無いので、絞り込みは `allow` の書きぶりだけが担っている。
書きすぎれば人が見なくなり、書かなければ確認が止まらない。ここは実測が要る。

**ルールが言及しない呼び出しは、判定を返さず Claude Code の権限モードに従う。**
設計 §13 は「暗黙的 ask」と呼んで ask の一種として扱っていたが、その名前ごとやめた。
ccnavi はその呼び出しについて判定を持っていない。持っていない判定を ask として
返すと、判断できるモードでも必ず人に止まり、auto モードが実質効かなくなる。

| `permission_mode` | 結末 | 記録の `decision` |
|---|---|---|
| `auto` | classifier が判断する | `handover` |
| `default` / `acceptEdits` / `plan` / 不明 | 人に確認が出る | `ask` |
| `dontAsk` / `bypassPermissions` | 通さない（REQ-PRE-08） | `deny` |

渡した回も記録には残る。ルールを足すべき場所は `handover` の数で決まり、ガードが
実際に効いた回は `ask` と `deny` で決まるので、同じ欄には置けない。ルールが `ask` と
書いた場所は権限モードによらず確認に出す。人が意図して置いた確認ポイントを
classifier の判断で消さないため。記録の `code` は `RULE_ASK` と `UNDECLARED` で
数え分けられる。文面は「危険だから聞いている」とは書かない。言えないから委ねている。

**読み切れないコマンドに `allow` は当てない。** 当てる先が実行される部分ではなく
生の文字列なので、そこで許可を出すのは「読めなかった文字列にそう書いてあった」を
根拠に通すことになる。`deny` と `ask` は当てたままにする。厳しい側へ外れるだけで、
そのことは文面が断る。結果として読み切れないコマンドは `PARSE_UNCERTAIN` で
人に確認が出る。ここだけは権限モードに委ねない。読めなかったという事実は判定の
結果に現れず、委ねた先には伝わらないので、それを言えるのがここしかない。
設計 §13.1 がこのコードを ask 系に置いていたとおりになった。

**既定に落ちると、読み取り以外はほぼ確認になる。** 組み込みの既定には
プロジェクトの `allow` が無いため。`Read` だけ通してある。いちばん数が多く、
作業ツリーを変えようがない。ここまで確認を出すと、本当に見てほしい 1 件が
その中に埋もれる。REQ-PRE-06 の「修復を妨げない」は満たしている。妨げるのは
`deny` だけで、`ask` は人が答えれば進む。

**YAML の解析エラーを ValueError に包み直している。** 包まないと、ルールファイルの
書き損じが hook の異常終了になる。既定に落ちる経路を通らずに落ちるので、
壊れたファイルを直す呼び出しも止まる。テストで見つけた。読み手を替えるときは
包み直しの側も一緒に見ること。

**チケットは絞る方向にしか使わない。** 設計のチケット層（§9）は、Layer 1 の
`target_directories` の上に乗る。その土台はまだ無い。チケットが担うのは
「宣言した範囲の外は止め、中は聞かない」の 2 つで、ルールが何も言わなかった
ときだけ見る。ルールのほうが強いので、チケットが上位の宣言を覆すことはない。
設計 §9.2 の「できないこと」の側（上位の deny を覆さない、明示の ask を allow にしない）は、
広げる経路そのものが無いので自動的に満たされている。

**判定が読むのは台帳で、チケットのファイルではない。** チケットはエージェントが
書けるので、判定がそこを直接読んでいたら、範囲の外で止められたエージェントが
範囲を 1 行足して通れる。止められた側が止め方を書き換えられる仕組みは止めていない。
（この段落は台帳の時代のもの。いまは写しで、後の「並行するチケットを実装した」に続く。）
`ccnavi --approve` が承認した内容を `.claude/ccnavi/approvals.jsonl` に追記し、
判定はそこだけを読む。承認後にチケットを書き換えても、効くのは台帳の側なので
範囲は広がらない。広がらないかわりに食い違うので、食い違いは毎回報告する。

**未承認を「全部拒否」に倒さなかった。** 倒すと、チケットを直す手も台帳を直す手も
同時に止まる。ルールファイルが読めないときに組み込みの既定へ落ちる（REQ-PRE-06）のと
同じ形で、倒れた先に回復の道を残した。そのかわり呼び出しごとに言う。
黙って外れた制限は、掛かっているつもりで居られるぶん最初から無いより悪い。

**チケットのファイル自身は範囲の外でも書ける。** 塞ぐと、いちど承認した範囲から
出る道が無くなる。次のチケットを提案する経路は開けておく。提案が効くのは
承認されてからなので、開けても範囲は広がらない。

**チケットまわりで設計と違うところ。**

| 設計 | 実装 | なぜ |
|---|---|---|
| 承認台帳は `.claude/approved-tickets.jsonl`（§17.5） | `.claude/ccnavi/approvals.jsonl` | そこはルールが Write / Edit を止め、既定がシェル経由を止めている。置き場を分けると守り方も 2 通りになり、片方が緩んでも気づかれない |
| 指紋は frontmatter 全文の正規化ハッシュ（§17.5） | 解釈した中身（`ticket` `title` `rationale` `write`）のハッシュ | 人が承認画面で見たものと、指紋が覆う範囲を一致させる。全文だとコメント 1 つで再承認になり、承認が「また出た、押しておこう」に化ける |
| `config_hash` も併せて記録（§17.5） | していない | 対応する config が無い。ルールファイルの版を混ぜる案もあったが、ルールが変わるたびに全チケットが未承認になる |
| 承認画面に「編集して承認」「再生成」（§17.2） | 承認と中止だけ | チケットを書くのはエージェントの仕事。承認する場所で書き替えられると、承認した人が承認したものの作者になる |
| リスクは §17.3 の 14 項目（宣言の広さで数える） | 実績で数える（`risk.py`、2026-09-10）。宣言の点と二段階承認は消した | 守りたかったのは「計画では軽いと思ったが実際は大きかった変更がレビューを通らずに進む」こと。宣言は実績を保証しない。宣言の広さは親が `human_review.reason` で言う |
| 未承認は「全 allow を ask に降格」（§17.5） | チケット由来の判定を一切出さない | 結果はほぼ設計どおりになる。チケットが何も言わなければルールの 3 タイプだけが残り、そこも言及していない場所は Claude Code の権限モードに従うので、降格の処理を別に書かずに同じ着地点へ行く |
| リスク 40 ちょうどの扱いが揺れている（§17.3 の転記注記） | 閾値は「以上」。HIGH（既定 40）以上で宣言に関わらずレビュー要 | 段階の表と一致する側。40 で HIGH と表示しながら通せるほうが読み手を裏切る |
| 実績のリスクの項目 | 定量（組み込み 4 種）・スクリプト・定性（サブエージェントが判断、親が記録）の 3 系統。スクリプトは guard の内側にしか置けず、測れなければ重い側に倒す | エージェントが書ける場所のスクリプトを許すと自分のリスクを自分で決められる。「測れないから 0」は壊れたスクリプトがリスクを消す。定性の判定は子の HEAD に結び、動けば取り直す |

**並行するチケットの設計を決めた（ccnavi.md §24、REQ-TKT）。** 2026-09-09 に
利用者と 1 問ずつ詰めた。決めたことと、採らなかった側の理由。

| 決めたこと | 採らなかった側 | なぜ |
|---|---|---|
| 判定の鍵は Write の行き先が属する作業ツリー | `cwd`、`session_id`、`agent_id` | サブエージェントは親と `cwd` を共有しうる。行き先なら誰が書いても同じ判定になる |
| 作業ツリー名 = チケット識別子。親が git ラッパで作る | チケットに `worktree:` を書く、`Agent` の `isolation: worktree` に任せる | `isolation` は名前を選べない。名前で結べば `--lint` で一致を確かめられる |
| 子は親の部分集合。書いていない場所は範囲外 | 親からの差分だけ書く | 子のファイルだけ読んで、子が書ける場所が分かる |
| 親子は厳しい側が勝つ。深さ 2 段 | 無制限 | 承認画面で祖先を全部たどらずに済む。要ったら上限を上げるだけ |
| 親も自分のチケットに縛られ、作業ツリーを持てる | 親は main で 1 本だけ | 複数の親を同時に走らせたい。main はチケットを持たない |
| 承認済みの姿はチケットごとの写し。台帳 jsonl は廃止 | 識別子ごとに引ける台帳 | 有効な集合 = ディレクトリの中身。指紋照合が要らない |
| 閉じるのは親だけ。`status: done` を書く保護済みスクリプト | `ccnavi --close` を打たせる、作業ツリー削除で閉じる | 閉じるのは範囲の消える向きで承認不要。エージェントに ccnavi のコマンドを打たせない |
| 計画中の親はルールだけ。計画の終わりに束で承認 | 親を先に承認、未承認の親に `wip/tickets/` だけ開ける | 「未承認は 1 ミリも効かない」を壊さない |
| フェーズは子の `phase:`。同じ番号に足せる | 必ず次の番号 | 足した時点で印を消せば意味は保てる |
| ゲートは `Agent` と Bash（3 つの例外あり）。Write は通す | Write も止める | 次のフェーズの計画はレビュー前に進めてよい |
| ゲートを開けるのは MR / PR の未解決スレッドが 0 | 人が印を打つだけ | レビューの実物に結ぶ。GitLab と GitHub をリモート URL で見分ける |
| 未解決を残して進めるのは人が端末で | チャットの OK をエージェントが伝える、UserPromptSubmit で文を拾う | 止められた側がゲートを開ける形にしない。人の手が入る場所を承認と同じ 1 種類に寄せる |
| ブランチの親子と範囲の重なりは見ない | `--lint` で merge-base を見る、重なる承認を拒む | 今回は write の範囲だけ。重なりを止めると範囲を広げて逃げる向きに働く。ただしレビュー依頼の前提としてだけは合流を見る（下の表） |

**参考の運用（`参考/issue-mr-ticket-workflow`）から抜いて改めた（ccnavi.md §24.13）。** 同日、
上の設計を参考の運用層と突き合わせた。参考の `hook機構.md` は ccnavi.md の元になった同じ設計書で、
新しい要素は無い。価値があったのは `ticket.sh` / `worktree.sh` / `boundary.sh` とそれを縛る hook 群の側。
参考は並列実施を「隔離はされるが統制が効かない」で発効保留にしていて、その穴は判定の材料を
cwd のツリーの作業中チケットから採ることにある。ccnavi の「行き先で結ぶ」がそこを越える。

| 採ったもの | 改めた点 |
|---|---|
| 状態はディレクトリで表し、提供コマンドだけが動かす | 判定の権威は写しのまま。置き場は閉じる向きにだけ効く |
| `start` が `base_sha` を記録し、事後検査は基準点からの差分 | 基準点は写しにも写し、検査は写しの値を使う |
| 人間レビュー要否をチケットごとに持ち、要のある切れ目だけレビュー | 既定は要。省略は印に残す |
| `request` / `complete` / `note` と前提検査、依頼以降の指摘だけを数える、変更要求は `--accept` でも通さない | 依頼の前提に子ブランチの合流を足した |
| 進行状態は共有ルート 1 か所、判定記録はツリーごと | 写しと印は main、記録は各ツリー |

| 採らなかったもの | なぜ |
|---|---|
| 既定は作業ツリーを切らない、本流で 1 枚ずつ | 上の穴からの妥協で目標ではない。常に 1 チケット = 1 作業ツリー |
| 採番を本流に 1 本化 | 親が main に居ない。親の識別子を名前空間にして `<親>-<連番>` で衝突を防ぐ |
| `executor`（実行者のモデル）の照合 | 起動前に止められないと参考でも分かっていて報告止まり。write の範囲だけの今回は入れない |
| `allow.ops`（実行してよい操作の分類）、敵対的レビュー、md + HTML の対、draft 解除 | ワークフローの側の話で、ガードの範囲外 |
| 15 種のタスクとフェーズ列のテンプレート | ccnavi は権限の道具で、作業の種類を知らなくてよい |
| 振り分けの宣言（entry skill）の強制 | ccnavi のルールと権限モードの委譲で足りる |

参考の `.claude/` を持つディレクトリをリポジトリの中に置くのは、参考自身が「攻撃面」と警告している形で、
実際に Claude Code がそこのスキル 28 本を読み込んでいた。`.gitignore` に入れた。**読み終えたら外へ出すこと。**

**フェーズに種類を与え、親が計画を持つようにした（2026-09-10、設計 §24.15）。** それまでの
フェーズは番号しか持たず、「2 番目の束」以上の意味が無かった。`.claude/ccnavi/phases.yml` に
種類（調査・設計・受入テスト作成・実装・フィードバック対応など）を定義し、親が `plan:` に
種類の並びを書く。それが全体計画で、`--approve` が通ることが合意。各フェーズの計画は、その
番号の子を提案して承認を受けること。用語は「フェーズの種類」（定義）と「フェーズ」（計画に
並ぶ番号付きの実体）で、「段」は使わない。

| 決めたこと | なぜ |
|---|---|
| 種類は人が持つ設定（`guard-approved` の内側）。組み込みの既定は無し | エージェントが種類を書けると、レビュー不要の種類を作ってから使える。既定を組み込むと意図せずレビューの要否が決まる |
| `kind: work` は全体計画に、`kind: feedback` はフィードバック計画にしか置けない | 対応の中で実装をやり直すなら `implement-feedback` のような対応の種類を定義する。線を種類の側に引く |
| 順序は承認で止める。N 番目の子は N-1 番目までが閉じてレビューが済むまで承認されない。`overlap` は例外 | 作業が終わるまで次の計画は立てない、をそのまま形にする。承認されたのに始められない子を作らない |
| 計画の項で `review: defer`（延期）が書ける。延期は省略ではなく、次にレビューがあるフェーズの依頼に含める。最後の項とレビュー不要の種類は延期できない | 対応が軽いとき「設計の対応は実装と一緒に見る」ができる。省略の道は作らない |
| フィードバック計画は全体計画の最後のレビューが済んでから 1 回だけ。指摘が無くても `feedback: []` を改版で出して承認を受ける | 作業をするまで対応は計画できない。空でも出すのは「見たうえで対応なし」の証跡を残すため。1 回だけなのは計画であって、承認済みのフィードバック作業フェーズの中で差し戻しを受けて子を足すのは何度でもできる |
| 改版は写しが動かない原則の唯一の例外。変えられるのは `plan` と `feedback` だけで、`plan` は子がまだ承認されていない番号の項に限る | 番号がずれると子の `phase` が指す先が変わる |
| フェーズの最後の子を閉じる前に、種類の `deliverables` が親か子の作業ツリーに在って追跡されているかを見る。中身は見ない | 空でも在ることは分かるので「調査したことにする」は塞げる |
| 残った指摘は `ccnavi-review.sh handoff` で別の issue に切り出す。exe が段階を確かめて下書きを書き、sh が issue を作って MR に引き継ぎの note を残す | 対応の連鎖を切る出口を機械が持つ |
| `--approve` は、読めない提案があって承認するものが無いとき 1 を返す | 壊れた提案を「何も無い」で通さない。テストで見つけた |
| 親はコンテキストを清潔に保ち、作業は必ず子に切り出す（§4 P12） | 親の範囲は広いので親が手を動かすと範囲の絞りが効かず、フェーズの終わりも子の閉じで数えている |

このリポジトリ自身の `phases.yml` も置いた（見本を兼ねる）。計画の無い親は今までどおり動く。
実物の GitLab に対しては引き続き未実測。

**人の手を 4 回から 3 回に減らした（2026-09-10）。** 1 周させたあとの見直しで、
push とマージリクエストの作成を親に渡した。人に残るのは範囲の承認・レビュー・
未解決の受け入れの 3 つ。

| 変えたこと | なぜ |
|---|---|
| `ccnavi-git.sh` が push を通す。ただし居るブランチをそのままの名前で送る形だけ。force / delete / all / mirror / tags / refspec は不可、`main` `master` `develop` `release*` へ直接は不可 | レビューはマージリクエストの実物に結ぶので、そこまでは親が運べたほうがよい。統合を決めるのは人なので、マージは人の側に残す。通さない形は「戻せなくなるもの」と「人の判断を飛び越すもの」の 2 つに絞った |
| Draft を外すのは親（`ccnavi-review.sh ready`）。条件は親を閉じられる条件と同じで、`ops.close_problems` を 2 つの経路で共有する。マージは人のまま | 「Draft を外す」は判断ではなく合図で、親が出せる。判断（マージ）は人に残す。条件を 2 つ持つと片方だけ緩む |
| `ready` は「`wip/` が追跡から消えている・未コミット無し・push 済み」も求め、GitLab では `squash` を立てる。wrapup は Draft を外さず、外すのは `ready` の 1 本 | 途中のコミットとチケットの置き場を既定のブランチに残さない（人は squash で取り込む）。締めた直後は状態の移動が未コミットで `wip/` も残っているので、そこで外すと「片付いていない MR」が外れる。道を 1 本にすれば、外れた MR は必ず片付いている |
| 人が締める道 `ccnavi-review.sh wrapup --reason`。未着手の子を取り消し、印を置き、未解決を受け入れ、残りを issue に写す。`phases/<親>/wrapup.json` があれば親はフィードバック計画が無くても閉じられる | 「まだ todo があるが、キリの良いところまでやった」は現実に起きる。検査を 1 つずつ人が迂回する形にすると、迂回した痕跡が散る。1 手で締めて、残りは普段の層（cancelled/・skipped・accepted・issue）に普段の形で残す |
| push は親だけ。ラッパは子チケットの作業ツリー（写しに `parent:`）からの push を拒み、hook はサブエージェントからの `ccnavi-git.sh push` を `DENY_SUBAGENT_TICKET_OP` で拒む | push を開けた直後の見直しで、子のツリーからも送れることが分かった。リモートに置く枝は親ブランチ 1 本で、子の枝が上がるとレビューの外にある枝ができる。ラッパはツリーで、hook は素性で見る 2 層にしたのは、サブエージェントが親のツリーへ `cd` して打つ形をラッパが見分けられないため |
| `review request` が、マージリクエストが無ければ作る（下書きで） | 人はレビューをそこで行う。入れ物が無いことで止める意味がない。題・本文・`Closes #<課題>` は親チケットから写す。`prepare` が控えの置き場に下書きを書き、標準出力の 2 行目でその綴りを返す |
| チケットに `issue` の欄（親だけ、省ける）を足した | `Closes #<番号>` を機械が書けるようにするため。マージで課題が閉じる |
| 受け入れたスレッドの控えを `phases/<親>/accepted.json` へ移した | 印に書いていたので、次に通った `check` が同じ印を `accepted: []` で上書きし、記録が飛んでいた。子を足したときの `clear_marks` でも消えていた。人が 1 度言った「これは承知で進める」は取り消されるまで残す |

**issue から close まで 1 周させて、3 つ落ちていた（2026-09-10）。** GitLab を実物で
立てようとしたが、Docker Desktop の VM が 2GB でホストの空きが 1GB しか無く、GitLab CE
（4GB 要る）が起動中に engine ごと落ちた。実物は諦め、GitLab API v4 の代役サーバを立てて
`ccnavi-review.sh` の HTTP 経路（curl・jq・ページング・投稿・認証）を実際に走らせ、
issue → MR → チケット → 計画 → 作業 → レビュー → 差戻 → 作業 → レビュー → クローズを 1 周した。
落ちていたのは 3 つ。

| 落ちていたこと | どう出たか |
|---|---|
| origin の綴りからポートが落ち、それがプロジェクトのパスの先頭に混ざり、scheme が https 固定だった | `http://localhost:8929/demo/greeter.git` が host=`localhost`、path=`8929/demo/greeter`、API=`https://localhost/api/v4` になる。手元や社内に平文で立てた GitLab は全滅。ssh の `git@host:g/p` の `:` はパスなので、数字だけのときにポートと見なす形で直した |
| 作業ツリーのルートからの相対パスが小文字になっていた | `README.md` が `readme.md` になり、チケットが `README.md` と宣言した範囲に**永久に当たらない**。`Dockerfile` も `src/Components/*` も同じ。`os.path.relpath` は比較にだけ normcase を使い返す綴りは元のままなので、normcase 済みの綴りを渡すのをやめて直した。区別しない機械では当てる側も揃える |
| 未解決の指摘を「依頼より後」だけで数えていた | 指摘が残ったまま、子をもう 1 本足して承認してもらい依頼をやり直すと、前回の指摘が数から消えて `check` が通った。人が解決も受け入れもしていないのに通る。時刻で絞ることをやめ、機構自身の投稿と人が受け入れた分だけを除く形にした |

**同じ 1 周で確かめたこと。** ゲートがサブエージェントの起動と普通のシェルを止め、スクリプト 3 本は
通ること。変更要求が立っている間は `--accept-unresolved` でも通らないこと。新しい子の承認で印が消え、
ゲートがまた閉じてレビューをやり直す形になること。`--approve` と `--reviewed` が端末を求めること。
`gh` / `glab` が入っていても未認証のホストでは curl とトークンへ落ちること。親を閉じるのは子が全部
閉じてゲートが開いてから、であること。

代役サーバは `tools/gitlab/fake_gitlab.py` に置いた。自動テストからは呼んでいない（人が手で 1 周させる道具）。
使い方はそのファイルの先頭に書いてある。

**実物の GitLab で 1 周した（2026-09-10、GitLab CE 18.5.4、Docker Desktop の VM を 4GB にして）。**
`tools/gitlab/probe_gitlab.py` が、一時リポジトリと使い捨てのプロジェクトを作り、sh 3 本と exe を本物に
当てる。人間役（レビュアー）は別ユーザのトークンで API を直に叩く。通ったのは、
push 前の `request` が前提で止まる → 親の push（ラッパ経由）→ `request` が Draft の MR を作り
（題 `Draft:`、本文に `Closes #課題`）依頼の note を投稿して印を置く → 2 度目は依頼済みで止まる →
レビュアーの討論で `check` が止まりゲートは閉じたまま → 解決と approve で `check` が通りゲートが開く →
`note` → もう 1 本の討論を `accept` で受け入れ、受け入れの note が MR に写り、以後 `check` で数えない →
親を閉じて `wip/` を消して push し `ready` で Draft が外れ `squash` が立つ → 別の親を `wrapup` で締めて
残りが issue になり、未着手の子が `cancelled/` へ動く。返ってきた JSON は sh の読み方と合っていた
（`discussions` の `notes[0].resolvable` と `resolved`、`reviewers` の `{user, state, created_at}`、
位置の無い討論は `path` 空・`line` 0）。

| 分かったこと | どうしたか |
|---|---|
| 変更要求（`POST .../request_changes`）は CE の `lib/api` に無い。EE 限定 | 当てられない。sh の `requested_changes` の読みは EE の文書どおりのまま。CE では `reviewers` の `state` は `unreviewed` / `reviewed` / `approved` だけ |
| URL にトークンを埋めた origin（`http://oauth2:<token>@localhost:8929/...`）で host にトークンが混ざり、`origin` の出力にそのまま出た | sh はユーザ情報を落とし、出力で伏せる。exe の `remote_kind` も読み飛ばす。`tests/test_review_origin.py` |
| ラッパ経由の push は `GIT_CONFIG_COUNT` を落とす（設定の注入を塞ぐため）ので、環境変数で credential helper を差し替えても効かず、`GIT_TERMINAL_PROMPT=0` で即失敗する | 認証は git の設定側に置く。probe はリポジトリの `credential.helper` を空文字で一度リセットしてから、トークンを返す helper を足す。実運用なら GCM に保存しておく |
| トークンは `docker exec -i gitlab gitlab-rails runner -` に Ruby を流し込んで作れる（`tools/gitlab/make_gitlab_tokens.rb`）。ブラウザも初期パスワードも要らない | GitLab 18 は組織（organization）とパスワードの強度を求める。root と reviewer の 2 人分を作る |
| 起動直後は API の `PUT` が 30 秒を超えることがあった | probe は 120 秒で 3 回まで待つ。sh の curl は無期限 |
| 未解決の一覧で、位置の無い討論が ` :0 ` と出る | 直していない。読めるので後回し |

**main の作業ツリーでの編集をルールで止めた（2026-09-11）。** 「編集は worktree で」は CLAUDE.md の
指示だけで、hook は main の Write / Edit を通していた（allow の `source` と `project-files`）。
ルールに `{root}`（ワークスペースルートの実パスに読み込み時に置き換わる合言葉）を足し、deny の `main-tree` で
「ワークスペースルートの下で、かつ `.claude/worktrees/` の外」を止める。ルールの契約が先読みを禁じているので、
ワークスペースルートの直下の 1 段目で場合分けする形（`.` で始まらない名前 / `.c` 以外で始まる隠し名 / `.cl` 以外 /
`.claude/` の下の `w` で始まらない名前）。worktree の中でも `.claude/settings*.json` は
`worktree-settings` で止める。hook・スクリプト・写しは既存の deny が場所を問わず当たる。
`source` と `project-files` は deny の陰で死ぬので消した。作業ツリーの中は `worktrees` の allow が
まとめて通す。`tests/test_root_placeholder.py` と `.claude/ccnavi/rule-samples.yml` に固定してある。
このセッション自身が main で編集していたのは、hook が dry-run だったから。enable に戻せば止まる。

**この 1 周で溶かした時間の内訳**（同じ道を戻らないために）。Docker Desktop を起動したら
利用者の GitLab が `restart=unless-stopped` で勝手に上がり、2GB の VM に収まらず engine ごと落ちた。
`docker exec` も `docker ps` も 500 を返すようになり、トークンを取る手立てが無くなった。
`docker desktop stop` で機械を元に戻してある（起動前と同じ状態）。
Windows のコンソール経由で日本語を引数に渡すと CP932 になり、`jq --arg` が UTF-8 でない
JSON を作る。本文はファイルで渡すこと。`jq` の実体は `C:\Program Files\jq\jq` で、
パスに空白がある。`"$JQ"` と引用しないと割れる（本番の sh は全部引用済み）。

**実行ファイルの境界を「自分のディレクトリの中」に引いた（設計 §4 P11）。** 2026-09-10、
`review check` が GitLab を exe の中から直接読みに行っていることが分かった時点で、そこを外へ出した。
思想はこう。**exe が自分で見て判断するのは、プロジェクトのディレクトリの中で把握できるものだけ。**
作業ツリー、git のオブジェクトと参照、承認済みの写し、印、hook の payload。その外にあるもの
（MR、スレッド、ホストの API、認証）は `.claude/scripts/` の sh が取ってきて写し（JSON）で渡し、
exe はその写しを材料にチケットと印を動かすところだけを持つ。理由は 3 つ。

1. 外との接続は実測でしか確かめられず、プロジェクトごとに違う。配布物の中に閉じると、壊れたときに
   exe を作り直すしかなく、直せるのが作った人だけになる。sh なら使う側が直せる
2. 写しの形を契約にすると、テストは sh の代わりに写しを渡すだけで本番と同じ経路を通る。
   代役という別の経路が要らない
3. 信頼の境界が言葉で言える。sh はルールで守られた場所にあり（`guard-scripts`）、写しを exe へ
   直接渡せるのは人の手だけ（`CCNAVI_GUARD_TICKET_APPROVAL`）。exe が約束するのは写しに対する判定であって、
   写しが実物と一致していることではない。それは sh と道具の側の責任

この線は今後も動かさない。exe に「外を見に行く」コードを足したくなったら、それは sh の仕事で、
exe に足すのは「その結果をどう読むか」だけ。

**並行するチケットを実装した。** 設計 §24 のとおりだが、書いてみて決まったことが 4 つ。

| 決めたこと | なぜ |
|---|---|
| 子の作業ツリーにある提案は読まない。同じ識別子が複数のツリーにあれば、親のツリー（親自身なら自分のツリー）の側を採る | 子は親のブランチから切るので、親の `wip/tickets/` がそのまま写っている。全部を数えると「複数の場所にある」で状態を動かせない |
| 子が親の部分集合かは、子の項の字義どおりの前置に 1 文字足した綴りを親に当てて決める（`src/b/*` → `src/b/x`） | 前置の末尾の `/` を落として当てると、親の `src/b/*` が `src/b` に当たらず、正当な子を「超えている」と読んだ |
| スクリプト 2 本は実行ファイルを探して渡すだけの薄い形。本体は `ccnavi ticket` と `ccnavi review` | 判定と同じ写しの読み方を 1 か所に保つ。`.git` の `gitdir:` から main を引くので、作業ツリーの中から呼んでも写しは main の側 |
| リモートの読み書きは `.claude/scripts/ccnavi-review.sh` に出し、実行ファイルはネットワークに出ない。sh が写し（JSON）を `--result` で渡し、実行ファイルは作業ツリーの中の前提検査と写しの判定と印だけを持つ | 実測できていない部分（API のパス、トークンの権限、ページング、セルフホストの差）が配布物の中に閉じ、壊れたときに実行ファイルを作り直すしかなかった。sh ならプロジェクトごとに直せる。写しの形が契約なので、テストは sh の代わりに写しを渡すだけで本番と同じ経路を通る |
| 依頼は `prepare`（前提の検査と本文の書き出し）→ sh が投稿 → `requested`（印）の 2 段 | 段の名前が違えば、どちらで止まったかが exit code を見なくても分かる |
| `check` / `requested` は、依頼の印にあるホストと MR の番号が写しと一致しなければ拒む | 写しは sh が作るファイルなので、別の MR の写しでゲートを開けない |
| sh は `gh` / `glab` を優先し、無ければ `curl` とトークン、それも無ければ MCP などを提案して止まる。道具は起動時に絶対パスへ固定する | 入れる前提を増やさない。PATH の細工で `curl` をすり替える道を 1 行で塞ぐ |
| 以前の `--review-fixture`（環境変数で指せた版は、エージェントが Bash の環境に付けて `check` を打てば偽のホストでゲートを開けられた） | `--result` に置き換わった。実行ファイルへ直接渡せるのは人の手だけ（`CCNAVI_GUARD_TICKET_APPROVAL`） |

**敵対的レビューの後に直したもの（ticket-hardening）。** exe を各プロジェクトに配って使う
前提で、サブエージェントによるレビューを 2 本走らせ、通ったものを直した。

| 直したこと | 理由 |
|---|---|
| `CCNAVI_GUARD_TICKET_APPROVAL`（既定 enable）。シェルから実行ファイルを `--approve` / `--reviewed` / `ticket …` / `review …` 付きで打つ形を `builtin-guard-ticket-approval` で止め、`--approve` と `--reviewed` は標準入力が端末であることを求める | スクリプト 2 本の中身は実行ファイルの呼び出しなので、スクリプトを経由せずに打てば全部の門が開いた。環境変数で切れるのは、テストと端末を持たない配管のため |
| 親の `done` / `cancel` は、開いている子か閉じたゲートがある間は拒む | 親の写しが閉じるとゲートの鍵（cwd から引く親）が消え、レビュー待ちのまま誰も止めなくなった |
| `check` は依頼時の HEAD と同じで push 済みであることを求める。`since` はホストの時計 | 依頼の後に積んだコミットが「レビュー済み」に含まれた。手元の時計だと、依頼直後の指摘が依頼より前に落ちて黙って除かれた |
| GitHub のレビューはレビュアーごとに最新だけ。DISMISSED は無い扱い。REST も GraphQL もページを最後まで読み、読み切れなければ拒む | 過去の全レビューを返すので、後で approve しても古い変更要求が残った。100 件で切れると 101 件目の未解決が見えなかった |
| GitLab の変更要求は `/reviewers` の `requested_changes` | 「GitLab に変更要求は無い」は誤り |
| 子のブランチは作業ツリーではなくブランチ名で引く。引けなければ前提の未充足 | 作業ツリーを消すと検査から外れた |
| `request` の未コミット検査は未追跡を数えない。`--body-file` の相対は `--cwd` から | 依頼文そのものを作業ツリーに置くと前提を落とした |
| 状態の移動は欄の行だけを書き換える（`set_fields`）。元を消せなければ書いた側を消して戻す | 読み直して書き出すと人の書いたコメントが消えた。両方に残ると以後の操作が全部止まった |
| 状態の置き場と例外・禁止の形は PowerShell にも当てる。ゲートの例外は読み切れないコマンドに与えない。状態ディレクトリは綴りまで同じものだけ読む | Windows で `DONE/` が `done/` として開け、PowerShell はシェルとして扱われていなかった |
| サブエージェントが差し戻しを無視して終わったことを、PostToolUse(Agent) で親に言う | 差し戻しは 1 回きりなので、2 度目の終了は黙って通っていた |
| 束の承認の鍵は最も重い親の識別子。子の画面の親は同じ束から引く | 識別子順の先頭にすると軽い親の識別子で重い親まで通った |
| `--lint` が、超えている子・深さ・ホストに合うトークン・SubagentStart/Stop の登録・スクリプトの有無・`CCNAVI_GUARD_TICKET_APPROVAL=disable` を言う | 承認の場で初めて知るより早く |

状態の移動（`start` / `done`）はスクリプトが置き場を動かすだけで、コミットは親がする。
参考の `ticket.sh` は自分でコミットしていたが、こちらは git ラッパが別にあるので分けた。

**実行後の監視は設計（ccnavi.md §18）と 4 か所で違う。** どれも要件書
（REQ-PST）は満たしている。設計側は実装が追いついてから直す。

| 設計 | 実装 | なぜ |
|---|---|---|
| 保護領域は project 設定の `target_directories` が宣言する | `match` に書き込み系ツールを含むルールがそのまま宣言になる | 宣言を 2 か所に分けると必ず食い違い、食い違った側は誰にも気づかれないまま緩む |
| `auto_restore` は off / warn / auto の 3 値 | `CCNAVI_RESTORE` は off / auto の 2 値 | 設計の warn は「通知して復元は AI にさせる」だが、戻す手順を示すことは REQ-PST-03 で必須なので、off でも同じことをする。同じ挙動に 2 つ名前を付けない |
| auto は新規ファイルを削除する | 退避して退避先を報告する | 消すと「保護領域を汚した実行」と「出力先を間違えただけで中身は要るもの」を見分けられないまま片方に決めることになる |
| 承認キャッシュを事後無効化する（§18.3） | していない | 無効にする記憶がまだ無い。REQ-PRE-07 と対で入れる |

**判定の間に外部プロセスを起こさない、を実行前に限定した。** 配布物の条件に
そう書いてあったが、作業ツリーの実物を見る手段は git しかない。自前で持つなら
保護領域に当たりうるファイルを毎回歩いて指紋を取り続けることになり、それは
git がすでに持っている情報を作り直すことになる。条件が守っていたのは
「呼び出しを止めている最中の遅れがガードの穴に化ける」ことで、実行後は
止めていないので、その理屈が掛からない。読めなければ何も言わないだけで穴も開かない。

**前から在った変更を差し戻さない。** 最初は `git status` に出たものを全部
「直前の実行が汚した」として返していた。このリポジトリで動かすと、他セッションの
書きかけを毎回「戻せ」と言うことになった。初回に控えを取り、以降に現れたものだけを
差し戻す形に変えた。控えの側も 1 度は伝えるが、文面を分けて戻すなと明示し、
自動復元の対象からも外す。控えは初回に必ず書く。違反が無いときに書き落としていて、
綺麗な作業ツリーで始まったセッションが永久に「初回」のままになり、その後の
汚れが全部「前から在ったもの」に化けていた。テストで見つけた。

**自動復元の `dry-run` が何も言わないままだった。** `CCNAVI_RESTORE_IF_DENY` は
3 値を取るのに、`post.py` は `enable` しか見ていなかった。`dry-run` と `disable` が
同じ挙動になっていて、3 値目が何も買っていない状態。中核ファイル側
（`CCNAVI_GUARD_CORE_FILES`）は `would-restore` を出していたので、片方だけ
抜けていた。報告に `would-restore` の行を足し、記録の `detail` にも件数を残した。

2 値（true / false）に落とす案も出たが、モードを掛けた表（requirements.md §2.2）の
「ブロック × 予行」が書けなくなる。判定は本番のまま戻しだけを試す状態で、対象が
プロジェクトの宣言次第で動くルール由来の保護では、そこがいちばん要る。

**監視は自分の書き込みを自分の違反にしない。** 記録も控えも `.claude/ccnavi/` に
あり、このリポジトリのルールはそこを保護領域として宣言している。塞がないと、
記録を 1 行足すたびに自分がそれを違反として報告し、その報告がまた記録を増やす。
無視設定にも入れてあるが、それだけに頼らない。

**ルールが読めないときは組み込みの既定に落とす（REQ-PRE-06）。** それまでは
enable で全ツール呼び出しが exit 2 で止まっていた。壊れたファイルを直すための
呼び出しも止まるのでセッションが死ぬ。既定モードが enable なので、ルールを置く前に
hook を登録しただけで踏む。要件書に「設定が読めないは判断できないではなく
設定が壊れているであり、拒否側へ倒すと壊れた設定を直す操作まで止まって
回復できなくなる」と書いてあったとおりのことをやっていた。

既定に入れるのは取り返しの付かない操作だけに絞った。ガードが落ちている最中は
ルールの一覧が信用できないので、広く止めても「なぜ止まったのか」を確かめる
手立てが利用者に無い。止める数ではなく、止めそこねたときに戻せないものを見る。

**Write / Edit によるルールファイルの編集は既定では通し、シェルからの書き込みは
止める。** 対になっている。通常のルールは前者を止めているが、既定で止めると
直す道が 1 本も残らない。一方でシェルを開けると、`echo x > .claude/ccnavi/rules.yml`
でルールを壊し、壊れた結果として緩んだ既定に落ちる、という順路ができる。
壊す側と直す側で経路を分けることでそれを閉じた。

**Go から Python へ移した。** 理由は 1 つで、コマンドの分割を自分で書きたくないから。
自作の字句解析はバグの元で、実際に 1 件踏んだ（`FOO="bar" cmd` を代入と読めなかった）。
Python の `shlex` はシェル自身の字句規則で、こちらが間違える筋合いのものではない。

移す前に測った。速度は理由にならない。Windows ではプロセス生成が支配的で、
Go の exe が 106〜146 ms/call、Python 3.12 が 130〜167 ms/call。差は誤差の中にある。
knowledge にある「Python 起動 0.5 秒」は `uv run` を挟んだ場合の数字で、
インタプリタを直接呼べば再現しない。

`shlex` が解いてくれる範囲も測った。引用とコメントと区切り記号は解ける。
`$( )` の括弧が区切り記号として出てくるので、コマンド置換の中身が
独立したコマンドとして扱われるのはただで付いてくる。引用を剥がして語を組み直すので、
`"git" push` を「特定できない」として諦める必要も無くなった。
縮退の理由が 3 つから 2 つに減っている。
一方でヒアドキュメントは `shlex` の概念に無いので、本文を落とす処理は自分で書いた。
このリポジトリがいちばん踏む誤検知がそこなので、移してもこの部分は消えない。

配布は PyInstaller の onedir。onefile は起動のたびにランタイムを展開するので
1 呼び出しあたり 1.0〜1.5 秒かかり、全ツール呼び出しに乗るので使えない。
onedir は約 220 ms で、Go より 80 ms ほど遅いだけ。REQ-HKS-04 の
「単一の実行ファイル」は「実行ファイルとその同梱物 1 フォルダ」に書き換えた。

**Python にして増えた危うさが 1 つある。** Go の RE2 は組み合わせ爆発を起こさないが、
Python の `re` は起こす。ルールの `regex` に病的な式を書くと判定が固まり、
hook が期限に達して素通りになる。`re` に時間制限は無く、C の中で GIL を握るので
別スレッドの見張りも動かない。今は先読み・後読み・後方参照を読み込み時に弾いて
書ける範囲を狭くしてあるだけで、爆発そのものは塞げていない。
ルールを書くのは人で、エージェントは `guard-config` のルールで
`.claude/ccnavi/*` の編集を止められているので、脅威というより足元の穴。

**常駐にしない。** セッション開始時に常駐させて以降を取り次ぐ案を検討して却下した。
`command` 型の hook である限り呼び出しごとのプロセス起動は消えず、
常駐はその上に通信を 1 往復足すだけになる。起動を実際に省けるのは通信型の hook だが、
それは判定を待ちの相手に預けることになり、相手が固まればガードが無言で消える。
速さが問題になったら、まず測り、次に正規化済みのルールをファイルへ落として
設定が変わるまで使い回す。常駐はその後。詳細は requirements.md の付録 C。

**設定は `.claude/settings.json` の `env` に置く。** 最初は
`.claude/ccnavi/settings.json` という別ファイルにしたが、settings.json が 2 つあるのが
分かりにくいという指摘で移した。`.claude/settings.json` に `ccnavi` のような
独自キーを足す案はスキーマ検証に弾かれる。`env` ブロックが唯一開いている場所。

**`disable` は settings.json から効かせない。** その設定ファイルは作業ツリーの中にあり、
エージェントが書き換えられ、変更は次のツール呼び出しから効く。
ccnavi は settings.json の `env` を自分で読み、そこに `disable` と書かれていたら
無視して理由を出す。ファイルが別の値を書いているのに環境が `disable` を渡した場合は、
その `disable` はファイル由来ではないので通す。

**モードの名前をガードの状態で言うようにした。** 以前は `off` / `warn` / `block` で、
ツール呼び出しがどうなるかを言っていた。判定に `deny` と `ask` の 2 つが入った時点で
その言い方が破綻する。`ask` で済む回に「ブロックするモード」と名乗るのは嘘だし、
`warn` は「警告する」と読めるが実際にやっているのは判定の予行演習でしかない。
旧い名前は受けない。読み手が 2 通りある状態を残さないため。残っている設定は
未知の値として `enable` に落ちるので、緩む側には倒れない。ただし `dry-run` の
つもりだった設定が止めはじめるので、名前を変えるときは設定の側も一緒に直す。

**やさしいパターン記法。** `\bgit\s+push\b` は書きにくいという指摘で
`git push *` と書けるようにした。記号は 3 つだけ。`*` が任意の文字列、
`?` が 1 文字、空白が任意の空白の並び。単語の切れ目は自動で見るので
`sed *` は `sedate` に当たらない。`/` は両方の区切り文字に当たる。
択一のように書けないものだけ `regex` フィールドに正規表現を書く。
先読み・後読み・後方参照は読み込み時に弾く。

## 実測で分かった落とし穴

次のセッションで同じところを踏まないように。

- **作業ツリーが消せない（Windows）。`git worktree remove` が `Permission denied` で落ち、
  `.venv` の 1 ファイルだけの抜け殻が残る。** 原因は uv のハードリンクと Windows の
  削除規則の組み合わせで、消そうとしている作業ツリーで**何も走っていなくても**起きる。
  2026-09-11 に隔離した場所で再現させて確かめた（`fsutil hardlink list` と、掴む側 /
  消す側を分けた実験）。
  1. uv は wheel の中身をキャッシュから venv へハードリンクで置く。実体は 1 つで、
     `_yaml.cp312-win_amd64.pyd` は main・全作業ツリー・uv のキャッシュで同じファイル
     （このプロジェクトで C 拡張を持つ依存は PyYAML だけなので、当たるのはこの 1 本）
  2. Windows は、実体が DLL として読み込まれている間、**どの名前も**消させない。
     rename は通る。Linux は mmap 中でも unlink できるので、ここは Windows だけの話
  3. 並行するセッションはターンの終わりに `test-py.sh` で数分テストを走らせ、
     そこで PyYAML を読み込む。作業ツリーが数本あると、ほぼ常に誰かが掴んでいる
  4. 掴まれている間に別の作業ツリーを消そうとすると、その 1 ファイルだけが残る
  対処は入れた（`pyproject.toml` の `[tool.uv] link-mode = "copy"`。複製にすれば実体が
  分かれる）。ただし**既にある `.venv` はハードリンクのまま**なので、効くのは次に作る
  ぶんから。いま在るものを切り替えるなら、テストが走っていないときに各作業ツリーの
  `.venv` を消して作り直す。
  それでも「自分のテストが走っている間に自分の作業ツリーを消せない」は残る。落ちたら、
  掴みが離れるのを待つか、抜け殻を `mv` で `.claude/worktrees/` の外へ出して
  `git worktree prune` する（rename は通るので、これは必ず成功する）。

- **`${CLAUDE_PROJECT_DIR}` は hook の `command` では展開されるが `env` では展開されない。**
  中括弧のままの文字列が渡る。`env` には相対パスを書く
- **`env` ブロックはライブリロードされない。** 値を変えてもセッションを開き直すまで
  古い値が残る。hook の `command` は即座に反映される。モード名を変えたときに
  古い値が残って未知の値になり、既定の block に落ちて自分の作業が止まった。
  ccnavi は未知のモード値を報告するようにしてあるので、次は原因がすぐ分かる
- **ツールのプロセスに `CLAUDE_PROJECT_DIR` は入っていない。** hook に入るかは未確認。
  だからワークスペースルートは上方向の探索でも見つけられるようにしてある
- **`.claude/settings.json` はスキーマ検証があり、未知のトップレベルキーを拒否する**
- **Windows は実行中の exe を上書きできない。** 名前の変更はできるので、
  `build.py` は組み上がったものを別の場所に作り、古いほうを `.old` に退避してから
  入れ替える。失敗しても数回やり直す
- **テストが session の環境を継承する。** 自分自身に仕掛けているので
  `CCNAVI_MODE` が入ってくる。テスト側でモードを固定してある
- **PyInstaller は指定したスクリプトをパッケージの外の素のスクリプトとして走らせる。**
  `ccnavi/__main__.py` を直接渡すと相対 import が解決できない。だから絶対 import で
  書いた `main.py` を root に置いて、それを渡している
- **`shlex.lineno` はトークンの後ろの空白まで進んだ位置を返す。** 行末のトークンは
  返ってきた時点で次の行に数えられている。ヒアドキュメントの本文を落とすときに
  「同じ行に残っている語」を判定するので、行番号はトークンを読む前に控える
- **`shlex` は引用された `<<` と素の `<<` を区別しない。** どちらも同じ文字列で
  返ってくるので、`grep -n "<<" README.md` はヒアドキュメントの始まりに見える。
  閉じない本文として縮退し、ヒアドキュメントのルールに当たって止まる。
  **これは直す対象ではない。** 許容する誤検知として設計に書いてある
  （ccnavi.md §12.3 ①、§23.1 L-7）。塞ぐにはトークンが引用されていたかを
  `shlex` から取り出す必要があり、公開された手段が無い。次に入る人が
  「バグだ」と思って追いかけないように、ここにも残しておく
- **算術式の `$((1 << 2))` は左シフトであってヒアドキュメントではない。**
  `$` `((` … `))` として落としてから読む。落とさないと本文の始まりに見えて、
  コマンド全体が読めなくなる
- **`shlex` は行継続を知らない。** `\` と改行をトークンの中に改行として残すので、
  語のつなぎ目として読まれてしまう。トークン化の前に 2 文字とも落とす
- **Python の識別子に空白は入らない。** テスト名を日本語で書くとき
  `def test_warn は…` のように英字と日本語の間に空白を入れると構文エラーになる
- **複合コマンドは 1 つの区間が読めなければ全体が縮退する。** `a && bash -c "..."`
  は `a` の側も生の文字列で判定される。縮退の向きが安全側なのでそのままにしてある
- **lint hook は編集 1 回ごとに走る。** 複数ファイルにまたがる変更は途中の状態で
  必ず差し戻される。順番に直せば通るので、途中の差し戻しは無視してよい
- **テストを編集ごとの hook に置かない。** 一度そうしていたが、途中の状態の失敗が
  毎回 40 行以上返ってきて、本当の失敗がその中に紛れた。Stop へ移してある

## セッション中に自分自身へ仕掛けたもの

`.claude/settings.json` に 4 つの hook が入っている。

- `PreToolUse` に `dist/ccnavi/ccnavi`。dry-run なので手を出さない。通知だけ返す
- `PostToolUse` に同じ `dist/ccnavi/ccnavi`。こちらは実行後の監視で、
  保護領域（`.claude/ccnavi/` と `.claude/hooks/`）が変わっていないかを見る。
  warn なので差し戻さず報告だけ返す。`matcher` は絞らない。絞ると Bash が抜ける
- `PostToolUse` の `Write|Edit` に `.claude/hooks/lint-py.sh`。
  Python ファイルの編集時だけ動き、整形と検査をかける
- `Stop` に `.claude/hooks/test-py.sh`。ターンの終わりに 1 回だけテストを走らせる

検査もテストも、通らないと exit 2 で差し戻される。実際に何度も差し戻された。
うるさければ `.claude/settings.json` の hooks から外す。

Stop の差し戻しには上限がある。3 回で打ち切って止まらせる。直せない失敗を無限に
差し戻すと、同じ場所を往復して人の手が入る機会が来ない。回数はセッションごとに
`.claude/ccnavi/stop-retries/` に置いて数える。payload の `stop_hook_active` は
真偽値でしかなく「何回目か」を持たないため。

数の一生は次のとおり。作るのは差し戻すときだけ。消えるのは、テストが通ったとき、
新しい連鎖が始まったとき（`stop_hook_active` が false）、上限に達したとき、
そして 1 時間経ったとき。最後の 1 つが要るのは、連鎖の途中でセッションが
終わるとファイルが取り残されるから。その session_id は二度と現れないので
誰も消さず、1 セッションにつき 1 個ずつ溜まる。

セッションごとに分けてあるので、並行して動いている別セッションと数が混ざらない。

**どのツリーをテストするかも、そのターンで触ったものだけに絞ってある。**
`CLAUDE_PROJECT_DIR` は worktree で作業していても元のディレクトリを指したままなので、
そこだけをテストすると、直したツリーが検査されないまま通る。逆に在るツリーを
全部テストすると、触ってもいない他セッションの書きかけでこちらが差し戻される。
どこを触ったかは `PostToolUse` の `lint-py.sh` が `<session>.trees` に書き残し、
`Stop` の `test-py.sh` がそれを読む。ツリーは編集したファイルから
いちばん近い `pyproject.toml` を上に辿って決める。`.claude/worktrees/` という
置き場の決まりを焼き込んでいないので、決まりを変えてもここは古くならない。

実行ファイルはどちらの hook でも作り直さない。PyInstaller が 11 秒かかるので、
編集 1 回あたり 20 秒近くになっていた。動かして確かめるときに手で
`uv run --with pyinstaller python build.py` を回す。

`dist/`、`build/`、`.claude/ccnavi/log.jsonl` は git 管理外。
記録には絶対パスとコマンド全文が入るのでコミットしない。
