# ccnavi

Claude Code の hook から呼ばれ、危ないツール呼び出しを止めて代わりの道を返すガード。
用語集。実装は ccnavi.md、要求は requirements.md、判断の理由は docs/adr/。置き場のパスと環境変数は ccnavi.md 4、9.2、11.2。

## 用語

### 判定の層

**全体ルール**:
ルールファイルで宣言され、hook がすべてのツール呼び出しに当てる deny / ask / allow。
チケット制御の対になる語。どの層に書いたかは問わない。
使わない言い方: 共通ルール、グローバルルール

**層**:
設定 3 本（ルール、フェーズの種類、リスクの配点）を 1 組で置く場所。
共通層・自身の層・プロジェクトの層の 3 種がある。3 本は独立に無くてよく、無い層は空。
使わない言い方: レイヤー、設定セット（状態を持つ場所の「2 層」（ccnavi.md 9.6）は別の話）

**共通層**:
ワークスペースルートの ccnavi の設定の置き場に置く層。どのツリーにも効く。名前は `common` で、識別子に接頭辞は付かない。
使わない言い方: 全体層、グローバル層、ワークスペースの層（自身の層と紛れる）

**自身の層**:
ワークスペースルートに、プロジェクトの層と同じ形で置く層。
ワークスペース自身のツリー（ワークスペースルートと、そこから切ったワークツリー）にだけ効く。名前は `self`。
使わない言い方: ワークスペースの層、self 層

**プロジェクトの層**:
各プロジェクトの git プロジェクトルートに置く層。そのプロジェクトのツリー（プロジェクト本体と、そこから切ったワークツリー）にだけ効く。
名前はプロジェクトの名前。`common` と `self` は予約してあり、その名前のプロジェクトは層として数えない。
使わない言い方: リポジトリの層、プロジェクトのルール（ルール以外の 2 本も持つ）

**層の和**:
共通層に、そのツリーの層を足したもの。判定はこれで行う。足すだけで上書き・取り消しは無い。
パスを持つツール（Read / Grep / Glob / Write / Edit / NotebookEdit）は共通層 + 行き先の 1 層、パスを持たないツール
（Bash / PowerShell / WebFetch / Skill / Agent）は共通層 + 全部の層。フェーズの種類と配点は共通層 + 親の承認済みチケットの `project:` の層。
使わない言い方: マージ、上書き、継承

**チケット制御**:
提案を承認して承認済みチケットを作り、その範囲・フェーズの HITL ポイント・サブエージェントの制限を判定に掛ける働き全体。
ワークスペースごとに使うかを決める（プロジェクトごとには切り替えない）。
使わない言い方: チケットによる範囲の制御、チケットの範囲、チケット管理（拡張の入口と、そこから開く画面の名前としてだけ使う）

**保護領域**:
「ここに書かせない」と宣言された場所。別の設定では宣言せず、書き込み系ツールを `match` に含む
deny / ask のルールがそのまま宣言になる。実行後の監視が見るのはここ。
使わない言い方: 保護対象ディレクトリ、監視対象

**コアファイル**:
ルールの外で、組み込みで守るファイル。hook の登録・各層の設定 3 本・実行ファイル。ルールを空にされても守りは消えない。
使わない言い方: 中核ファイル、重要ファイル、システムファイル

**縮退**:
Bash のコマンドを実行される部分まで読み切れなかったとき、生の文字列との一致で判定すること。
縮退した呼び出しに allow は当てない。
使わない言い方: フォールバック、劣化

### 作業の進め方

**直接作業**:
チケットを起こさず、全体ルールだけの判定で進める作業。調査と小さな修正。
ワークスペースルート直下と、承認済みチケットの無いワークツリーでの作業がこれにあたる。
使わない言い方: チケット無しの作業、軽い作業

**チケット作業**:
提案を書いて承認を受け、フェーズとリスクの配点に従って issue とマージリクエストを作りながら進める作業。
使わない言い方: 重い作業、正式な作業

### チケットの持ち物

**提案**:
エージェントが各ワークツリーの提案の置き場（`todo/`）に書く、これから効かせたい範囲の宣言。人が読んで承認する。
承認されると提案の置き場から承認済みチケットの置き場へ動く（写しは残らない。ADR-0055）。
使わない言い方: チケットファイル、下書き

**承認済みチケット**:
人が承認して承認済みチケットの置き場（`doing/`）へ動かした提案そのもの。判定が読むのはこれだけ。
作業が終わると提案の置き場の `review/` へ、人が見ると承認済みチケットの置き場の `done/` へ動く。
承認済みチケットの置き場へ動かすのは人、提案の置き場へ動かすのはエージェント。
使わない言い方: 写し、複製、台帳

**レビュー待ちのチケット**:
エージェントが `finish` で閉じ、提案の置き場の `review/` に在るチケット。次に動くのは人。判定の範囲としては効かない。
使わない言い方: 完了チケット、done

**マーカー**:
フェーズの進み（依頼済み・レビュー済み・省略）と、親の締め（閉じた・下書きを外した・人が締めた）を表す、
承認済みチケットの置き場の下のファイル。
使わない言い方: 印、マーク、フラグ

**先行**:
子チケットが `predecessors` に挙げる、先に閉じているべき子。承認と着手は、先行が全部 `.ccnavi/approved/done/` に在って
取り消しでないことを求める（ADR-0088）。レビュー待ちは満たさない。ボードでは「先行待ち」と見せる。
使わない言い方: 依存チケット、前提チケット、ブロッカー

**状態の跡**:
実行ファイルがチケットの置き場を動かすたび、マーカーを置くか消すたびに、承認済みの領域の `events/<識別子>.ndjson` へ
追記する 1 行。いつ・どの経路で・どこからどこへ動いたかを残す補助で、状態の正は置き場（ADR-0086）。ボードでは「履歴」と見せる。
使わない言い方: イベントログ、監査ログ（判定の記録 `logs/log.jsonl` と紛れる）、台帳

**ワークツリー**:
チケット 1 本のために切る git worktree。名前はチケットの識別子と同じ。判定の鍵はファイルの
行き先がどのワークツリーか。
使わない言い方: ブランチ、作業ツリー（git の working tree と紛れる。下の「作業ツリー」を見よ）、
worktree（英語の綴りは git のコマンド名としてだけ使う）

**レビュー準備中**:
人間レビュー要のフェーズが終わってから、レビューの依頼を出すまでの間。サブエージェントの起動とシェル実行は
止まり、次に動くのはエージェント（合流・push・依頼）。このセッションで見るフェーズは、人が端末で先へ進めるまでここに居る。
依頼を出せば「レビュー待ち」へ移る。
使わない言い方: ゲート閉、足止め、ロック、ブロック

**レビュー待ち**:
レビューの依頼を出してから、レビュー済みのマーカーが置かれるまでの間。次に動くのは人。
使わない言い方: ゲート閉、依頼済み待ち

**HITL ポイント**:
人の手が入るところ（Human In The Loop）。範囲の承認・レビュー・未解決の受け入れの 3 か所（ADR-0030）。
フェーズの終わりのレビューでは、レビュー準備中 → レビュー待ち → レビュー済みと進む。
判定とボードの綴りは `gate_closed`。
使わない言い方: ゲート、足止め、ロック、ブロック

**マージリクエスト**:
レビューを行う場所の 1 つ。GitHub の pull request も同じ語で呼ぶ。ホストの違いは origin の URL で見分ける。
使わない言い方: MR、PR、プルリクエスト

**見る場所**:
フェーズの終わりに人がどこでレビューするか。見ない・このセッション・マージリクエストの 3 つ。
フェーズの種類が宣言し、厳しい側が勝つ。
使わない言い方: レビュー先、レビュー方式

**このセッションで見る**:
差分を Claude Code のセッションの中で人が見て、端末から先へ進めるレビューの受け方。
使わない言い方: ローカルレビュー、chat レビュー（設定に書く綴りとしての `chat` は別）

### フェーズ

**フェーズの種類**:
設定ファイルに定義された名前付きの型。親の計画に並べる。
使わない言い方: 段、ステージ

**フェーズ**:
親の計画に並んだ番号付きの実体。同じ親の同じ番号の子チケットのまとまり。
使わない言い方: 段階、ステップ

**局面**:
親チケットが計画のどこにいるか。全体計画待ち・作業中・レビュー準備中・レビュー待ち・
フィードバック計画待ち・フィードバック対応中・閉じられる。
使わない言い方: 段階、ステータス

**リスクレベル**:
実績で測ったリスクの高さ。LOW / MEDIUM / HIGH / CRITICAL の 4 つ。「レベル」とだけ書かない。
使わない言い方: 等級、段階、ランク

**境目の点**:
リスクの点がそれ以上になるとリスクレベルが 1 段上がる値。`levels` の medium / high / critical の 3 つで、
既定は 20 / 40 / 70。判定は「以上」。
使わない言い方: 閾値、しきい値、基準

**基準**:
配点の項目で、この数を超えたら加点する数。`lines_over` / `files_over` / `deleted_over` の値。判定は「超え」。
使わない言い方: 閾値、しきい値、境目

### 場所

**ワークスペースルート**:
Claude Code を起動した場所。hook の登録、実行ファイル、保護済みスクリプトはここにある。
使わない言い方: main、リポジトリのルート

**プロジェクト**:
プロジェクト置き場（`projects/`）の直下に clone した、ワークスペースとは別のリポジトリ。
ツリー 3 種のうちの 1 つで、記録と層の上の名前はこの名前。
使わない言い方: サブリポジトリ、子リポジトリ、git プロジェクトルート（あれは 3 種の総称）

**ツリー**:
判定の単位になる場所。ワークスペースルート・プロジェクト・ワークツリーの 3 種。
所属は実パスの最長一致で決まる。
使わない言い方: リポジトリ、チェックアウト

**git プロジェクトルート**:
`.git` がある場所。ワークスペース自身・各プロジェクト・ワークツリーの 3 種の総称。
「ワークツリーでない側」を言うときは**元リポジトリ**を使う。
使わない言い方: リポジトリルート、プロジェクト（3 種の 1 つを指すときはそちら）

**作業ツリー**:
git の working tree。索引と対になる側で、ツリー 3 種のどれでもありうる。
実行後の監視が `git status` で読むのはこれ。
使わない言い方: ワークツリー（あれはチケットのための git worktree）

**元リポジトリ**:
そのワークツリーがどのリポジトリに属するか。ワークスペースルートかプロジェクトのどちらかで、
`.git` ファイルの `gitdir:` が指す先で確かめる。層の選択・`project:` の照合・ワークツリー側の
設定を探す先は、これで決まる。
使わない言い方: 切り元、親リポジトリ（親チケットと紛れる）、ベース、
元ブランチ（ブランチではない。ブランチは判定では見ない。requirements.md 4 章）
