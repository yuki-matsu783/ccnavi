# ccnavi

Claude Code の hook から呼ばれ、危ないツール呼び出しを止めて代わりの道を返すガード。
ここは用語集だけを置く。実装の判断は ccnavi.md、外から観測できる要求は requirements.md。

## Language

### 判定の層

**全体ルール**:
ルールファイル（`rules.yml`）で宣言され、hook がすべてのツール呼び出しに当てる deny / ask / allow。
全プロジェクトが使う。
_Avoid_: 共通ルール、グローバルルール

**チケット制御**:
提案を承認して写しを作り、その範囲・フェーズのゲート・サブエージェントの制限を判定に掛ける働き全体。
プロジェクトごとに使うかを決める（`CCNAVI_TICKET_CONTROL`）。
_Avoid_: チケットによる範囲の制御、チケットの範囲、チケット管理（拡張の入口の名前としてだけ使う）

### 作業の進め方

**直接作業**:
チケットを起こさず、全体ルールだけの判定で進める作業。調査と小さな修正。
main 直下と、写しの無い作業ツリーでの作業がこれにあたる。
_Avoid_: チケット無しの作業、軽い作業

**チケット作業**:
提案を書いて承認を受け、フェーズとリスクの配点に従って issue と MR を作りながら進める作業。
_Avoid_: 重い作業、正式な作業

### チケットの持ち物

**提案**:
エージェントが各作業ツリーの `wip/tickets/` に書く、これから効かせたい範囲の宣言。人が読んで承認する。
_Avoid_: チケットファイル、下書き

**写し**:
人が承認した提案の、ワークスペースルート側（`.claude/ccnavi/tickets/`）にある複製。判定が読むのはこれだけ。
_Avoid_: 承認済みチケット、台帳

**印**:
フェーズの進み（依頼済み・レビュー済み・省略）と親の締めを表す、写しの置き場の下のファイル。
_Avoid_: マーク、フラグ

**作業ツリー**:
チケット 1 本のために `.claude/worktrees/<識別子>` に切る git worktree。判定の鍵はファイルの行き先がどの作業ツリーか。
_Avoid_: ブランチ、ワークツリー

**ゲート**:
人間レビュー要のフェーズが終わってから印が置かれるまでの間、サブエージェントの起動とシェル実行を止める状態。
_Avoid_: ロック、ブロック
