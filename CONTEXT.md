# ccnavi

Claude Code の hook から呼ばれ、危ないツール呼び出しを止めて代わりの道を返すガード。
ここは用語集だけを置く。実装の判断は ccnavi.md、外から観測できる要求は requirements.md。

## Language

### 判定の層

**全体ルール**:
ルールファイル（`rules.yml`）で宣言され、hook がすべてのツール呼び出しに当てる deny / ask / allow。
どのワークスペースでも使う。チケット制御の対になる語で、「チケットの範囲ではなくルールで判定する側」を指す。
どの層に書いたかは問わない。共通層・自身の層・プロジェクトの層のどれに書いたルールも全体ルール。
_Avoid_: 共通ルール、グローバルルール

**層**:
設定 3 本（ルール `rules.yml`、フェーズの種類 `phases.yml`、リスクの配点 `risk.yml`）を 1 組で置く場所。
共通層・自身の層・プロジェクトの層の 3 種がある。3 本は独立に無くてよく、無い層は空。
_Avoid_: レイヤー（設計書 §6〜§11 の Layer 0〜3 は別の話）、設定セット

**共通層**:
`.claude/ccnavi/` に置く層。どのツリーにも効く。記録と id の上の名前は `common` で、id に接頭辞は付かない。
_Avoid_: 全体層、グローバル層、ワークスペースの層（自身の層と紛れる）

**自身の層**:
ワークスペースルートの `.ccnavi/config/` に置く層（`.ccnavi` は `CCNAVI_PROJECT_HOME` の既定値）。
ワークスペース自身のツリー（ワークスペースルートと、そこから切った作業ツリー）にだけ効く。名前は `self`。
_Avoid_: ワークスペースの層、self 層

**プロジェクトの層**:
`projects/<名前>/.ccnavi/config/` に置く層。そのプロジェクトのツリー（git プロジェクトルートと、そこから切った作業ツリー）にだけ効く。
名前はプロジェクトの名前。`common` と `self` は予約してあり、その名前のプロジェクトは層として数えない。
_Avoid_: リポジトリの層、プロジェクトのルール（ルール以外の 2 本も持つ）

**層の和**:
共通層に、そのツリーの層を足したもの。判定はこれで行う。足すだけで、後ろの層が前の層を上書きしたり取り消したりはしない。
Write / Edit / NotebookEdit は共通層 + 行き先の 1 層、Bash は共通層 + 全部の層。フェーズの種類と配点は共通層 + 親の `project:` の層。
_Avoid_: マージ、上書き、継承

**チケット制御**:
提案を承認して写しを作り、その範囲・フェーズのゲート・サブエージェントの制限を判定に掛ける働き全体。
ワークスペースごとに使うかを決める（`CCNAVI_TICKET_CONTROL`）。`projects/<名前>` ごとには切り替えない。
_Avoid_: チケットによる範囲の制御、チケットの範囲、チケット管理（拡張の入口の名前としてだけ使う）

### 作業の進め方

**直接作業**:
チケットを起こさず、全体ルールだけの判定で進める作業。調査と小さな修正。
main 直下と、写しの無い作業ツリーでの作業がこれにあたる。
_Avoid_: チケット無しの作業、軽い作業

**チケット作業**:
提案を書いて承認を受け、フェーズとリスクの配点に従って issue と MR を作りながら進める作業。
_Avoid_: 重い作業、正式な作業

### チケットの持ち物

**提案**:
エージェントが各作業ツリーの `wip/tickets/` に書く、これから効かせたい範囲の宣言。人が読んで承認する。
_Avoid_: チケットファイル、下書き

**写し**:
人が承認した提案の、ワークスペースルート側（`.claude/ccnavi/tickets/`）にある複製。判定が読むのはこれだけ。
_Avoid_: 承認済みチケット、台帳

**印**:
フェーズの進み（依頼済み・レビュー済み・省略）と親の締めを表す、写しの置き場の下のファイル。
_Avoid_: マーク、フラグ

**作業ツリー**:
チケット 1 本のために `.claude/worktrees/<識別子>` に切る git worktree。判定の鍵はファイルの行き先がどの作業ツリーか。
_Avoid_: ブランチ、ワークツリー

**ゲート**:
人間レビュー要のフェーズが終わってから印が置かれるまでの間、サブエージェントの起動とシェル実行を止める状態。
_Avoid_: ロック、ブロック
