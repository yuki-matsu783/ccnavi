# ccnavi

Claude Code の hook から呼ばれ、危ないツール呼び出しを止めて代わりの道を返すガード。
ここは用語集だけを置く。実装の作りは ccnavi.md、外から観測できる要求は requirements.md、
判断の理由と経緯は docs/adr/。置き場のパスと環境変数の名前はここには書かない（ccnavi.md §4、§9.2、§11.2）。

## 用語

### 判定の層

**全体ルール**:
ルールファイルで宣言され、hook がすべてのツール呼び出しに当てる deny / ask / allow。
どのワークスペースでも使う。チケット制御の対になる語で、「チケットの範囲ではなくルールで判定する側」を指す。
どの層に書いたかは問わない。共通層・自身の層・プロジェクトの層のどれに書いたルールも全体ルール。
使わない言い方: 共通ルール、グローバルルール

**層**:
設定 3 本（ルール、フェーズの種類、リスクの配点）を 1 組で置く場所。
共通層・自身の層・プロジェクトの層の 3 種がある。3 本は独立に無くてよく、無い層は空。
使わない言い方: レイヤー、設定セット（状態を持つ場所の「3 層」（ccnavi.md §9.6）は別の話）

**共通層**:
ワークスペースルートの ccnavi の設定の置き場に置く層。どのツリーにも効く。記録と識別子の上の名前は `common` で、識別子に接頭辞は付かない。
使わない言い方: 全体層、グローバル層、ワークスペースの層（自身の層と紛れる）

**自身の層**:
ワークスペースルートに、プロジェクトの層と同じ形で置く層。
ワークスペース自身のツリー（ワークスペースルートと、そこから切った作業ツリー）にだけ効く。名前は `self`。
使わない言い方: ワークスペースの層、self 層

**プロジェクトの層**:
各プロジェクトの git プロジェクトルートに置く層。そのプロジェクトのツリー（git プロジェクトルートと、そこから切った作業ツリー）にだけ効く。
名前はプロジェクトの名前。`common` と `self` は予約してあり、その名前のプロジェクトは層として数えない。
使わない言い方: リポジトリの層、プロジェクトのルール（ルール以外の 2 本も持つ）

**層の和**:
共通層に、そのツリーの層を足したもの。判定はこれで行う。足すだけで、後ろの層が前の層を上書きしたり取り消したりはしない。
Write / Edit / NotebookEdit は共通層 + 行き先の 1 層、Bash は共通層 + 全部の層。フェーズの種類と配点は共通層 + 親の承認済みチケットの `project:` の層。
使わない言い方: マージ、上書き、継承

**チケット制御**:
提案を承認して承認済みチケットを作り、その範囲・フェーズのゲート・サブエージェントの制限を判定に掛ける働き全体。
ワークスペースごとに使うかを決める。プロジェクト置き場の下のプロジェクトごとには切り替えない。
使わない言い方: チケットによる範囲の制御、チケットの範囲、チケット管理（拡張の入口の名前としてだけ使う）

**保護領域**:
「ここに書かせない」と宣言された場所。別の設定では宣言せず、書き込み系ツールを `match` に含む
deny / ask のルールがそのまま宣言になる。実行後の監視が見るのはここ。
使わない言い方: 保護対象ディレクトリ、監視対象

**中核ファイル**:
ルールの外で、組み込みで守るファイル。hook の登録・各層の設定 3 本・実行ファイル。ルールを
空にされても守りが消えないように、守りの根拠を守られる対象の外に置いたもの。
使わない言い方: 重要ファイル、システムファイル

**縮退**:
Bash のコマンドを実行される部分まで読み切れなかったとき、生の文字列との一致に落ちること。
縮退した呼び出しに allow は当てない。
使わない言い方: フォールバック、劣化

### 作業の進め方

**直接作業**:
チケットを起こさず、全体ルールだけの判定で進める作業。調査と小さな修正。
ワークスペースルート直下と、承認済みチケットの無い作業ツリーでの作業がこれにあたる。
使わない言い方: チケット無しの作業、軽い作業

**チケット作業**:
提案を書いて承認を受け、フェーズとリスクの配点に従って issue とマージリクエストを作りながら進める作業。
使わない言い方: 重い作業、正式な作業

### チケットの持ち物

**提案**:
エージェントが各作業ツリーに書く、これから効かせたい範囲の宣言。人が読んで承認する。
使わない言い方: チケットファイル、下書き

**承認済みチケット**:
人が承認した提案の、ワークスペースルート側にある複製。判定が読むのはこれだけ。
使わない言い方: 写し、台帳

**印**:
フェーズの進み（依頼済み・レビュー済み・省略）と親の締めを表す、承認済みチケットの置き場の下のファイル。
使わない言い方: マーク、フラグ

**作業ツリー**:
チケット 1 本のために切る git worktree。名前はチケットの識別子と同じ。判定の鍵はファイルの
行き先がどの作業ツリーか。
使わない言い方: ブランチ、ワークツリー、worktree（英語の綴りは git のコマンド名としてだけ使う）

**ゲート**:
人間レビュー要のフェーズが終わってから印が置かれるまでの間、サブエージェントの起動とシェル実行を止める状態。
使わない言い方: ロック、ブロック

**マージリクエスト**:
レビューを行う場所。GitHub の pull request も同じ語で呼ぶ。ホストの違いは origin の URL で見分ける。
使わない言い方: MR、PR、プルリクエスト

### フェーズ

**フェーズの種類**:
設定ファイルに定義された名前付きの型。親の計画に並べる。
使わない言い方: 段、ステージ

**フェーズ**:
親の計画に並んだ番号付きの実体。同じ親の同じ番号の子チケットの束。
使わない言い方: 段階、ステップ

**局面**:
親チケットが計画のどこにいるか。全体計画待ち・作業中・レビュー待ち・フィードバック計画待ち・
フィードバック対応中・閉じられる。
使わない言い方: 段階、ステータス

**等級**:
実績で測ったリスクの高さ。LOW / MEDIUM / HIGH / CRITICAL の 4 つで、名前は固定。
使わない言い方: 段階、レベル、ランク

### 場所

**ワークスペースルート**:
Claude Code を起動した場所。hook の登録、実行ファイル、保護済みスクリプトはここにある。
使わない言い方: main、リポジトリのルート

**git プロジェクトルート**:
`.git` がある場所。ワークスペース自身、プロジェクト置き場の下の各プロジェクト、作業ツリーの
それぞれが持つ。
使わない言い方: リポジトリルート

**切り元**:
その作業ツリーがどの git プロジェクトルートから切られたか。`.git` ファイルが指す先で確かめる。
使わない言い方: 親リポジトリ、ベース
